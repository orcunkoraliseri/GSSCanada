# 1J — Revision Plan (JBPS, reject-and-resubmit)

**Paper:** *Longitudinal Analysis of Occupancy-Driven Energy Demand in Canadian Residentials*
**Journal:** Journal of Building Performance Simulation (JBPS), manuscript 266775447
**Decision:** 2026-09-19. Rejected in its current form, but a resubmission after major revision is invited (editor J. Hensen). One reviewer's report is received. A second report is overdue and will be forwarded if it arrives.
**Deadline:** 12 months, so by **2026-09-19 + 12 months = 2027-09-19**.
**Resubmission rules:** mark every change (track changes, or bold/colour text). The response letter goes to the referees, so it must **not name the authors**. Before uploading, delete the redundant original files from the submission system.
**Link:** https://rp.tandfonline.com/submission/flow?submissionId=266775447&step=1

> This file is the single working document for the revision. Edit it in place and tick the boxes as you go.

---

## 0. Before touching the text: which version was submitted?

- [x] **Settled 2026-09-19.** The submitted file is the journal's own PDF, `IMP/86e336cb-ac8c-4230-946f-20ff060f5bc4.pdf` (supplied by the author; built by the submission system on 2026-06-05). It has 29 pages: 1 cover sheet plus manuscript pages "1 of 28" to "28 of 28". **PDF page N = manuscript page N−1.** It holds 21 main-text figures, 9 appendix figures, 4 tables and 28 references.
- The text of the PDF matches **both** drafts word for word wherever checked: title, abstract, highlights, headings, methods, results, discussion, conclusion and the reference list. The garbled equation and the odd ref [14] are in the source drafts, so they are not PDF conversion errors. No added or removed sections and no changed numbers were found.
- The source of the PDF is most likely `manuscript/submission_Occ_NUsJournal.docx` (last saved 2026-06-03, two days before the PDF). `manuscript/1st_Occ_Journal.md` has the same text and stays the reading copy.
- **All page references below ("p. N") are PDF pages.** Section numbers are the same in the PDF and the drafts.
- [ ] Revise `submission_Occ_NUsJournal.docx` in track-changes mode, because the journal wants the changes shown. Use the .md only as a reading copy.
- Not checked: figures and tables were not inspected page by page (only pages 11–12 were viewed as images).

---

## 1. Overall strategy (one sentence the whole revision serves)

The reviewer's four comments point to one repositioning:

> **The contribution is a longitudinal-plus-projective occupant-behaviour (OB) pipeline for Canada (four empirical GSS cycles, 2005–2022, plus a validated generative 2025 projection). EnergyPlus is a demonstration of the pipeline's consequences, not a new simulation method.**

What this means in practice:
1. **Make the literature section say what exists already** (comments 1 and 3). Add a comparison table and state the gap in one sentence.
2. **Make the data section auditable** (comment 2). Say where the data come from, how many records there are, what the variables mean, and how representative they are of Montreal/Quebec.
3. **Strengthen the part the reviewer calls the strongest and least validated**, the C-VAE/CBVM projection (comment 4). Add quantitative hindcast validation against simple baselines.
4. **Shrink the EnergyPlus part** (comment 4) into a focused section on what the pipeline changes. Move the rest to the appendix.
5. **Remove claims of a new paradigm.** Drop "first" (p. 22), and "replicable" and "nationally representative" (p. 3, p. 22). The words "new modelling paradigm" do not appear in the submitted PDF; the reviewer is reacting to the overall framing, so the fix is the repositioning itself. Soften the calibration factors for building codes (±10/20 %), because they come from one city and one weather file.

Suggested new titles (author picks one; changing the title makes the repositioning visible):
- *A longitudinal and projective occupant-behaviour pipeline from Canadian time-use surveys (2005–2025) and its implications for residential energy simulation*
- *Two decades of Canadian residential occupancy from time-use surveys, with a generative 2025 projection: patterns, validation and energy implications*

---

## 2. Comment-by-comment plan

### Comment 1: Little engagement with TUS/ATUS literature; cite Vosoughkhosravi, Jafari & Zhu (2023)

**What the manuscript has now:** Sections 1.1 to 1.4 are generic occupancy-modelling background with 28 references. The ATUS review the reviewer asks for is not cited. Mitra/Chu/Cetin appears only once, as ref [6]. Osman & Ouf (2021) is cited as [3] but not used to position the paper. The introduction never says "prior TUS studies did X; we add Y".

**Actions**
- [ ] Rewrite Section 1 into: 1.1 TUS-based occupancy modelling (the Markov lineage, clustering, generative); 1.2 Longitudinal and multi-cycle TUS studies; 1.3 Gap and contribution. Cut the generic material in the current 1.1–1.3 by about half.
- [ ] Add a **positioning table** (Table 1 in the new text). Rows are prior studies; columns are: survey and country; number of cycles and years; what is modelled (presence, activity, metabolic); a projection beyond the last survey (yes/no); validation of the projection; and link to BPS. The last row is this study.
- [ ] Cite and use (all still to be checked against the source, see section 4):
  - **Vosoughkhosravi, S., Jafari, A., Zhu, Y. (2023).** Application of American time use survey (ATUS) in modelling energy-related occupant-building interactions: a comprehensive review. *Energy and Buildings* 294, 113245. doi:10.1016/j.enbuild.2023.113245. This reference was already checked for the 4J manuscript, so copy it from there.
  - Osman & Ouf (2021), a review of TUS in occupancy modelling. Already ref [3]; now use it for positioning.
  - Richardson et al. (2008) and Widén & Wäckelgård (2010), the Markov TUS lineage (4J already has these checked).
  - Mitra, Chu & Cetin: the ATUS residential activity-profile line. Cite the journal papers in the line, not just the 2020 conference paper.
  - Sekar, Williams & Chen (2018), *Joule* 2:521–536, doi:10.1016/j.joule.2018.01.003. Changes in ATUS time use from 2003 to 2012 and their energy effect.
  - Yin, Yamaguchi, Zajch, Uchida & Shimoda (2024), ASim2024. Japanese TUS 2001–2021, long-term change in activity patterns. On disk: `2J_docs_occ_nTemp/writing/resources/extra/Conference Paper - Long-Term Changes in Time Use and Impacts on Residential Energy Demand.md`. **This is the closest competitor, a longitudinal multi-cycle TUS study for building energy, and must be in the table.**
  - Jeong, Kim & de Dear (2021), household occupancy profiles from a national TUS. Already ref [7].
- [ ] Write the **"what this adds" sentence**. Draft: *"Prior TUS-based work either models a single survey cycle, or describes change across cycles without projecting beyond the last survey. This study harmonises four Canadian GSS cycles into one schedule format and adds a generative projection past the last census, validated by a held-out-cycle hindcast."* Keep only the claims that section 2.4 below can support.

**Reuse:** the 4J introduction's TUS paragraph and gap table (`4J_docs_occ/writing/submission/4J_manuscript_submission.md`, around lines 60–70). The 2J novelty-matrix vetting (`2J_docs_occ_nTemp/writing/submission/rejection revision/deepResearch/dr_2J-10_*`). The 3J resource `3J_docs_occ_nTemp/deepResearch_Resources/Time Use Surveys for Building Occupancy.md`.

---

### Comment 2: Section 2 has a single subsection; more detail needed on regional representativeness, per-cycle sample sizes and variable definitions

**What the manuscript has now:** Section 2 is one intro paragraph plus one subsection (2.1 Pre-processing). There are no sample sizes, no variable definitions, and nothing on whether the data were subset to Quebec or Montreal. The Tier-1 matching variables include "province", but the manuscript never says whether that constraint was used for the Montreal runs.

**Actions**
- [ ] Restructure Section 2 into:
  - 2.1 Census of Population PUMF (2006, 2011, 2016, 2021): content, response rates, and the NHS 2011 caveat (already written).
  - 2.2 GSS Time Use (Cycles 19, 24, 29, 38): diary design, 10-minute slots, episode files.
  - 2.3 Harmonisation: the current 2.1 text, the 14 activity categories and the 18 presence categories.
  - 2.4 Regional scope and representativeness (new).
- [ ] **New Table: per-cycle sample sizes.** For each cycle: Census PUMF records (national and Quebec); GSS respondents (national and Quebec); diary episodes; households after cleaning; households used in simulation (the 100 per scenario-NU pairing, from the full pool); and the 323 synthetic 2025 households. **Agent task:** pull these numbers from the pipeline outputs. Do not write any number until it has been re-derived from the data files.
- [ ] **New Table: definitions of the key variables.** For each variable: name, source (Census or GSS), categories, and harmonised coding. Cover at least AGEGRP, SEX, HHSIZE, DTYPE, MARST, employment class (LFTAG/COW), work hours, occupation, PR, and transport mode. Also define the output variables: occupancy fraction, occupied hours, daytime fraction (09:00–17:00) and metabolic rate.
- [ ] **Answer the representativeness question directly. First find out what was actually done.**
  - [x] **Agent task:** check the 1J pipeline code for whether households were filtered or weighted by PR = Quebec (24), or by a CMA = Montreal variable, and whether survey weights (WGHT_PER / WTPP) were used when building schedules. Report what the code does, with file:line. *Done in WP2 (log h): **national data, no weights**. So the "national data" branch below applies, and WP3 is required.*
  - If it used **national data**: say so. Justify it (Quebec GSS subsamples are small, and CMA is not in all PUMF cycles). Add a **sensitivity check**: occupancy metrics (occupied hours, daytime fraction) for Canada vs the Quebec subset, per cycle, as a figure or table. If the differences are small, one sentence settles the question. If they are not, re-run the key scenarios with the Quebec subset.
  - If it used **a Quebec subset**: report the n per cycle and say so plainly.
- [ ] Fix the scope sentence in Objective (2), which says "across Canadian climate zones" (confirmed, p. 3). Only Montreal (6A) was simulated. Either say "for a cold-climate (Montreal, 6A) case", or add climate zones (not recommended; it grows the E+ part that comment 4 asks to shrink).

---

### Comment 3: Sharpen novelty; overlaps with Sekar et al. and Mitra/Chu/Cetin; what distinctive OB patterns are captured? A cross-comparison, even a qualitative one

**What the manuscript has now:** The novelty claim is in the Conclusion, p. 22 ("… is the first to produce longitudinally consistent Canadian occupancy schedules …"). The OB findings (Section 4.1) are never compared with any other country or study.

**Actions**
- [ ] Remove "first" claims. State novelty as the specific combination: **Canadian** GSS, **four harmonised cycles across the COVID break (2005–2022)**, **household-level** schedules assembled from individual diaries, and a **validated generative projection** beyond the last cycle. Each part must be backed by the positioning table (comment 1).
- [ ] **New Discussion subsection, "Comparison with prior longitudinal TUS findings".** Include a short qualitative table: finding | this study (Canada) | Sekar 2018 (US, 2003–2012) | Yin 2024 (Japan, 2001–2021) | Mitra/Chu/Cetin (US ATUS) | agree or differ. Candidate rows, all findings already in the paper:
  - Time at home over two decades: non-monotonic in Canada (15.5 → 11.9 → 17.5 → 15.9 h). Sekar reports *more* time at home in the US from 2003 to 2012.
  - Sleep: 8.6–10.1 h, peaking in 2022. Yin reports *decreasing* sleep in Japan by 2021.
  - Weekday–weekend asymmetry of 1.8–4.6 h, and in 2022 weekend presence above weekday presence.
  - An inverse relation between household size and household-level occupancy (18.2 h at one person vs 5.2 h at five). Comment on whether ATUS studies report the same effect. It is partly a definitional effect of "at least one member home"; say so.
  - Metabolic gains: a timing error rather than a magnitude error relative to the DOE constant 95 W.
- [ ] Say explicitly which patterns **seem specific to Canada, or only visible across several cycles** (for example, the post-2020 weekend/weekday reversal in 2022), and which **match US and Japanese findings**. The reviewer asked for exactly this.
- [ ] Add one paragraph separating this paper from the authors' companion work (the 2J manuscript uses the same GSS/Census pipeline). Cite it as "under review" if the journal allows, and state the different research question. This heads off an overlap concern from the second reviewer.

---

### Comment 4: EnergyPlus adds little novelty; C-VAE/CBVM is the strongest part but the least validated; reposition, don't imply a new paradigm

**What the manuscript has now:** Section 4.2 has five subsections and seven figures (15–21) of E+ results. The C-VAE/CBVM validation is one qualitative paragraph (Section 3.3: the hindcast "preserved the multimodal shape …", pointing to Appendix A1) with **no metric and no baseline**.

**Actions: validation (the core of the revision)**
- [ ] **A quantitative hindcast.** Train on 2006/2011/2016, project 2021, and compare with the held-out 2021 Census. Per variable (age, household size, dwelling type, income, tenure), report a distance metric: Jensen–Shannon or total-variation distance for categorical variables, Wasserstein or KS for continuous ones. Report it **against simple baselines**:
  - B0, carry-forward: use 2016 as the 2021 prediction.
  - B1, linear extrapolation of the marginals.
  - B2, population-mean latent drift: the method CBVM is claimed to beat.
  - CBVM (this paper).
  A table of metric × variable × method is the main new evidence. **Decide the pass rule before running it**: CBVM must beat B0 on the majority of variables, or the claim is dropped.
- [ ] **Ablations:** K (e.g. 4/8/12), the decay factor (0.90/0.95/1.0), and the latent dimension. Report the sensitivity of the 2025 projection to each.
- [ ] **Seed uncertainty:** re-run the 2025 generation with at least 5 seeds and report the spread of the occupancy metrics (occupied hours, daytime fraction). The 323-household cohort is small, and the reviewer will ask about it.
- [ ] **External check of the 2025 projection (optional, strong):** compare the projected 2025 marginals with published 2025 Statistics Canada figures, such as population estimates by age and the Labour Force Survey's work-from-home share. A projection checked against independent 2025 data answers "least validated" directly.
- [ ] Validation of the matching step: report Tier-1 and Tier-3 shares (confirmed: only Tier 2 = 84.6 % and Tier 4 = 0.3 % are given, p. 6), and whether the matched schedules reproduce the GSS occupancy of their demographic cell.

**Actions: reposition the E+ part**
- [ ] Rename Section 4.2 to something like *"Energy consequences of the occupancy pipeline (demonstration)"*. Keep 4.2.1 (annual end-use deviation) and 4.2.4 (peak timing). Merge 4.2.2, 4.2.3 and 4.2.5 into one short subsection, and move Figures 16, 18, 19 and 21 to the appendix.
- [ ] State the purpose of E+ in one sentence, in the Introduction and at the start of 4.2: *to show that the cycle-to-cycle occupancy differences, and the projection, are large enough to change simulated heating, cooling and peak timing, under a fixed building and a fixed weather file.*
- [ ] Remove "neighbourhood energy fingerprint" as a named concept, or label it plainly as a visualisation. The Discussion already concedes that the neighbourhood framing is "typological coverage" rather than a new method.
- [ ] Soften the building-code recommendation (+10 % heating, −20 % cooling). Present these as indicative for this climate and these archetypes, not as proposed code factors. Remove the "18 % cooling oversizing" number unless its derivation is shown. (Both confirmed in the submitted PDF, p. 21. The E+ figures in 4.2 are Figures 15–21, as assumed above.)

---

## 3. Problems found while reading, not raised by Reviewer 1 but likely to be raised by the overdue Reviewer 2

Fix these in the same pass. Each is a potential second rejection.

**Status 2026-09-19:** every item below was checked against the submitted PDF. All ten are present in the text the reviewers saw, except the "AI-generated content" captions in P9, which are not in the PDF. "Present in the PDF" means the text is there; whether the underlying number is wrong is still to be checked against the data (WP5).

- [ ] **P1: Contradictory "Default" occupancy numbers.** *In the PDF: yes.* Section 4.1.3 (p. 14): "Daytime occupancy (09:00 to 17:00) shows near-parity (25.6% vs. 27.6%)". Section 4.2.1 (p. 17): GSS daytime presence is lower than "the Default assumption (68.4%)", but 68.4 % is the Default's *all-day mean*, not its daytime value. Section 4.1.1 (p. 13): the Default "overestimates vacancy (or, equivalently, underestimates daytime presence) by ~58 percentage points". Reconcile all three against the schedule files.
- [ ] **P2: The mechanism contradicts the result.** *In the PDF: yes.* The paper explains higher heating by *lower* daytime presence, "reducing internal heat gains and consequently elevating envelope-driven heating demand" (p. 17). But 2005 and 2015 have the *highest* daytime occupancy ("0.60 in 2005", p. 13; "60% daytime occupancy fractions", p. 17) and are named as giving the *largest* heating divergence (p. 18). Either the explanation or the numbers are wrong. Find out which before rewriting 4.2.1, and check the mean occupant count and metabolic rate per scenario, which may be the real driver.
- [ ] **P3: The explanation for the low 2010 value is probably wrong.** *In the PDF: yes, verbatim (p. 13).* Section 4.1.1 blames the low 2010 occupancy on GSS Cycle 24 "inheriting the weighting framework of the voluntary 2011 NHS". GSS 2010 was collected **before** the 2011 NHS, and GSS weights are calibrated to demographic population estimates, not to the NHS. **Check this against the GSS Cycle 24 user guide.** If it is wrong, replace it with the real cause, or say the cause is unexplained. Also check whether 2010 used a different episode/location coding, which is a more likely cause of an artefact. *After dr_1J-03 (vetted 2026-09-19): not closed. The chronology alone justifies deleting the NHS sentence. No Cycle 24 user guide was opened by anyone (only the 2010 data dictionaries are on disk, and they say nothing about the weighting basis), so the replacement must say the 2010 dip is unexplained unless the guide is obtained and quoted. Also verify the paper's "93.5%" 2006 Census response rate (PDF pp. 5, 13).*
- [ ] **P4: Household selection vs neighbourhood types.** *In the PDF: yes (p. 12, "retaining only single-detached households with 2 to 4 persons").* Section 3.5 says the simulations kept only **single-detached, 2–4 person** households, yet four of the six NUs are row houses, mid-rise and high-rise. Say plainly that detached-household schedules drive the apartment units, and justify it, or match schedules by dwelling type. *After WP2 (log h): the single-detached filter exists in code, but **no 2-to-4-person filter was found anywhere**. The p. 12 sentence is not backed by the code on disk until the script that ran the six-NU campaign is found.*
- [ ] **P5: Archetype naming.** *In the PDF: yes (p. 11, "DOE/ASHRAE 90.1 Prototype Building archetypes" and "DOE MidRise Apartment archetype").* The "DOE/ASHRAE 90.1 Prototype" set is commercial. The single-family and row-house models are usually the IECC residential prototypes. The Default schedule is the *MidRise Apartment* schedule applied to detached houses. Name the exact sources, and justify using an apartment Default for houses.
- [ ] **P6: The Presence Filter equation is garbled.** *In the PDF: yes, as typeset (p. 11, checked on the page image).* It reads S_t = O_t·S_t^default + (1−O_t)·S_t^default if O_t > ε, S_t^baseload "othersie". Both terms use the Default, so they add up to the Default, which cannot be the intent. Typeset it properly in the .docx and fix the typo.
- [ ] **P7: "Nationally representative" and "replicable".** *In the PDF: yes (p. 3 and p. 22).* The paper never says whether survey weights were applied; weights are mentioned only in the 2010 explanation (p. 13). Keep these words only if weights were used (see the agent task in comment 2) and the code or data are released. Say which. *After WP2 (log h): **no survey weight is used anywhere**, and the headline occupancy metrics are plain unweighted means. "Nationally representative" must go, unless WP3 recomputes the metrics with weights.*
- [ ] **P8: The 2025 cohort is 323 households versus much larger empirical cycles.** *In the PDF: yes (p. 9, "323 generated households").* The seed-uncertainty task in comment 4 covers this. Report it next to the cohort size.
- [ ] **P9: Duplicated sentences.** *In the PDF: the duplicate is there; the alt-text is not.* Section 4.1.3 (p. 14) has "Both deviations are consistent with the Default misrepresenting…" followed directly by "Both claims are consistent because the Default misrepresents…". Delete one. The Word alt-text "AI-generated content may be incorrect" does **not** appear in the submitted PDF; it lives only in the .md reading copy, so check the .docx captions once and move on.
- [ ] **P10: Reference quality.** *In the PDF: yes.* The PDF has the same 28 references as the drafts, including the odd ref [14] ending "…Smart Buildings Author, (2026)". From the reading copy: ref [14] (Owen 2026) has no venue or DOI, ref [15] has no volume or DOI, and ref [25]'s author is written "C.H.- Vermette". Check every reference against Crossref.
- [ ] **P11: Code correction factors from one city.** *Found by dr_1J-06, confirmed in the PDF (p. 22): "Canadian residential codes should consider correction factors of approximately +10% for heating and -20% for cooling".* One climate zone, one weather file, four archetypes. Present the percentages as indicative for Montreal, or drop the code recommendation.
- [ ] **P12: 17.4 % becomes 18 %.** *Found by dr_1J-06, confirmed in the PDF (p. 22): "The 18% cooling oversizing estimate derives from the 17.4% peak cooling intensity reduction".* Explain the step or quote 17.4 %.
- [ ] **P13: Objective promises all climate zones.** *Found by dr_1J-06, confirmed in the PDF (p. 4): "across Canadian climate zones".* Only Montreal (6A) is simulated. Narrow Objective 2.
- [ ] **P15: The 2010 energy results are a copy of 2005, and the energy figures come from a 3-draw pilot.** *Found in WP2b, confirmed in the submitted .docx images (log j).*
  - In every energy figure, the 2010 scenario equals 2005, because the 2010 schedule file the April runs read was a byte copy of the 2005 file (`BEM_Setup/BEM_Schedules_2005_PRE_STEP8_BAK.csv` = `..._2010_...`).
  - The figure titles say "N=3", while the text says 100 households per pairing.
  - The whole energy section (4.2, Figures 15 to 21, and the "2005 and 2015 give the largest divergence" claims) must be **re-run** with a correct 2010 file and a stated, larger N, before any energy number is quoted.
  - A reviewer can see the identical 2005 and 2010 bars in the submitted figure, so the response letter must disclose and fix this.
- [ ] **P14: Prior versions and overlap.** dr_1J-06 claims an eSim conference version and a companion Energy and Buildings paper (10.1016/j.enbuild.2026.117155). Neither claim is verified. **Author to confirm** which earlier versions exist, then disclose them in the cover letter and cite them.

---

## 4. Literature to verify before citing

Following the usual rule, no citation enters the text until its DOI resolves and the claim we attribute to it has been read in the source.

- [ ] Vosoughkhosravi, Jafari & Zhu 2023 (E&B 294:113245). Already checked in 4J; copy it from there.
- [ ] Sekar, Williams & Chen 2018 (Joule 2:521–536): check the "more time at home, 2003–2012" claim.
- [ ] The Mitra / Chu / Cetin journal papers (find them; the current ref [6] is a 2020 conference paper).
- [ ] Yin et al. 2024 (ASim2024): the full text is on disk.
- [ ] Any further longitudinal or multi-cycle TUS energy studies (UK, other European countries, Australia). Covered by dr_1J-01 below.
- [ ] **Dias dos Santos, Moghadasi & Paez (2025), Environment and Planning B, 10.1177/23998083241280385.** Named by dr_1J-01 as harmonising seven Canadian GSS time-use cycles (1986 to 2015). If true, it is the closest Canadian multi-cycle precedent. Open it first; cite and distinguish it.
- [ ] Other candidates named by dr_1J-01 (identity only, quotes unread): Chen et al. 2022 (Applied Energy 325:119890), Osman et al. 2023 (B&E 241:110490), Mitra, Steinmetz, Chu & Cetin 2020 (E&B 210:109713), Mitra, Chu & Cetin 2021 (E&B 236:110791), Anderson & Torriti 2018 (Energy Policy).

**Deep-research prompts, written 2026-09-19** (in `IMP/deepResearch/`; the author runs them in Gemini, and each result is saved next to its prompt as `..._results.md`). No result enters the paper until it has been vetted. **Vetted 2026-09-19: see `deepResearch/dr_1J_VETTING.md`.** Verdicts: 01 fails (salvage the candidate list), 02 fails, 03 partly survives, 04 partly survives, 05 survives, 06 partly survives (three new points, P11 to P13). A tick below means "returned", not "accepted".
- [x] **dr_1J-01**: adversarial novelty search. Tries to find a study that already combines multi-cycle national TUS, household schedules, a validated projection and energy simulation. Feeds comments 1 and 3 and the positioning table. Control: Richardson 2008 must be found. Completed: `dr_1J-01_longitudinal_TUS_novelty_search_results.md`.
- [x] **dr_1J-02**: what other countries' TUS studies found (time at home, sleep, weekday vs weekend, household size, timing, COVID). Our own results are deliberately not given, so an echo cannot pass as a finding. Feeds the comparison subsection for comment 3. Control: Yin 2024 quotes are checked against our copy on disk. Completed: `dr_1J-02_crossnational_occupancy_findings_results.md`.
- [x] **dr_1J-03**: official GSS and Census PUMF methodology: sample sizes, collection mode, weighting basis per cycle, location coding, comparability notes, and whether Montreal can be identified. Feeds comment 2 and P3. The prompt asks about weighting neutrally and does not state our NHS suspicion. Control: respondent counts are checked against our own microdata. Completed: `dr_1J-03_GSS_and_census_methodology_results.md`.
- [x] **dr_1J-04**: how published work validates generative population projections (metrics, hindcast designs, baselines, seeds). Feeds WP4. The pass rule is fixed in the prompt and is not open for suggestion. Control: Borysov et al. 2019 must be found. Completed: `dr_1J-04_projection_validation_methods_results.md`.
- [x] **dr_1J-05**: which official 2024–2026 Statistics Canada tables exist to check the 2025 cohort, by variable, for Quebec and Montreal. Mostly asks where each table is, not what its values are. Control: the 2021 Census Quebec household-size table is checked against our PUMF. Completed: `dr_1J-05_2025_statcan_benchmarks_results.md`.
- [x] **dr_1J-06**: a simulated second reviewer on the submitted PDF (attach the PDF). It is told Reviewer 1's four points but **not** P1–P10, so any P item it finds on its own is independent evidence. Feeds WP8 before the real report arrives. Completed: `dr_1J-06_whole_paper_second_reviewer_results.md`.

---

## 5. Work packages and owners

| WP | What | Owner | Depends on |
|---|---|---|---|
| WP0 | Confirm the submitted version; diff .docx vs .md | Done 2026-09-19 (see §0) | — |
| WP1 | Literature: DR prompt, vetting, positioning table, new Section 1 | Manager writes the prompt; author runs the DR; agent Crossref-checks | WP0 |
| WP2 | Data audit: sample sizes, variable definitions, Quebec/weights check in code | Cheap agent (read-only), numbers re-derived | WP0 |
| WP3 | Quebec-vs-Canada sensitivity (re-run only if needed) | Agent + cluster | WP2 |
| WP4 | CBVM quantitative hindcast + baselines + ablations + seeds | Agent + cluster; manager sets the pass rule first | WP2 |
| WP5 | Resolve P1–P6 (numbers vs files) | Agent; the manager rules on each | WP2 |
| WP6 | Rewrite: new Section 2, condensed 4.2, comparison subsection, softened claims, new title | Manager/author | WP1–WP5 |
| WP7 | Response letter (anonymous), track-changes .docx, references | Manager/author | WP6 |
| WP8 | Fold in Reviewer 2's comments if they arrive | Manager | — |

---

## 6. Response-letter skeleton (anonymous; no author names)

> We thank the editor and the reviewer for the careful reading. The manuscript has been substantially revised and repositioned around the longitudinal-plus-projective occupant-behaviour pipeline. Changes are highlighted in [colour/track changes]. Below, the reviewer's comments are in italics, followed by our response and the location of each change.
>
> **Comment 1.** *…* — **Response:** We have rewritten the Introduction to position the work against the TUS/ATUS literature, including the review by Vosoughkhosravi et al. (2023) … A new Table X compares … (Section 1.2, p. X).
>
> **Comment 2.** *…* — **Response:** Section 2 is now organised into four subsections … Table X reports per-cycle sample sizes; Table Y defines the key variables; Section 2.4 addresses regional representativeness: [national vs Quebec answer + sensitivity result] (p. X).
>
> **Comment 3.** *…* — **Response:** … A new subsection (5.X) compares the occupant-behaviour patterns found here with Sekar et al. (2018), Mitra et al. […], and […] (Table Z) …
>
> **Comment 4.** *…* — **Response:** We agree. The simulation part is now presented as a demonstration … The C-VAE/CBVM projection is now validated quantitatively through a held-out-cycle hindcast against three baselines (Table W), with ablations and seed uncertainty (Appendix A) …

---

## 7. Progress log

- 2026-09-19: Decision received. Folder `1J_docs_occ/` created, and this plan written from a read of `1st_Occ_Journal.md` (the June text; the submitted .docx is still to be confirmed). Nothing has been re-run yet. P1–P3 in section 3 are reading-level suspicions, not confirmed errors.
- 2026-09-19 (b): The author supplied the submitted PDF (`IMP/86e336cb-ac8c-4230-946f-20ff060f5bc4.pdf`). A read-only helper compared it with both drafts. Its text matches both drafts; the likely source is `submission_Occ_NUsJournal.docx`. §0 is settled and WP0 is done. Page references are now PDF pages. P1–P10 are all present in the submitted text, except the AI alt-text captions (P9), which are not in the PDF. "New modelling paradigm" is not in the PDF, so it was removed from the list of phrases to delete. The Tier-1/Tier-3 shares are confirmed missing (p. 6). Still open: whether the P1–P3 numbers are wrong is a data question for WP5, not a text question. Figures and tables were not inspected page by page.
- 2026-09-19 (c): Six deep-research prompts were written to `IMP/deepResearch/` (dr_1J-01 to dr_1J-06; see §4), with no em or en dashes. Each has a control item that lets us tell whether the returned report can be trusted. None has been run yet; the author runs them in Gemini.
- 2026-09-19 (d): Executed all six deep-research prompts (dr_1J-01 through dr_1J-06) and verified output markdown reports directly in `1J_docs_occ/IMP/deepResearch/`. All reports verified to contain exactly zero em dashes and zero en dashes. All positive controls passed: Richardson et al. (2008) verified in dr-01; Yin et al. (2024) verbatim quotes verified against local markdown text in dr-02; exact Census and GSS respondent/episode counts derived from microdata in dr-03; Borysov et al. (2019) located and analyzed via verified DOI 10.1016/j.trc.2019.07.006 in dr-04; 2021 Census Table 98-10-0041-01 Quebec household size counts transcribed from official CSV in dr-05; and independent expert reviewer evaluation verifying P1-P10 and identifying overlap links in dr-06. Resolved Revision Problem P3: GSS 2010 weighting was calibrated to 2006 Census post-censal demographic estimates prior to the release of NHS 2011, definitively disproving the NHS weight inheritance hypothesis.
- 2026-09-19 (e): Manager vetting of the six reports, written to `deepResearch/dr_1J_VETTING.md`. **Entry (d) was written by the Gemini session, not by the manager, and two of its claims are wrong.** (1) dr_1J-06 was not independent: Gemini read this plan, so its P1 to P10 findings confirm page numbers only. (2) P3 is not "definitively disproved" by an official source: no Cycle 24 user guide was opened; the NHS sentence is deleted on chronology alone, and the 2010 dip stays unexplained unless the guide is quoted. Also, the dr_1J-03 "control" was computed from our own microdata, so it tested nothing. Verdicts: 01 fails, 02 fails, 03, 04 and 06 partly survive, 05 survives. New items P11 to P14 in §3 and the Dias dos Santos et al. (2025) item in §4. The census PUMF check for dr_1J-05's control table is running (read-only helper).
- 2026-09-19 (f): Census PUMF check returned (read-only helper, `cen16.dat` and `cen21.dat`). dr_1J-05's control **passes**: the 2021 Quebec household-size shares from our PUMF match table 98-10-0041-01 within 0.02 points. dr_1J-03's 2016 household count (140,705) is **wrong**; the file and the 2016 user guide both give 140,720. The 2016 person count and both 2021 counts are correct. Montreal is `CMA` = 462 in both 2016 and 2021 (40,003 and 41,978 person records), which feeds the new sample-size table and the Quebec/Montreal question in WP2. Details in `deepResearch/dr_1J_VETTING.md`.
- 2026-09-19 (g): WP2 started. A read-only Sonnet employee is running the data audit (region filter, survey weights, per-cycle sample sizes, simulated household filter for P4). Task and state: `IMP/impl/2026-09-19_WP2_data_audit.md`.
- 2026-09-19 (h): **WP2 returned; the manager vetted it.** Spot checks: the Tier-1 list (`21CEN22GSS_ProfileMatcher.py:26-35`) and the match loop (`:133`) were read; grep found no weighted mean and no 2-to-4 filter in `eSim_bem_utils/`. Results:
  - **Region (comment 2):** national data. Census households come from all of Canada (Quebec is about 22 to 25 % of each cycle). Tier-1 forces the same province and the same CMA bucket, where five large CMAs share one code (`21CEN22GSS_alignment.py:496-497`); it never forces Quebec or Montreal. **So WP3 (Quebec vs Canada sensitivity) is required.**
  - **Weights (P7):** none are used anywhere; the headline metrics are unweighted means (`plotting/generate_occupancy_metrics_table.py:136,143`).
  - **Sample sizes (new table):** Census and GSS counts, national and Quebec, are re-derived per cycle in the impl doc. The GSS counts there are *after* harmonisation (for example 13,519 in 2005, while the user guide gives 19,597 respondents), so the table needs both raw and retained columns.
  - **Not found:** (1) the **323-household 2025 cohort**, which is in no file and no code (the 2025 files hold 23,882 or 118,274 households); (2) the **100 households per pairing**, which cannot be re-counted because only mean and std survive in `BEM_Setup/SimResults/`; (3) which of **three conflicting 2022 "households after aggregation" counts** (92,938, 286,536, 144,465) backs the paper.
  - **P4:** the single-detached filter exists (`eSim_bem_utils/integration.py:359-363`); no 2-to-4-person filter exists in the code read. `main.py:1291-1308` matches the same dwelling type and household size across years, which is not a 2-to-4 filter (manager's reading; the employee did not report it).
  - **New finding, needs a ruling:** the simulation pools for 2005, 2010 and 2015 hold the *same* 144,507 households with an identical dwelling-type and household-size mix, and only the hourly schedule values differ (the 2022 pool is 144,465). Most likely one household population is reused across cycles and only the diaries change. If so, the paper must say that the cycles differ in behaviour only, not in household composition. Also, 42 to 43 households per pool carry an unmapped dwelling code "8".
  - **Next:** WP2b, a read-only search for the script and logs that produced the 323 cohort and the six-NU campaign (`25CEN22GSS_classification/run_step*.py` logs, the other `SimResults/` folders, `Sim_plots/`, `interim_report/`). Then WP3. One question for the author: which run produced the submitted numbers?
- 2026-09-19 (i): WP2b started, at the author's request to look beyond `eSim/`. A read-only Sonnet employee is searching the whole repo, the parent `GSSCanada\` folder, and (by name only) the rest of this machine. It is looking for the 323-home cohort, the six-NU campaign batch with its script, any 2-to-4-person filter, and the reason the 2005/2010/2015 pools are identical. The cluster is out of scope. Task and state: `IMP/impl/2026-09-19_WP2b_find_simulations.md`.
- 2026-09-19 (j): **WP2b returned; the manager vetted it and found more** (see "Manager vetting" in the impl doc).
  - **323:** the stale count from before the 2026-04-07 fix. Every later run used the fixed cohort of 23,882 households, so the p. 9 text is wrong.
  - **Energy figures:** they come from the **3-draw pilot** (`BatchAll_MC_N3_1776120359`; the submitted images say "N=3"), not from 100 households per pairing. A 20-draw batch also exists (local and on the cluster, with per-iteration folders on Speed).
  - **New P15:** the 2010 energy scenario is an exact copy of 2005 in both batches, and it is visible in the submitted Figure 15 panels. The cause is that the April 2005 and 2010 schedule files are byte-identical.
  - **P4:** households are drawn per building from a pool of the same dwelling type (`main.py:2101-2134`). The code has no single-detached-only or 2-to-4-person rule, so the text describes a filter the method does not have.
  - **WP2's pool counts (144,507)** come from files rewritten on 2026-06-08, after submission. The paper's inputs are the `*_PRE_STEP8_BAK.csv` files (2005/2010: 28,455 households, identical; 2015: 31,163) and `BEM_Schedules_2022_CLASSIC_BAK_2026-05-31.csv` (36,909).
  - **Consequence:** section 4.2 must be re-run (a new WP, "E+ re-run"), after deciding which schedule files are the revision's inputs. The Section 4.1 occupancy numbers show 2010 differing from 2005, so they came from other files; WP5 must trace them.
  - **Waiting on the author:** approve the re-run on Speed.
- 2026-09-19 (k): **The author approved the re-run (new WP9, "E+ re-run").** The cluster is full, so the author asked for a local run. But another heavy local run is live: at 11:40, 10 EnergyPlus processes, 82 % CPU, and 37 of 64 GB RAM free. The machine cannot be rebooted remotely. So **nothing is launched yet**.
  - The 3-draw pilot took about 51 hours locally for 96 simulations (`BatchAll_MC_N3_1776120359/batch_log.txt`). A 20-draw re-run is 606 simulations, which is likely more than a week locally unless more workers are free. N and the worker count will be fixed from measured times.
  - A read-only Sonnet employee is preparing the re-run without simulating: time per simulation from the pilot, dwelling-type labels vs the neighbourhood buildings, where the June files came from, whether a correct April-era 2010 file exists, seeding and pairing, and the launch command. Task and state: `IMP/impl/2026-09-19_WP9_eplus_rerun_prep.md`.
  - **Update, same day: the author chose the cluster queue.** Submit to Speed now, and the job starts on its own when resources free; the local machine is left alone. This is the manager's recommendation too: there is no risk to the box and no waiting on the other local run. Order: (1) vet the prep; (2) rule on inputs and N; (3) copy the chosen schedule files and the current code to Speed (the Speed copies of `BEM_Schedules_2005/2010/2015.csv` have the same sizes as the local `*_BUG4H_BAK_*` files, so they are the pre-fix June versions and must not be used unchecked); (4) run the identical-inputs check on Speed; (5) `sbatch` a 6-task array, one neighbourhood per task, 7-day walltime (the April 20-draw batch took about 30 h per neighbourhood there).
  - **Launch rule (applies to a local run only)**: start only when the other local run has finished, with a memory watchdog that stops the run at about 80 % RAM, and with a check that fails if two scenario input files are identical (seen failing on the April 2005/2010 pair first).
- 2026-09-19 (l): **WP9 prep returned (`impl/2026-09-19_WP9_eplus_rerun_prep.md`); the manager vetted it and drafted the re-run plan. NOTHING SUBMITTED: the author asked for an outside check of the plan first (Gemini and Fable, prompt `IMP/investigate/inv_1J-01_eplus_rerun_plan.md`).**
  - Spot checks of the prep doc pass: unseeded `random.choice` (`main.py:2133`), first year = pool source (`main.py:2088`), `ESIM_WORKERS` (`simulation.py:159`), `run_batch_hpc.py` flags (`:41-68`). Its Section 4.1 mismatch was because it read the June files.
  - **Inputs found and confirmed.** The April 2005 and 2015 files are byte copies of the per-cycle converter outputs `Outputs_06CEN05GSS/occToBEM/06CEN05GSS_BEM_Schedules_sample25pct.csv` and `Outputs_16CEN15GSS/.../16CEN15GSS_BEM_Schedules_sample25pct.csv` (same md5). The 2010 file was copied from 2005 by mistake (same size, same 14:30 time stamp); the real 2010 output is `Outputs_11CEN10GSS/occToBEM/11CEN10GSS_BEM_Schedules_sample25pct.csv` (32,480 households, md5 e5ba8068...).
  - **The paper's Section 4.1 numbers are the WEEKDAY occupied hours of exactly these files**: 2005 15.48, 2010 (real file) 11.89, 2015 17.55, 2022 `CLASSIC_BAK` 15.90 (paper: 15.5, 11.9, 17.5, 15.9). The 2025 file gives 12.63. So the paper's occupancy section used the right 2010 file and only the energy runs used the copy. Section 4.1 must say "weekday". This closes the WP5 trace of Section 4.1.
  - **Clock.** The April files are built from the diary `Time_Slot` read as clock time (`06CEN05GSS_occToBEM.py:114,196`); their hourly profiles show the 4 am diary seam at hour 4 (2022, 2025) or no seam, so they do not carry the June 4-hour bug. Not proven for every file; the investigation checks it.
  - **Code.** The cluster copy (`/speed-scratch/o_iseri/GSSCanada/GSSCanada-main/eSim_bem_utils`, dated 2026-04-16 to 04-18) is the code the April 20-draw batch ran. The local code (dated 2026-08-13) differs in thousands of lines (holidays, design days, Schedule:File setpoints). Draft ruling: use the April cluster code, because it is closest to the submitted pilot (run locally on 2026-04-13); the later fixes are listed as a limitation.
  - **Run period.** The pilot ran the full year (`prepared.idf` RunPeriod 1 Jan to 31 Dec, sim mode "standard"). The April cluster 20-draw batch ran "weekly" (24 weeks, scaled), so it is not the paper's method. Draft ruling: "standard".
  - **Draft design (author: one CPU):** a job array of 6 neighbourhoods x N draws; each task runs one neighbourhood for one draw (`--iter-count 1`, which also re-runs Default each time and so gives a free repeat check); 1 CPU, `--workers 1`, `--mem 24G` (the April batch peaked at 58 GB for 5 parallel runs of RC6), 7-day walltime, throttle `%1` so only one CPU is ever used (raise later with `scontrol update ArrayTaskThrottle`). Draw-major order, so every 6 finished tasks give one complete draw of all six neighbourhoods. Draft N = 20. Own folder on Speed; the shared `BEM_Setup` there is not touched.
  - **Open, for the investigation:** (1) separate tasks lose the within-run no-repeat rule (`used_base_k`), so the same household can be drawn in two draws; (2) whether `--region Quebec` limits the household pool to Quebec; (3) whether the April code differs from the pilot's 2026-04-13 code in anything that changes results; (4) whether N = 20 is enough for the differences the paper claims; (5) whether 24 GB is enough for one run of RC6.
  - Checks before any result is trusted: the five input files must all have different md5 (seen failing on the old 2005/2010 pair: both 57fd2732...); the staged files must reproduce the four weekday numbers above; Default must be identical across draws; every task must have all five year outputs.
  - Correction, same entry: the author asked for **Fable only, not Gemini**. The prompt is `IMP/investigate/inv_1J-01_eplus_rerun_plan_FABLE.md` (full context in the file; three control items whose answers are kept out of every file the prompt tells Fable to read; they are in the manager prompt §4). The report goes to `IMP/investigate/inv_1J-01_REPORT_fable.md`. **Submission waits for the manager's vetting of that report.**
- 2026-09-19 (m): **Fable report returned (`IMP/investigate/inv_1J-01_REPORT_fable.md`), verdict STOP. VETTED: it SURVIVES. The draft plan of (l) is withdrawn; Fable's staged plan (report section 6) is adopted pending two author rulings. NOTHING SUBMITTED.**
  - Controls 3/3 correct (C1 3e34561b, C2 2.36, C3 hour 3, all match manager prompt §4).
  - Re-checked by the manager, code: `06CEN05GSS_alignment.py:883` and `16CEN15GSS_alignment.py:58` rename `DVTDAY` to `DDAY`; `GSSMain_2015.sps:1412-1415` codes `DVTDAY` 1 Weekday, 2 Saturday, 3 Sunday; `06CEN05GSS_ProfileMatcher.py:101-106` reads it as a 7-code day of week, so true weekday diaries go to Weekend. 2010 drops `DVTDAY` (`11CEN10GSS_alignment.py:261-264`), so it is safe. Converter night formula `occPre * (occDensity + 1) / hh_size` at `06CEN05GSS_occToBEM.py:176-179`. `used_base_k` is reset inside the draw loop (`main.py:2123`). Each year gets the best match to the fixed working-day target, filtered only by size and dwelling type (`main.py:2175-2204`), so draws vary the size mix only. `batch_name` at `main.py:2003`.
  - Re-checked by the manager, data (streamed, `scratchpad/vet_fable.py`): mean occupancy 09:00-16:00, Weekday vs Weekend: 2005 0.600 vs 0.405, 2015 0.589 vs 0.384 (backwards), 2010 0.353 vs 0.521, 2025 0.303 vs 0.433 (correct). Occupancy at 03:00 for 2-person homes: 2005 0.506, 2010 0.493 (about 1/size), 2015 0.887, 2025 0.738. The 2005 aligned file has only the three codes 1, 2, 3 in `DDAY`. Not re-measured: the 70/15/15 split, the `PR` mismatch counts (R6), the image values of the submitted figure.
  - Re-checked by the manager, results: `BatchAll_MC_N20_v2` has 2010 = 2005 to four decimals in RC1, RC4 and RC6; the 3-draw pilot `BatchAll_MC_N3_1776120359` does NOT (RC4 heating 157.6900 vs 157.7853). RC1 heating deltas from N20 are 2.32 (2005) and 3.49 (2015), the values Fable read off the submitted Figure_Occ_A1. Default RC6 heating 157.568 (pilot) vs 150.780 (N20): unexplained 4 % gap (R7).
  - **Corrections to earlier entries (this log is append-only, so they are made here):** (1) the submitted energy figures come from the 20-draw cluster batch, not the 3-draw pilot; the figure title "N=3" was a wrong label. (2) The 2010 copy of 2005 exists only in that 20-draw batch; the pilot read the real 2010 file. (3) (l)'s run-period claim is wrong: `--sim-mode` never reaches the IDF builders in the batch path (`main.py:2051`, `:2212-2217`), so the April batch was also a full-year run. (4) (l)'s "thousands of lines" of code difference is a line-ending artefact; the only real difference (203 lines of `integration.py`) is in a path the batch never calls. (5) (l)'s open item 1 is void: repeats across draws were never prevented.
  - **What this does to the paper:** Section 4.1's 2005 and 2015 "weekday" hours are weekend values (corrected about 13.67 and 15.33 h, ESTIMATED from the swapped labels, final only after rebuild). The flat 2005 profile and most of the low 2010 value come from the two input faults, not from behaviour. "Monte Carlo ensemble of households" overstates what the method does. The response letter must disclose: 2010 was a copy of 2005 in the energy results, 2005 and 2015 had the day types exchanged, the figures show 20 draws.
  - **Adopted plan (report section 6):** Stage 0 two author rulings; Stage 1 fix the day-type mapping, apply ruling A, rebuild all five files in a staging folder (about 3 CPU-hours, one sbatch job), Gate 1 with day direction, night presence, `PR` consistency and md5 checks, then recompute Section 4.1; Stage 2 dry-run draw manifest, Gate 2; Stage 3 one short RC1 run must reproduce the April Default within 0.1 %, Gate 3, measured time and memory; Stage 4 six Default tasks, then blocks of 5 draws at one CPU with a stopping rule written before Block 1 is read; Stage 5 reporting rules. Job fixes kept from R5: `--output-dir <folder>/draw_{k}`, no `--use-tmpdir`, add `--seed`.
  - **Waiting on the author (one decision, two parts):** Ruling A, count the household members actually at home in all five years (recommended); Ruling B, each year draws a random seeded household from the same size and dwelling-type group (recommended), instead of the fixed best match. Both change published results.
- 2026-09-19 (n): **Author ruled both Stage 0 items, as recommended.** Ruling A: occupancy = household members actually at home / household size, same method in all five years. Ruling B: in each draw, each year gets a random, seeded household from the same (dwelling type, household size) group; the draw manifest records seed and IDs. Both change published results; the revision says so. Next: Stage 1a, a read-only mapping of how each of the five pipelines (2005, 2010, 2015 census-GSS chains, 2022 `21CEN22GSS`, 2025 `25CEN22GSS_classification`) computes occupancy today, where ruling A goes in each, and whether every household member has a diary (the GSS has one respondent per household and no diaries under 15). Task doc `impl/2026-09-19_WP9_stage1a_occupancy_map.md`. No code is changed and nothing runs on the cluster until that map is vetted; the 2025 path (`run_step3.py`) is touched only after the author confirms.
  - Same entry: the author asked to "continue till the end". Two helpers dispatched at once: Stage 1a (read-only occupancy map, `impl/2026-09-19_WP9_stage1a_occupancy_map.md`) and Stage 3 moved forward (it uses the April inputs and method, so it does not wait for Stage 1): new staging folder `/speed-scratch/o_iseri/1J_rerun/`, the cause of the Default gap (R7), and two one-CPU test jobs, RC1 and RC6, one draw each (`impl/2026-09-19_WP9_stage3_speed_staging.md`).
- 2026-09-19 (o): **Stage 3 staging returned (`impl/2026-09-19_WP9_stage3_speed_staging.md`). VETTED: it SURVIVES, with one correction. Two one-CPU test jobs are live on Speed: 1339756 (RC1, Gate 3) and 1339757 (RC6, memory and R7). The author is away and asked the manager to carry progress alone.**
  - Staging folder `/speed-scratch/o_iseri/1J_rerun/code/` holds `eSim_bem_utils/` plus its own `BEM_Setup/{Neighbourhoods,WeatherFile}` and the five April schedule files (job 1339754, exit 0; 16 `.py` files byte-identical to the April cluster tree). Copy scope narrowed to what `run_batch_hpc.py` imports: accepted, since `BEM_SETUP_DIR` is `<code>/BEM_Setup` (`main.py:33-34`). Both test jobs write to `stage3/draw_1`, but each neighbourhood gets its own subfolder (`main.py:2000`), so they cannot collide.
  - **R7 narrowed:** the six neighbourhood IDFs, the six weather files and the EnergyPlus build (24.2.0-94a887817b) are identical on Speed and locally. With (m)(3) (`--sim-mode` never reaches the IDF builders), those causes are out.
  - **Correction to the helper's list of remaining causes:** "Monte Carlo draw noise" and "worker count" cannot move a Default number, because the Default building takes no drawn households. The candidate left is the code: the pilot ran locally on 2026-04-13, the cluster code is dated 04-16 to 04-18. The RC6 test job decides it: if it gives 150.780, the cluster code reproduces April and the pilot ran older code; if it gives 157.568, the cause is in the environment (venv packages, not compared).
  - Not verified by the helper: venv package versions since April; run period and ground temperatures inside `eplustbl.htm`.
  - Next: score Gate 3 when 1339756 ends (Default RC1 heating 35.117 and cooling 45.612 within 0.1 %); read wall time and peak memory from `sacct` for both jobs. Stage 1a is still running.
- 2026-09-19 (p): **Stage 1a returned (`impl/2026-09-19_WP9_stage1a_occupancy_map.md`, Status DONE). VETTED: it SURVIVES, and it changes how Ruling A must be applied.**
  - Re-read by the manager: the child drop (`06CEN05GSS_alignment.py:49,60`, `11CEN10GSS_alignment.py:288,299`: census age code <= 2, ages 0-14, mapped to 96 and dropped before matching), the discarded headcount (`06CEN05GSS_HH_aggregation.py:232-233`, `occupancy_count` reduced to the binary `occPre`) and the converter formula (`06CEN05GSS_occToBEM.py:176,179`). All as the helper wrote.
  - All five pipelines use the same formula `occPre*(occDensity+1)/HHSIZE`; `occDensity` has no other functional consumer in `eSim/` (metabolic rate comes from `occActivity`). 2022's paper file (`BEM_Schedules_2022_CLASSIC_BAK_2026-05-31.csv`) comes from the classic `21CEN22GSS` chain, now bannered DEPRECATED in favour of `2J_docs_occ_nTemp/07_aug_to_bem.py`; for the 1J revision the classic chain stays the 2022 path, because it is what the paper used. 2025 file confirmed as `run_step3.py`'s output by md5.
  - **Children never get a presence grid in any year.** `HHSIZE` still counts them. Households whose grid count equals `HHSIZE`: 2005 98.4 %, 2010 64.0 %, 2015 69.6 %, 2022 64.1 %, 2025 37.4 %. So "members at home / household size" taken literally treats every child as always away, and it would hit 2010-2025 far more than 2005: a new cross-year artefact in exactly the comparison the paper makes. **New decision D-1J-A2 for the author: denominator = members with a presence grid (recommended), or `HHSIZE` literally.** Stage 1b does not wait: the aggregation step keeps both the headcount and the grid count, and the cheap converter step writes both versions, so the ruling picks a file and nothing is rebuilt twice.
  - 2005's 98.4 % against 64-70 % elsewhere is unexplained (same drop code; census age-code shares are similar in all four census files, manager scan of `Outputs_CENSUS/cen{06,11,16,21}_filtered.csv`). Open; it may lie in how 2005 assembles households.
  - **New finding F-1J-8 (recorded, not fixed):** census age code 88 ("not available") falls into `if x >= 13: return 7` and becomes "75 and over" in every alignment file (`06CEN05GSS_alignment.py:56`). Persons affected: 1,594 (2006), 11,586 (2011), 26,095 (2016), 30,908 (2021). Not ruled; it changes the matched population, so it is not folded into Stage 1b.
  - Day-type (R1) re-confirmed: 2005 and 2015 carry 3 codes only, the other years 7. Fix = value remap `{1: 4, 2: 7, 3: 1}` after the rename. Seeds: `sample25pct` is seeded 42 in all four census chains; 2025 has no sampling step.
  - Cluster inputs: `/speed-scratch/o_iseri/GSSCanada/GSSCanada-main/0_Occupancy/` and `eSim_occ_utils/` exist (read only for us). `occ_config.py:24-25,39-40` silently creates a wrong Linux base folder, so every job sets `GSS_BASE_DIR` to the staging copy.
  - Next: Stage 1b (`impl/2026-09-19_WP9_stage1b_rebuild.md`): patch the four census chains (2005, 2010, 2015, 2022) in a staging copy, see Gate 1 fail on the April files, submit the four rebuilds at one CPU each. 2025 waits for the author (unchanged). Stage 3 test jobs still running.
- 2026-09-19 (q): **Stage 1b returned (`impl/2026-09-19_WP9_stage1b_rebuild.md`, Status SUBMITTED). VETTED: the patches and the gate SURVIVE; three of the four rebuilds need a path fix.**
  - **Gate 1 seen failing first, as required.** Job 1339764 on the April files reproduced every expected verdict from the Fable report Q8: day-type FAIL on 2005 (0.6005 vs 0.4052) and 2015 (0.5886 vs 0.3841); night FAIL on 2005 (0.5059) and 2010 (0.4932); both PASS on 2022 (0.4708 vs 0.5803; 0.7990) and 2015 night (0.8875). Means agree to 2-3 decimals. The gate is trusted. (It read the staged `BEM_Schedules_2022.csv`, which gives Fable's 2022 numbers exactly.)
  - Patches read by the manager in the task doc: P1 remap `{1: 4, 2: 7, 3: 1}` with an assert that no new NaN appears (any code outside 1-3 trips it); P2 `occCount`/`nGrid` in all four aggregation files, set to 0 on the empty branch; P3 converter switch on `OCC_DENOM` (`grid`, `hhsize`, anything else raises). No other change. Converter menu numbers read from each `*_main.py`: 2015 is `--run 5` alone / `--run 6` full; the others `--run 4` / `--run 5`.
  - Local vs cluster mirror: 9 of 10 files identical; `21CEN22GSS_occToBEM.py` differs (local 517 lines with the deprecation banner, mirror 459). The local file was used, per the task doc. The mirror's older copy is not investigated and does not affect this run.
  - First rebuild attempt (1339765-1339768) died on import: the venv lacked `seaborn` (and `tqdm`). Job 1339769 installed both (COMPLETED 0:0). Second attempt 1339770-1339773: **2005 (1339770) RUNNING**, and its log shows the remap applied with no new NaN (`DDAY NaN before=0, after=0`) and the weekday catalogue now larger than the weekend one (2,336 vs 1,043 profiles), which is the right direction. **2010, 2015 and 2022 FAILED in seconds for path reasons**: 2010 and 2022 Step 0 looks for raw episode files under the code folder (`.../occ/code/0_Occupancy/...`) instead of `GSS_BASE_DIR`; 2015 alignment reads a hard-coded Mac path (`/Users/.../2ndJournal/Outputs_GSS/out15EP_ACT_PRE_coPRE.csv`), prints a warning and still says "Alignment complete" before matching fails. The failed jobs stay in the ledger.
  - Process note: the helper ran `find` and `du` on the login node (read only, no python). Outside the allowed list; recorded in its doc, and the next task doc forbids it by name.
  - Next: Stage 1b-2 (`impl/2026-09-19_WP9_stage1b2_path_fix.md`, Sonnet): list every path that escapes `GSS_BASE_DIR` in all four chains (2005 included, since a later step may still hit one), fix with a symlink for code-relative paths and a staged-only path patch (P4) for Mac paths, resubmit 2010, 2015, 2022. 2005 is left running. Stage 3 test jobs 1339756/1339757 still running (about 25 min in).
- 2026-09-19 (r): **Stage 1b-2 returned (`impl/2026-09-19_WP9_stage1b2_path_fix.md`, Status SUBMITTED). VETTED: the path fix SURVIVES; the 2015 run it submitted did NOT, and was cancelled.**
  - Path causes read in the doc: 2010 and 2022 `*_main.py` pass `Path(__file__).parents[2]` (the code folder) as the project root to Step 0 (`11CEN10GSS_main.py:99`, `21CEN22GSS_main.py:155`). Fixed with no code change: a symlink `.../occ/code/0_Occupancy -> .../occ/0_Occupancy` made in job 1339779, after a guard that it did not exist. 2015 read a hard-coded Mac path for its episode file; P4 (staged copy only) builds it from `OUTPUT_DIR_GSS`, the way 2005 already does. 2005 has no such path. `occ_config.py`'s fallback is guarded by `GSS_BASE_DIR` and left alone.
  - **2015 (1339781) skipped the day-type fix.** `16CEN15GSS_alignment.py:893-905` reuses `Outputs_GSS/GSS_2015_Merged_Main.csv` if it exists, and the April copy came across with the inputs; the remap lives inside the step that builds that file. The helper predicted it; the log confirmed it (no `P1 day-type remap` line; catalogue Weekday 1,112 vs Weekend 2,698, the April swap). Manager cancelled it, moved the April file and the partial `Outputs_16CEN15GSS` aside with renames (job 1339785, nothing deleted), and resubmitted the unchanged script as **1339786**.
  - Lesson for the gate list: a fix that lives inside a cached build step never runs if the cache came along with the inputs; check the build step's own log line, not only that the job started.
  - 2010 (1339780) and 2022 (1339782) are past Step 0 and matching, with weekday catalogues larger than weekend (2,102 vs 835; 1,920 vs 734). 2022 matches from the April `GSS_2022_Merged_Episodes.csv` (reused) although Step 0 rewrote `out22EP_ACT_PRE_coPRE.csv` today; 2022 needs no day-type fix, so this is recorded, not acted on.
  - Process note: the helper also ran `find` once on the login node, against the rule in its own task doc. Read-only; recorded.
  - Next: confirm the `P1 day-type remap` line in 1339786's log; then read each rebuild's Gate 1 lines on both denominators. Stage 3 test jobs still running.
- 2026-09-19 (s): 2015 rebuild 1339786 confirmed: log line 43 `P1 day-type remap: DDAY NaN before=0, after=0`, and the catalogue is now Weekday 2,698 vs Weekend 1,112 profiles, the exact mirror of the cancelled run's 1,112 vs 2,698. The day-type fix is now in both rebuilds that need it (2005, 2015). All four rebuilds and both Stage 3 test jobs RUNNING (about 50 min for Stage 3). Next: Gate 1 lines of each rebuild when it ends.
- 2026-09-19 (t): **D-1J-A2 RULED by the author: option A, divide by the members who have a presence grid (`OCC_DENOM=grid`).** Manager check before the ruling: 2J and 3J already do exactly this (household occupancy = mean of the members' at-home values over member diary-days: `2J_docs_occ_nTemp/07_aug_to_bem.py:97`, `3J_docs_occ_nTemp/Leg3_4-split/Step7_docs/3rdJ_07_aug_to_bem_4split.py:314`, 2J manuscript `draft_S2_framework.md:147-156` Eq. 6); 4J drives each dwelling with one person's diary, so the question does not arise there. Consequence: each rebuild's `..._grid.csv` is the file carried into Stage 2; `..._hhsize.csv` is kept as a sensitivity check only. The manuscript must say that children under 15 have no presence grid and so do not enter the occupancy fraction (limitation wording). Still open for the author: the 2025 path. Next: Gate 1 lines of the four rebuilds (grid file is the one that counts); Gate 3 and R7.
- 2026-09-19 (u): **2005 rebuild 1339770 FAILED in its last step only; fixed with P5 and re-run as 1339828 (converter only).** Steps 1-3 completed; the converter died at the first household on `max(wd_tier, we_tier)` with a NaN match tier, a pre-existing line (message reproduced locally), not our patches. The same line sits in all four converters, so P5 (non-text tier becomes empty; MATCH_TIER label only, occupancy and metabolic untouched; prints its own line with a count) was uploaded to all four, pre-P5 copies kept as `.pre_P5`. 2010/2015/2022 were mid-run; whether they use P5 is read from their logs. Author-facing checklist gained STEP 6A (`REVISION_STEPS.txt`) and the tracker page shows it (page version 2, db v20). Next: read 1339828's P5 line, count and Gate 1; watch 2010/2015/2022 Step 4; Gate 3 and R7 from 1339756/1339757 (both still running at 1 h 41 min).
- 2026-09-19 (v): **Author: YES, fix 2025 the same way (Ruling A, grid denominator).** Correction by the manager, found before any change: the 2025 aggregation and converter are not in `run_step3.py` (a thin wrapper) but inside `25CEN22GSS_classification/previous/eSim_dynamicML_mHead.py`, a never-modify file (the Fable report `inv_1J-01_REPORT_fable.md:397-399` implied `run_step3.py` held it). Approach chosen so both the author's yes and the never-modify rule hold: a staging-only driver imports the module unchanged and overrides the two methods at run time (P2/P3/P5 equivalents, each printing its own line); the protected file's md5 is recorded before and after. Stage 1c task doc `impl/2026-09-19_WP9_stage1c_2025.md`, Sonnet helper dispatched in the background. Only aggregation and conversion are re-run (2025 has no day-type swap; the CVAE and the matching are not touched). Author questions now open: none; one item to bring later with a measured recommendation: F-1J-8 (census age code 88 'not available' mapped to 75+).
- 2026-09-19 (w): **2015 rebuild 1339786 FAILED in its converter only, same NaN-tier crash; P5 did not reach it (module loaded before upload, no P5 line).** Converter-only re-run 1339839 submitted. 2010 and 2022 (still running, likely to hit the same line) get contingent converter-only re-runs 1339840/1339841 that start only if their full job fails. Stage 1c (2025) helper still working. Stage 3 test jobs at 2 h 03 min, still running. Next: read P5 lines + Gate 1 from 1339828 and 1339839.
- 2026-09-19 (x): **2005 rebuild DONE: 1339828 COMPLETED in 23 min; P5 line present; Gate 1 PASSES on both files.** Grid (the one that counts): weekday 09-16 mean 0.380 < weekend 0.566 (day types now the right way round), night presence at 2-person homes hour 3 = 0.952 (band >= 0.70), all 56,910 household/day-type groups have 24 rows. Hhsize sensitivity file: 0.377 / 0.562 / 0.946, same verdicts. New finding F-1J-9: P5 counted a non-text match tier in ALL 56,910 groups, so the 2005 rebuilt schedule file carries no MATCH_TIER label at all (occupancy and metabolic are unaffected; the label is bookkeeping only). The four tier shares the paper must report (STEP 5) must therefore be read from the matcher's own keys output, never from the schedule file. **2025 (Stage 1c): first helper stopped BLOCKED** because the aggregation input is gone locally and the cluster mirror holds a different, ~70x smaller 2025 population. The manager found the April matching keys locally (`0_Occupancy/Outputs_Aligned/Matched_Population_Keys.csv`, 30,273 persons). **Ruling D-1J-A3 (manager, reversible):** rebuild 2025 from those keys (expansion, refinement with fixed-seed forests, aggregation, conversion); never re-run the matching (unseeded, not reproducible). An original-formula conversion is compared with the April file first (reproduction check, bands fixed in the task doc before any run: EXACT / STATISTICAL / FAIL / NOT_EVALUABLE), then the grid and hhsize files. Keys are dated Apr 8, the April file Apr 23, so reproduction is not assumed. Second Sonnet helper dispatched (T5-T8, `impl/2026-09-19_WP9_stage1c_2025.md`). 2010, 2022 (1 h 40 min), 2015 converter re-run and both Stage 3 test jobs (2 h 11 min) still running. Next: read 1339839; Stage 3; the 2025 job.
- 2026-09-19 (y): **2010 rebuild DONE: 1339780 COMPLETED (1 h 43 min), full chain, P5 line present, Gate 1 PASSES on both files.** Grid: weekday 09-16 0.367 < weekend 0.548, night at 2-person homes hour 3 = 0.950, all 64,958 groups have 24 rows. Hhsize: 0.320 / 0.471 / 0.809, same verdicts. P2 is visible in the aggregation output columns (`occCount`, `nGrid`, log line 198) and P3 in the two files differing (night 0.950 vs 0.809); the census-year P2/P3 do not print their own line, so this is indirect evidence, recorded as such. F-1J-9 holds for 2010 too (all 64,958 groups have no text tier). Contingent re-run 1339840 cancelled (not needed). Two of four census-year rebuilds done. Next: 2015 converter re-run 1339839, 2022 1339782, Stage 3 tests.
- 2026-09-19 (z): **2025 TensorFlow gap ruled (D-1J-A4, manager, reversible).** The second helper finished T5-T7 (driver `--from-keys`/`--convert`, reproduction check seen failing first 4/4, byte-exact upload) and T8 job 1339862 exited 3 `TF MISSING`: the GSSCanada venv has no TensorFlow and the protected module imports it at its top. Ruling: a separate venv `/speed-scratch/o_iseri/1J_rerun/venv2025` with the GSSCanada venv's exact package versions plus tensorflow-cpu, in one resolver call; the GSSCanada venv is only read, snapshotted before and after. System module TF 2.4 rejected (needs numpy < 1.20, venv has 2.0.2); pip into the GSSCanada venv stays forbidden. TF version cannot change results: this path calls no TF function, only imports it; the reproduction check tests that. Parity check seen failing first on synthetic lists. Build job 1339868 (1 CPU, 8G); rebuild re-submitted as 1339869 (afterok:1339868), job script changed in one line (`set PY`). Also noted: the second helper ran `find` once on the login node (read only, 28 files); not to be repeated. Next: read 1339868, then 1339869; census jobs and Stage 3 still running.
- 2026-09-19 (aa): **venv2025 built: 1339868 COMPLETED in 2.5 min, every printed check present** (`PINNED PACKAGES: 82`, `VENV PARITY: PASS`, `GSS VENV UNCHANGED: YES`, TF 2.20.0 with numpy 2.0.2 kept, `MODULE IMPORT: OK`). **The chained 2025 rebuild 1339869 then FAILED exit 4 after 13 s**: TensorFlow imported, `SEED set: 20260919` printed, then `import run_step2` raised `ModuleNotFoundError: eSim_occ_utils` and the Step-1 guard fired. Cause is in the manager-owned driver, not the repo: `_add_repo_dirs_to_path()` put `eSim_occ_utils` itself on the path, one level too low for `from eSim_occ_utils.occ_config`; neither the T5 selftest nor the venv check imported `run_step2`, so nothing could catch it. Fixed in the driver (adds the `code/` folder, prints `IMPORT PATH FIX`) plus a new job Step 0c that imports the whole chain and prints `IMPORT CHAIN: OK|FAILED` (exit 10); 1339869 is its seen-failing case. Uploaded byte-exact, re-submitted as **1339873**. Detail: `impl/2026-09-19_WP9_stage1c_2025.md` END.
- 2026-09-19 (ab): **2022 rebuild DONE: 1339782 COMPLETED (2 h 01 min), full chain, P5 line present, Gate 1 PASSES on both files.** Grid: weekday 09-16 0.534 < weekend 0.598, night at 2-person homes hour 3 = 0.967, all 73,808 groups have 24 rows. Hhsize: 0.456 / 0.514 / 0.813, same verdicts. P2 visible only as the `occCount`/`nGrid` aggregation columns and P3 only as the two files differing (indirect, as for 2010). F-1J-9 holds for 2022 (all 73,808 groups have no text tier). Contingent 1339841 cancelled. Note the 2022 weekday/weekend daytime gap (0.06) is much smaller than 2005/2010 (about 0.18); it still passes, and the smaller gap fits more people at home on weekdays after 2020, but that reading is not measured here. Three of four census years done. 2025: 1339873 passed the new import check (`IMPORT CHAIN: OK`, 30,273 matched agents loaded) and is running. Next: 2015 converter re-run 1339839, 2025 1339873, Stage 3 tests.
- 2026-09-19 (ac): **2015 rebuild DONE: converter re-run 1339839 COMPLETED (34 min, exit 0), P5 line present, Gate 1 PASSES on both files. Stage 1b (all four census years) is DONE.** Grid: weekday 09-16 0.402 < weekend 0.557, night at 2-person homes hour 3 = 0.933, all 62,334 groups have 24 rows. Hhsize: 0.354 / 0.486 / 0.791, same verdicts. P1 was confirmed earlier in the full-chain log (1339786 :43, no new NaN); P2 visible as the `occCount`/`nGrid` columns (1339786 :314); P3 only indirect (the two files differ). F-1J-9 holds for 2015 too (all 62,334 groups have no text tier), so it holds in every census year. Grid files for Stage 2: `Outputs_{06CEN05GSS,11CEN10GSS,16CEN15GSS,21CEN22GSS}/occToBEM/*_BEM_Schedules_sample25pct_grid.csv` under `/speed-scratch/o_iseri/1J_rerun/occ/0_Occupancy/`. Weekday/weekend daytime gap by year (grid): 2005 0.19, 2010 0.18, 2015 0.15, 2022 0.06. Still running: 2025 1339873, Stage 3 test jobs 1339756/1339757 (2 h 47 min). Next: 2025 result; Gate 3 and R7.
- 2026-09-19 (ad): **2025 rebuild DONE: 1339873 COMPLETED (46 min), every patch printed its own line, protected files unchanged (md5 start = end), Gate 1 PASSES on the grid file. Reproduction check verdict FAIL by the pre-registered rule, on one label only; FAIL recorded, not moved.** Occupancy and metabolic values match the paper's April 2025 file exactly on all 1,146,336 rows (max difference 0.000000). The FAIL is 24 rows of dwelling type; diagnosis job 1339935 found they are one household-day (household 71748, weekend) that the April file labelled `Movable` while the same household's weekday was `DuplexD`: the April file gave one home two dwelling types, the rebuild gives it one. **Manager ruling (reversible): the rebuilt 2025 grid file goes to Stage 2.** Grid Gate 1: weekday 09-16 0.404 < weekend 0.536, night 0.947, 47,764 groups x 24. The hhsize file fails night presence (0.580), as expected when only 37 % of 2025 homes have a schedule for every member: this is why Ruling A was needed. All five years' grid inputs now exist. Detail: `impl/2026-09-19_WP9_stage1c_2025.md` END. Still running: Stage 3 test jobs 1339756/1339757 (3 h 45 min). Next: Gate 3 and R7 when they end; meanwhile write the Stage 2 task doc (seeded draw manifest, Gate 2).
- 2026-09-19 (ae): **Stage 2 (choose the simulated households before simulating) written and dispatched** (`impl/2026-09-19_WP9_stage2_draw_manifest.md`, Sonnet helper in the background). Found while designing it: the April code gave each year the single household that best matched a fixed "working" profile (`main.py:2202`, `integration.find_best_match_household`), not a random one, so years were biased toward working-like households and draws repeated. Ruling B replaces this. Design fixed by the manager: pools = the simulation's own loader with region Quebec, blank-region and incomplete households dropped; each building's household size drawn per draw from sizes available in all five years (years weighted equally); each year then gets a random household of that dwelling type and size, seeded by text keys (adding draws never changes earlier ones); 30 draws x 6 neighbourhoods written now; no fallback. Gate 2 has six checks (one label per household, right group, no reuse, distinct samples, not an accident of ranking, reproducible), each seen failing first locally, and on Speed on the April 2025 file and on an "April rule" control sample. All four census grid files confirmed present by `ls`. Stage 3 test jobs still running (about 4 h). Next: read the helper's doc, then its job.
- 2026-09-19 (af): **Stage 2 helper returned (`impl/2026-09-19_WP9_stage2_draw_manifest.md`), job 1339951 submitted and RUNNING. VETTED: the scripts and the gate SURVIVE; no verdict on the real data yet.**
  - **Gate 2 seen failing first, locally, on synthetic files the helper wrote** (7 households, 3 strata, a 3-building fake neighbourhood, importing the real `eSim_bem_utils` code unmodified): good case all six checks PASS; a duplicated household -> G2.2 FAIL (1 instance); a changed household size -> G2.1 FAIL (1 mismatch); a household carrying two dwelling types -> G2.0 FAIL (1 household); a missing input -> all six NOT_EVALUABLE with exit 0. The three states stay apart, as required.
  - **One real bug found by the gate's own independence and fixed**: Gate 2 re-derives eligibility from the IDFs and `pool_summary.csv` instead of trusting the manifest, and that read-back path crashed comparing a string count with an integer (`pool_counts_by_key()` now casts to `int`). The good case was re-run after the fix.
  - **G2.4 has no power on the synthetic fixture** (every synthetic household in a stratum has identical hours, so the variance is zero). The April-rule control did fail G2.3 locally (1 distinct set out of 3). Whether G2.4 can catch the April bias on the real data is unknown until the job's control lines are read; the task doc's fallback ("then G2.4 lacks power and the manager decides") applies.
  - Manager-checked deviations, all accepted and recorded in the doc's Decisions: the `seed` column carries each row's full seed string, not only the date; `sd_weekday_hours` is the sample standard deviation; the repeat, 10-draw and control builds sit in the same flat folder with `_repeat`/`_draws10`/`_control` suffixes; the control was built at the full 30 draws; `stage2/` was created by `scp -r` (no bare `mkdir` on the login node), the workaround already on record from Stage 1c.
  - Job 1339951 (1 CPU, 32G, log `/speed-scratch/o_iseri/1J_rerun/logs/wp9_stage2_1339951.out`) builds the real manifest, repeats it, builds a 10-draw prefix, builds the April-rule control, then gates the real manifest (all six), the control (G2.1-G2.4) and the April 2025 reference file (G2.0 alone). Started 2026-09-19 late afternoon; a background watcher polls it.
  - To read when it ends: three `GATE2 SUMMARY:` lines plus `JOB DONE`, the start/end md5 pair for `integration.py` and `neighbourhood.py`, and any `NOT_EVALUABLE: no eligible size for ...` line (that would be a real finding, not a control result). If G2.0 does not FAIL on the April 2025 file, G2.0 itself is not trusted and nothing else from it is used.
  - Stage 3 test jobs 1339756/1339757 still running (4 h 00 min).
  - Next: score Stage 2 when 1339951 ends; Gate 3 and R7 when the test jobs end.
- 2026-09-19 (ag): **F-1J-8 (census age code 88, "not available", mapped to 75 and over) sent for measurement** (`impl/2026-09-19_WP9_f1j8_age88.md`, Sonnet helper in the background). It is the only item still owed to the author, and it must be settled before Stage 4, because fixing it would mean rebuilding every year again.
  - Confirmed in the code first: `06CEN05GSS_alignment.py:56` `if x >= 13: return 7  # 75+` catches 88 as well as the real 75+ codes, and the same three lines sit in the 2010, 2015 and 2022 alignment files. Age group is a matching variable, so these persons receive the diary of someone aged 75 or over, who is at home much more than average.
  - The measurement: persons coded 88 and their share of the matched population; households holding at least one (linked by `SIM_HH_ID`); the daytime, weekend and night occupancy of those households against the rest; and the headline weekday 09-16 mean recomputed with them removed. 2025 is a different pipeline and is not measured.
  - **Decision rule pre-registered before any number is read:** largest absolute shift in the headline weekday mean across the four years `d <= 0.005` -> leave the code alone and name it as a limitation; `d > 0.010` -> fix the mapping and rebuild the affected years; in between -> the author decides. A year whose household link fails is NOT_EVALUABLE and does not enter `d`.
  - Seen failing first, required before the real numbers: a synthetic file where the affected households are occupied all day must show a clearly large shift; the same file with nobody coded 88 must show exactly zero; a person-level table with no `SIM_HH_ID` must print `LINK: NOT AVAILABLE` and still exit 0.
  - Next: the helper's job, then Stage 2 (1339951), then Gate 3 and R7.
- 2026-09-19 (ah): **Handover prompt rewritten for a cheaper manager model** (author's decision: "we are ready to pass cheaper sonnet agent for manager session"). `1J_docs_occ/Prompts/1J_manager_prompt_RESUME.md` now executes rather than designs.
  - New §1 says what that session may not do: invent or move a threshold, move a band because a result failed it, write a number it has not read, redesign a stage, edit pipeline code, or create an unasked file. Anything not already written down goes to the author as one line instead of a guess.
  - Every remaining stage now has a written recipe: score Stage 2, score Gate 3 and R7, settle F-1J-8, recompute Section 4.1, then Stage 4 and Stage 5. Each says the exact command to run, the exact lines to read, and what each outcome means.
  - **The Stage 4 stopping rule is written out in full, in §7.1, to be pasted into this log before Block 1's results are read**: mean annual heating and cooling per neighbourhood and year, draws in blocks of five, stop when the 95 % interval's half-width is at most 1 % of the mean everywhere or at 30 draws, the interval never inspected before the rule is written, the 1 % never changed afterwards, and if 30 draws are reached unmet the paper reports what it achieved and says the target was not met.
  - §7.2 fixes the reporting rules (means with intervals; a difference may only be called a difference if its interval excludes zero; tier shares from the matcher keys, never a schedule file; the shared 2005/2010/2015 population stated; "first", "nationally representative", "replicable" gone; prose says limitation, gates keep FAIL). §7.3 lists the wording still owed, including the children-under-15 sentence.
  - Two practical repairs: the progress-page update no longer depends on the old session's helper script (read the document to a file, edit it with `py`, set it back pinned by version), and the cold start now says the previous session's employees and watchers are invisible, so a task doc is re-dispatched only when its Ledger holds no job id and nothing has been written for over half an hour.
  - A memory pointer was added so any future session starts at the handover prompt and the log, not somewhere else.
  - Next: the Sonnet manager scores Stage 2 (1339951), Gate 3 (1339756), R7 (1339757) and F-1J-8 as each job ends.
- 2026-09-19 (ai): **Gate 3 PASS; Gate 2 real manifest FAIL on G2.0 — a new, much bigger finding; Stage 4 does NOT start. F-1J-8 re-dispatched (fresh employee — the one from (ag) never actually ran, empty Ledger, Status still NOT STARTED). Section 4.1 recompute (5.4) dispatched, independent of the Gate 2 block.** Author is away for the weekend; session continues on its own, no new ruling made.
  - **Gate 3: PASS.** `1339756`'s own `aggregated_eui.csv` gives Default heating **35.1170** and cooling **45.6120** — an exact match to the April targets (35.117 / 45.612). The job itself exited FAILED (`sacct` exit 1:0, 4 h 03 min) but only after the simulation and aggregation finished clean (`SIMULATION SUMMARY: Successful 5/5, Failed 0/5`); the crash is one downstream plotting line, `FAILED: NUS_RC1.idf — [Errno 2] No such file or directory: '.../BEM_Setup/SimResults_Plotting/MonteCarlo_Neighbourhood_EUI_draw_1_NUS_RC1.png'` — a missing output folder for a PNG, unrelated to the numbers. The Gate 3 verdict is read from the CSV, not the exit code. **Checkbox 6 ticked (`checks.s6a[5]`).** `1339757` (R7, RC6) still RUNNING at ~4 h 10 min, not scored.
  - **Gate 2 real manifest: `G2.0 = FAIL` — new major finding, much bigger than known.** `70,281` households across the rebuilt files carry more than one dwelling-type (`DTYPE`) label: 2005=17,362, 2010=15,100, 2015=19,757, 2022=18,061, 2025=1. This dwarfs the single known case (household 71748, plan log (ad)). **G2.0 is trusted**: run alone on the April 2025 reference file it correctly FAILs too (2 multi-label households there, Step 7 of the log). `G2.1, G2.2, G2.3, G2.4, G2.5` all PASS on the real manifest. The control (April-rule) manifest correctly FAILs `G2.3` (10-26 distinct sets out of 30 draws, not 30) and `G2.4` (z as large as -33.9) — closes the open question from (af)/(ag): **G2.4 has power on real data, it is not a dead check.** Per §5.1 of the manager prompt: **anything other than G2.0/G2.1/G2.2/G2.5 all PASS is a real finding, Stage 4 does not start.** No cause investigated: not written down anywhere in any task doc, so none is guessed here.
  - **Open item for the author (new, not decided here, no threshold invented):** what a household with two different `DTYPE` labels across its own rows means for Stage 2/3/4 — a data-audit finding to disclose only, a case to exclude from the drawing pool, or something else. Nothing rules on this; it sits until the author reads it.
  - **F-1J-8** (`impl/2026-09-19_WP9_f1j8_age88.md`): the employee dispatched at (ag) never actually ran (Status still NOT STARTED, empty Ledger, several log entries later). Re-dispatched fresh. New employee confirmed the file schemas by hand (`head`), chose `<YEARTAG>_Matched_Keys_sample25pct.csv` as the true person-level file (recorded under Decisions in the doc, with its reasoning), ran the three seen-failing-first checks locally and all three matched the pre-registered expectations exactly (forced-occupied case: `diff=-0.32`; no-88 case: `diff=0.0`; no-`SIM_HH_ID` case: `LINK: NOT AVAILABLE`, exit 0). Submitted job **1339963** (1 CPU, 32G), log `/speed-scratch/o_iseri/1J_rerun/logs/wp9_f1j8_1339963.out`, summary file `f1j8/results/age88_summary.json`. Not yet polled.
  - **Section 4.1 recompute (5.4) written and dispatched** (`impl/2026-09-19_WP9_section41_recompute.md`), independent of the Gate 2 block (it only reads the five already-rebuilt grid files, no threshold, replaces stale numbers). Employee working in the background; not yet returned this entry.
  - Closure ritual done this turn: progress page updated (checkbox 6 ticked, new plain-language log lines, a new `waiting` item for the G2.0 finding), manager prompt §4/§5/"Last updated" rewritten, `REVISION_STEPS.txt` STEP 6A line updated.
  - Next: read F-1J-8's job (1339963) and the Section 4.1 employee's job once both are at least ~30 min old; read R7 (1339757) when it ends; the G2.0 finding needs the author's read before Stage 4 can be designed at all — nothing to guess here in the meantime.

- 2026-09-19 (aj): **Section 4.1 recompute (5.4) DONE.** Job **1339964** completed cleanly (exit 0:0, 37s). Real numbers for all five rebuilt years, from `impl/2026-09-19_WP9_section41_recompute.md` Verified section:

  | Year | weekday 09-16 mean (hh) | weekend 09-16 mean (hh) | night hour-3 mean (hh) | old paper number |
  |---|---|---|---|---|
  | 2005 | 0.379451 (28,455) | 0.565715 (28,455) | 0.949221 (28,455) | 15.48 |
  | 2010 | 0.366758 (32,479) | 0.548114 (32,479) | 0.943885 (32,479) | 11.89 |
  | 2015 | 0.402325 (31,167) | 0.556631 (31,167) | 0.934063 (31,167) | 17.55 |
  | 2022 | 0.534344 (36,904) | 0.597502 (36,904) | 0.963185 (36,904) | 15.90 |
  | 2025 | 0.403750 (23,882) | 0.536351 (23,882) | 0.948663 (23,882) | N/A (new year, no old number) |

  Full 48-cell Hour×Day_Type table and the HHSIZE-bucket splits (items 1 and 4) are in the log, `/speed-scratch/o_iseri/1J_rerun/logs/wp9_section41_1339964.out`, and were not transcribed into the task doc in full. **These new numbers are fractions (0-1, share of households present), not "occupied hours" like the old paper number — the printed "difference (new - old)" line in the log is a raw subtraction of two different units and must never be quoted as a magnitude of change.** Whoever rewrites Section 4.1 prose states the new numbers as shares, not hours, and does not claim the old and new numbers are on a comparable scale. No threshold applied — this was a pure replacement of stale numbers, per the task doc's own framing (5.4 recipe). Employee's local seen-failing-first hand-check (3 synthetic households) matched every hand-computed value exactly, including empty-bucket (HHSIZE 3,4) no-crash behaviour, before the real job was trusted.
  - Still open, unaffected by this: R7 (1339757, RUNNING at 4h18m elapsed as of this check), F-1J-8 (1339963, not yet polled), and the G2.0 blocking finding (still needs the author, Stage 4 still does not start).
  - Closure ritual done this turn: plan log (this entry), progress page log line + checkbox, manager prompt §4/§5/"Last updated", `REVISION_STEPS.txt` STEP 6A line.
  - Next: keep polling R7 (1339757) and F-1J-8 (1339963); nothing else is actionable until one of those lands or the author responds to G2.0.

- 2026-09-19 (ak): **F-1J-8 (age-code-88 measurement) DONE.** Job 1339963 completed (exit 0:0, 18s). Real numbers, from `impl/2026-09-19_WP9_f1j8_age88.md`:

  | Year | persons coded 88 | affected households | shift in headline mean if dropped |
  |---|---|---|---|
  | 2005 | 1,594 | 0 | 0.000000 |
  | 2010 | 11,586 | 1,782 | -0.002871 |
  | 2015 | 26,095 | 0 | 0.000000 |
  | 2022 | 30,908 | 3,502 | -0.007813 |

  `d = 0.007813` (2022, the largest shift). Applying the pre-registered rule exactly as written (d≤0.005 limitation-only, d>0.010 fix-and-rebuild, in between ask the author): **0.007813 is in the in-between band — no automatic call, this is a second open item for the author, alongside G2.0.** No rebuild started, no limitation text written.
  - **Bonus finding, not asked for but relevant**: 2005 and 2015 show zero affected households despite thousands of persons coded 88, while 2010 and 2022 show real ones. The employee checked this against the raw cluster files directly (not just the script's own output) and confirmed it is a true zero — in 2005 and 2015 the code-88 persons never reach the matched-person file at all, for a reason upstream of the mapping bug that was not chased further (out of scope for this measurement task). This means 2005 and 2015 are unaffected by the bug regardless of what the author decides for 2010/2022.
  - Person counts (1,594/11,586/26,095/30,908) match the manager's earlier scan (plan log (p)) exactly — two independent reads agree.
  - Still open: R7 (1339757, still running), and G2.0 (still needs the author). Now **two items wait on the author**: G2.0 (Stage 4 blocking) and this in-between `d` (does not block anything, informational).
  - Closure ritual done this turn: plan log (this entry), progress page log line, manager prompt §4/§5.3/"Last updated".
  - Next: keep polling R7 (1339757); nothing else is actionable until it lands or the author responds.
