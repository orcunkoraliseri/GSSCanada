# Item 5.4 — decoding temperature — implementation state

Task doc:   `Step5_docs/4thJ_05_populationLinkage.md` §5.4
Status:     IN PROGRESS — three jobs submitted 2026-08-21, nothing read back yet

## Ledger

| JobID | fold | what | state | exit | output |
|---|---|---|---|---|---|
| 1285584 | es | temperature sweep, 9 grid points | SUBMITTED, RUNNING at 0:05 on speed-39 | — | `/speed-scratch/o_iseri/4J_step5_temp_1285584.out` |
| 1285585 | uk | temperature sweep, 9 grid points | SUBMITTED, RUNNING at 0:05 on speed-39 | — | `/speed-scratch/o_iseri/4J_step5_temp_1285585.out` |
| 1285586 | it | temperature sweep, 9 grid points | SUBMITTED, RUNNING at 0:05 on speed-39 | — | `/speed-scratch/o_iseri/4J_step5_temp_1285586.out` |

Artefact each writes: `/speed-scratch/o_iseri/4J_step5/temperature_calibration_<fold>.json`.

## Verified before submitting

* **Two adapter chains exist per fold**, both complete, both 96,498,736-byte adapters:
  `4J_step4/runs/leg4_primary_fold_<c>/adapter` (Aug 19) and
  `4J_step4/runs_ds45/leg4_primary_fold_<c>/adapter` (Aug 20, the D-S4-5 basis chain).
  🟢 **Author ruled 2026-08-21: `runs_ds45`.** D-S4-5 (b) is the registered basis for `G4.1`, so
  those are the runs whose verdicts the paper reports; calibrating the older weights would report
  one model's gates and ship another model's diaries. 🔴 Known cost carried knowingly: fold `it`
  DIVERGED in that chain.
* **The validation split comes from the shard manifest**, key `folds.<c>.heldin_val`, not re-derived:
  `heldin_val_es.jsonl` n=5,520 md5 `67e26e59045303e81bf4fd640f572dc6`. The script re-computes that
  md5 at run time and refuses if it moved. Counts match FINDING 11 (5,520 / 3,434 / 5,702).
* **No held-out country in its own validation split** — checked in code, refuses if present.
  Verified by hand for `es`: the shard's records are `it` and `uk` only.
* The corpus `split` labels are the D-S6-1 household split: heldout 7,328 / train 65,926
  (es 1,808 / it 3,894 / uk 1,626 heldout).
* Step 4's trainer and diagnostics both hardcode `temperature=1.0, top_p=1.0`
  (`4thJ_step4_train.py:747`, `4thJ_step4_diagnostics.py:397`). **Neither was edited.**

## Decisions taken here, and why

1. **Per-fold temperature, never a shared one.** A single global T would be chosen partly on Spanish
   validation diaries, and Spain is the held-out country on fold `es`. That is contamination by the
   same argument `G5.6i` scores. Three folds, three models, three temperatures. Not a preference —
   the alternative is inadmissible.
2. **Paired design.** 600 prompts drawn ONCE with `TH.SEED` and reused at every grid point; the
   reference is those same 600 real diaries. An unpaired draw puts sampling noise between the curve
   and the knob.
3. **Metrics are IMPORTED from `4thJ_step4_train`**, not reimplemented: `activity_entropy_nats`,
   `split_prefix_body`, `parse_episodes`. Step 4 already prints this entropy at every checkpoint
   (`entropy=3.282` on `es` epoch 0), so the numbers have to be comparable. Only the generation loop
   is local, because temperature is the one thing that must differ.
4. **Fidelity = the 144-slot at-home profile MAE (pp)**, with activity-share TVD (pp) reported
   beside it. Chosen for continuity: `TH.G4_1_STATISTIC` is `at_home_share` and Step 6 scores the
   same time-of-day family. Item 5.4 never defined "fidelity metrics"; this is the definition, and
   it is written down before the first curve was seen.
5. **No diary weights.** `weight_dia_cal` (FINDING 53) re-bases the real population onto the
   calendar week; that changes which population the reference is, not what the decoding knob does,
   and the design is paired against the drawn diaries. Stated, not silently omitted.

## Pre-registered before the run

* Grid `T ∈ {0.50 … 1.30}`, uniform, 0.1 apart. **Not a search.** If an optimum lands on an
  endpoint it is REPORTED as an endpoint; the grid is not extended to chase it.
* `top_p = 1.0`, `top_k = 0` — **no truncation at any grid point**, per item 5.4's tail hazard.
* `T_entropy = argmin |H_gen − H_real|`; `T_fidelity = argmin at-home MAE`.
* **Agree** iff `|T_entropy − T_fidelity| ≤ 0.1` (one grid step).
* **On disagreement, entropy matching wins** — the step document's own rule, not re-opened.
* A grid point with `parseable_frac < 0.95` cannot supply a temperature whatever its entropy reads.

## Next

1. Read the three `.out` files (`grep 'T=' `, then the `T_entropy`/`T_fidelity` block). **≥30 min
   between polls.** Do not re-derive anything already in this file.
2. `scp` the three JSONs back; write `outputs_step5/temperature_calibration.md` from them — the
   entropy curve, the fidelity curve, and **whether they agree**, which is what `G5.8` scores.
3. Write `outputs_step5/generation_config.json` (the chosen T per fold, `top_p` 1.0, no truncation)
   — `G5.9` reads it, and it must be the config Step 7 actually loads.
4. Re-run `tools/4thJ_gates_step5.py`: `G5.8` and `G5.9` move from BLOCKED to a real verdict.

## WHAT I DID NOT VERIFY

* **Nothing has been read back from any of the three jobs.** No curve, no temperature, no runtime.
  Every number above is an input, not a result.
* Runtime is unmeasured. 9 × 600 generations per fold; Step 4 did 600 per checkpoint, so the
  estimate is ~2–3 h/fold, and it is an estimate.
* 🔴 `G5.9`'s text is "if top-p is used at all, p ≤ 0.98". We do not use top-p, so the config will
  carry `top_p: 1.0`. **A checker that reads `1.0 > 0.98` and fails would be wrong**, and nobody has
  written that checker yet. Flagged, not resolved.
* The three jobs share one node (speed-39). Whether that slows them is not measured.

## Correction, 2026-08-21 late night — runtime is now MEASURED

The estimate above ("~2–3 h/fold") is **wrong by roughly 3x**. Read from `1285584`'s own log at
50 min elapsed:

```
T=0.50  generated 568/600  (48.0 min)
```

One grid point ≈ **50 min**, so nine grid points ≈ **7.5 h per fold**, three folds sharing
speed-39. Well inside the 7-day walltime — no job is at risk — but expect them to land the
following morning, not in three hours. All three were RUNNING at 50:26 when the session closed.

Nothing else changed: no curve, no temperature and no exit code has been read.

---

## 2026-08-21, later — READ BACK MID-RUN. `FINDING 65` + `FINDING 66` + `D-S5-13`

Jobs polled once, `squeue` only, nothing computed anywhere. All three still **RUNNING** on
`speed-39` at elapsed **2:54:26**, partition `ps`, no job at risk.

### Progress at that poll

| job | fold | grid points COMPLETE | at |
|---|---|---|---|
| 1285584 | es | 3 of 9 (0.50, 0.60, 0.70) | inside T=0.80 |
| 1285585 | uk | 5 of 9 (0.50 … 0.90) | inside T=1.00 |
| 1285586 | it | 3 of 9 (0.50, 0.60, 0.70) | inside T=0.80 |

🟢 **The 50 min/point figure is itself wrong — points get FASTER as T rises.** `es`: 48.0 min to
reach 568/600 at `T=0.50`, 47.6 at `0.60`, 43.1 at `0.70`, and `0.80` was at 488/600 by 26.6 min.
Higher T terminates earlier (`terminated_frac` climbs 0.262 → 0.500 → 0.852 → …), so the tail of
the grid is roughly half the cost of the head. Nine points is therefore closer to **5 h/fold** than
7.5, and `uk` is a point-and-a-half ahead of the other two.

### The curves so far — READ, NOT DECIDED

`es` (H_real = 3.5880, at-home 0.7004, 136 codes):

| T | H_gen | dH | at-home MAE pp | ACT TVD pp | parse | term | sum=1440 |
|---|---|---|---|---|---|---|---|
| 0.50 | 2.8394 | −0.7486 | 10.694 | 27.696 | 1.000 | 0.262 | 0.060 |
| 0.60 | 2.8719 | −0.7161 | 9.157 | 19.982 | 1.000 | 0.500 | 0.080 |
| 0.70 | 3.0826 | −0.5054 | 7.580 | 13.502 | 1.000 | 0.852 | 0.103 |

`uk` (H_real = 3.4365, at-home 0.7163, 135 codes):

| T | H_gen | dH | at-home MAE pp | ACT TVD pp | parse | term | sum=1440 |
|---|---|---|---|---|---|---|---|
| 0.50 | 3.1924 | −0.2441 | 11.150 | 24.900 | 1.000 | 0.465 | 0.087 |
| 0.60 | 3.2326 | −0.2040 | 5.917 | 15.831 | 1.000 | 0.785 | 0.092 |
| 0.70 | 3.2825 | −0.1541 | 4.858 | 10.915 | 1.000 | 0.942 | 0.120 |
| 0.80 | 3.2706 | −0.1660 | 4.344 | 9.837 | 1.000 | 0.998 | 0.127 |
| 0.90 | 3.3506 | −0.0860 | 3.216 | 7.968 | 1.000 | 1.000 | 0.135 |

`it` (H_real = 3.5207, at-home 0.6984, 122 codes — 🔴 the DIVERGED fold):

| T | H_gen | dH | at-home MAE pp | ACT TVD pp | parse | term | sum=1440 |
|---|---|---|---|---|---|---|---|
| 0.50 | 2.3753 | −1.1454 | 8.083 | 26.712 | 1.000 | 0.637 | 0.078 |
| 0.60 | 2.4856 | −1.0350 | 7.685 | 21.918 | 1.000 | 0.743 | 0.050 |
| 0.70 | 2.6290 | −0.8916 | 5.116 | 17.611 | 1.000 | 0.888 | 0.050 |

Three things visible already, all three PROVISIONAL — the grid is not finished:

* **`dH` is NEGATIVE at every point on every fold and shrinking with T.** The model is
  under-dispersed everywhere the grid has looked. Both `T_entropy` and `T_fidelity` are heading
  UP the grid, and on `it` the gap is still −0.89 at `T=0.70` closing at roughly 0.13 per grid
  step, so **`it` is on course to put its optimum ON THE UPPER ENDPOINT `T=1.30`**. That is
  pre-registered as REPORTED-as-an-endpoint; the grid is not extended.
* **`parseable_frac` is 1.000 at every point so far** — the `< 0.95` usability floor has not bound
  anywhere yet.
* 🔴 **`it` is `uk`'s curve shifted down by a nat.** `H_gen = 2.375` at `T=0.50` against a real
  3.521. It is the diverged fold behaving like one; read every `it` number that way.

### 🔴 `FINDING 65` — THE SWEEP AS SUBMITTED **CANNOT CLOSE `G5.8`**

`4thJ_05_populationLinkage_val.md` §"THE SENSITIVITY TRAP IN `G5.8`" is not commentary. It states a
**Requirement**, in bold, in the validation document for this gate:

> every temperature level is run at least 5 times with different seeds, and the step-to-step
> difference along the curve must exceed the spread from re-running one level. If it does not, the
> sweep has told us nothing about temperature and the reported deliverable is the **band**, not a
> chosen value.

**The running sweep is ONE realisation per grid point.** `4thJ_step5_temperature.py` has a single
generation call per `T` (`generate_at`, line 239) inside one `for T in grid` loop (line 346); there
is no replicate loop, no seed list, and the result schema has no replicate field. `SEED = TH.SEED`
is used at exactly one place — `rng = random.Random(SEED)` at line 326, the **prompt draw**.

So when these three jobs land they will produce exactly the artefact the trap section describes:
a curve with no error bar, no way to be wrong, and a guaranteed winner. **`G5.8` moves off BLOCKED
only to FAIL**, and it would be right to fail. Nothing about the run is wasted — the curve is real
and the paired design is sound — but it is one of the five realisations the gate asks for, not the
deliverable.

Caught by reading the gate's own validation doc before writing its checker, which is the only
reason it surfaced before the compute was spent rather than after.

### 🔴 `FINDING 66` — THE GENERATION RNG IS NEVER SEEDED, AND THE JSON SAYS `seed: 42` ANYWAY

`generate_at` calls `model.generate(..., do_sample=True)` with **no `torch.manual_seed` anywhere in
the file** (`grep -n seed` returns four hits, all the prompt draw). The sampling RNG is torch's
default state, advancing across the whole run, so:

* the single realisation at each grid point **is not reproducible**, not even by re-running the
  same script on the same node; and
* the artefact writes `'seed': SEED` into `temperature_calibration_<fold>.json` beside the curve,
  where a reader will take it to mean the curve is seeded. It scopes the prompt draw only.

This compounds `FINDING 65` rather than duplicating it: even if somebody argued one realisation
were enough, that realisation cannot be reproduced or re-derived. And it means the current run
cannot be counted as seed #1 of five — the five must be *distinguishable by seed*, and this one has
no seed.

Additive fix when it is fixed: `torch.manual_seed(s)` immediately before each `generate_at`, `s`
recorded per row; rename the existing JSON key to `prompt_seed`. **Not applied — the jobs are
running and the script on Speed must not move under them.**

### ⚪ Also observed, not a defect, worth stating once

**Generated diaries almost never sum to 1440 minutes**: `sum_1440_frac` runs **0.050–0.135** across
all three folds, and it stays low even where `terminated_frac` has reached 1.000 (`uk` `T=0.90`:
terminated 1.000, sums 0.135). Every one of the 73,254 real diaries sums to exactly 1440. This is
not news against Step 7 — item 7.1's grammar mask exists precisely to enforce the budget, and
`G7.10` (the XGrammar back-end) has never been run — but it does say out loud how much of the
budget constraint the decoding knob does **not** supply on its own. Temperature does not fix it:
the fraction moves 0.06 → 0.135 across the whole grid on `uk` and moves the WRONG way on `it`
(0.078 → 0.050). Recorded here so nobody later reads a chosen temperature as evidence the diaries
are well-formed.

### `D-S5-13` — OPEN, AND IT IS THE AUTHOR'S CALL

`G5.8`'s registered sensitivity requirement needs 5 seeds x 9 grid points x 3 folds = **135
generation passes** where 27 were submitted. At the measured cost (~30–50 min a point, cheaper at
high T) that is roughly **25 h/fold** if run the same way. Options, with a recommendation:

* **(a) — RECOMMENDED. Let the three jobs finish, then run 4 further seeded replicates per fold at
  a NARROW window of grid points around the chosen T (the argmin and its two neighbours), and
  report the band there.** Cost ~ 3 points x 4 seeds x 3 folds ~ 8–9 h/fold, submittable as
  parallel per-fold jobs. The spread is then measured exactly where the decision is made, which is
  the only place the requirement's comparison — *step-to-step difference vs re-run spread* — is
  actually evaluated. 🔴 It is a **narrowing of the requirement's text** ("every temperature
  level") and must be ruled and written down as one, not slipped in.
* **(b) The requirement as written: 5 seeds at every one of the 9 levels, all three folds.** ~135
  passes. Defensible, and it is what the doc says; it is days of GPU on a shared node and it
  measures spread at six levels nobody will choose.
* **(c) Kill and resubmit the whole design seeded from the start.** Cleanest audit trail — no
  unseeded realisation glued to seeded ones — and it discards ~3 h of finished compute per fold.
* **(d) Report the BAND, not a value, and do not choose a temperature at all.** The val doc names
  this as the outcome when the sweep is uninformative. It cannot be chosen *yet*: we do not know
  the sweep is uninformative until the spread is measured, and measuring the spread is (a) or (b).

🔴 Whatever is ruled, `FINDING 66` is fixed first — replicates that are not seeded are not
replicates. And the fix is additive: it does not touch the grid, the prompts, the metrics, the
decision rule, or Step 4's basis.

## WHAT I DID NOT VERIFY, this entry

* **No job has finished.** No `T_entropy`, no `T_fidelity`, no agreement verdict, no exit code, and
  no JSON exists on Speed yet. The three tables above are partial curves read from live logs.
* The endpoint projection for `it` is arithmetic on three points, **not a result**.
* `st_real` (the real reference's own structural stats) is written to the JSON but not printed to
  the log, so the real `sum_1440_frac` was not read back — it is asserted from the corpus
  measurement, not from this run.
* No checker for `G5.8` or `G5.9` has been written. The `G5.9` top-p trap flagged above is still
  open and still unresolved.

---

## 2026-08-21 (afternoon) — 🟢 `uk` LANDED CLEAN. 🔴 `FINDING 67`: THE AT-HOME STATISTIC IS CONTAMINATED BY A DEFECT THAT MOVES ALONG THE SWEPT AXIS. NEW `D-S5-14`.

### Ledger

| JobID | Fold | State | Exit | Elapsed | Artefact |
|---|---|---|---|---|---|
| `1285584` | `es` | RUNNING @ 3:59:43, speed-39 | — | — | not yet |
| `1285585` | `uk` | **COMPLETED** | `0:0` | `03:44:37` | `temperature_calibration_uk.json`, md5 `e2401535d61f4783021bbb0f4325afb3` |
| `1285586` | `it` | RUNNING @ 3:59:43, speed-39 | — | — | not yet |

🟢 **Runtime correction, measured:** `uk` took **3 h 45 m**, not the 7.5 h projected from the first
grid point. Points get faster as `T` rises (fewer unterminated generations run to `max_new_tokens`),
so the earlier extrapolation from `T = 0.50` overestimated by ~2x. `es`/`it` should land shortly.

### `uk` result — the full nine-point grid

| T | H_gen | dH | at-home MAE pp | ACT TVD pp | parse | term | sum=1440 |
|---|---|---|---|---|---|---|---|
| 0.50 | 3.1924 | −0.2441 | 11.150 | 24.900 | 1.000 | 0.465 | 0.087 |
| 0.60 | 3.2326 | −0.2040 | 5.917 | 15.831 | 1.000 | 0.785 | 0.092 |
| 0.70 | 3.2825 | −0.1541 | 4.858 | 10.915 | 1.000 | 0.942 | 0.120 |
| 0.80 | 3.2706 | −0.1660 | 4.344 | 9.837 | 1.000 | 0.998 | 0.127 |
| 0.90 | 3.3506 | −0.0860 | 3.216 | 7.968 | 1.000 | 1.000 | **0.135** |
| **1.00** | 3.3771 | −0.0595 | **2.566** | 6.752 | 1.000 | 1.000 | 0.092 |
| **1.10** | 3.4438 | **+0.0072** | 3.277 | 7.182 | 1.000 | 1.000 | 0.085 |
| 1.20 | 3.4541 | +0.0176 | 4.830 | 7.865 | 1.000 | 1.000 | 0.077 |
| 1.30 | 3.5467 | +0.1102 | 7.033 | 10.126 | 1.000 | 1.000 | 0.062 |

`H_real = 3.4365`. **`T_entropy` = 1.10** (|dH| = 0.0072 nats) · **`T_fidelity` = 1.00** (MAE
2.566 pp) · `agree` = **true** · **`T_chosen` = 1.10, basis = entropy matching** ·
`endpoint_entropy` = false, `endpoint_fidelity` = false.

⚪ Both curves are interior and the pre-registered rule fired as written. `dH` crosses zero between
1.00 and 1.10 — the fold is under-dispersed below and over-dispersed above, as the partial curve
predicted.

### 🔴 `FINDING 67` — `at_home_profile()` scores missing diary tails as "away from home"

`tools/4thJ_step5_temperature.py:130`. `home = [0] * N_SLOTS` is initialised to **not-at-home**. A
diary whose episodes total under 1440 min stops filling at `slot < 144`; the untouched tail keeps
its `0`, and line 154 rejects only non-multiples of 10, so the diary is still counted in the
denominator for **all 144 slots**. `structural()` at line 201 has the `sum == 1440` test;
`at_home_profile()` does not use it. The two functions disagree on what a usable diary is.

Real diaries are unaffected — all 73,254 sum to exactly 1440. **Generated diaries are:
`sum_1440_frac` runs 0.062–0.135 on this fold.** The bias is one-sided and depresses the generated
at-home curve in the late slots only.

**Why it is not merely cosmetic.** Computed from the full grid above, not quoted:

* All nine points: Spearman(at-home MAE, `sum=1440`) = **−0.483**. Weak on its own.
* Rising limb `T ≤ 0.90`: **−1.000** — but Spearman(MAE, `term`) is also **−1.000**, so the limb is
  perfectly confounded and proves nothing alone.
* 🔴 **Falling limb `T ≥ 0.90`, where `term` is pinned at exactly 1.000: −0.900.** Every diary
  terminates, the one competing explanation is held fixed by the data, and the MAE still tracks the
  1440 fraction: as diaries fill less of the day (0.135 → 0.062) the MAE rises (2.566 → 7.033).

So part of the at-home MAE curve — **including the location of its minimum at T = 1.00** — is the
artefact moving rather than the model improving.

⚪ **`uk`'s chosen value survives.** `T_chosen` came from **entropy matching**, which never touches
the at-home profile. But `T_fidelity` feeds the `agree` test, `es`/`it` are unread, and `G5.8` is
scored on the curve — so the fix is owed regardless.

### 🔴 `D-S5-14` — for the author

`at_home_profile`'s own docstring says it is *"the same statistic `G4.1` scores"* and *"the same
family Step 6 scores its time-of-day cells on"*, so its denominator is **not local to item 5.4**.

* **(a) RECOMMENDED — additive.** Per-slot denominator (count each slot only over diaries that reach
  it) plus an emitted **coverage curve**, reported **alongside** the existing `at_home_mae_pp`, in
  item 5.4 only. Both numbers printed, neither replaces the other, nothing registered moves. The
  size of the confound becomes measurable and `D-S5-13` can then be ruled on a clean number.
* (b) Change the statistic everywhere — `G4.1`, item 5.4, Step 6. Correct, but a basis change to a
  gate already run; out of scope for an open item.
* (c) Declare and leave. Cheapest; `T_fidelity` stays contaminated.

**Perturbation the new diagnostic must be seen felled by:** feed diaries truncated at 1000 minutes.
The coverage curve must fall to zero after slot 100 and the two MAEs must diverge.

**Cost:** ~20 lines, no re-run of finished work, no cluster time.

🔴 **`D-S5-13` cannot be ruled until `D-S5-14` is.** The replicate design in `D-S5-13`(a) is
specified around `T_fidelity`, and `T_fidelity` is the contaminated quantity.

### Next

1. Read `es` and `it` when they land; append their full grids here.
2. Rule `D-S5-14`, then `D-S5-13`.
3. `scp` the three JSONs back; write `outputs_step5/temperature_calibration.md` and
   `generation_config.json`; then the real `G5.8`/`G5.9` checkers.
4. 🔴 `G5.9`'s antecedent is still unresolved: the gate reads *"if top-p is used at all, p ≤ 0.98"*
   and we do not use top-p, so the config carries `top_p: 1.0`. A checker that reads `1.0 > 0.98`
   and FAILs would be wrong — **the antecedent is false, so the gate is vacuously satisfied.** It
   must be written that way and the perturbation ("set `top_p = 0.9`") must still fell it.

### WHAT I DID NOT VERIFY

* `es` and `it` full grids — still running, three partial points each.
* I did **not** re-derive `uk`'s at-home MAE under a corrected denominator. That number does not
  exist; no claim above depends on its size.
* I did **not** trace whether `G4.1` and Step 6 call this exact function or a copy. The docstring
  asserts it. `D-S5-14`'s scope depends on it and it must be checked before option (b) is weighed.
* `FINDING 65` (one realisation per grid point) and `FINDING 66` (generation never seeded) are
  **unchanged and still open**. `uk` landing does not touch either.

---

## 2026-08-21 (evening) — 🟢 ALL THREE FOLDS ARE IN. 🔴 TWO OF THREE DISAGREE AND `es` SITS ON THE GRID ENDPOINT. `D-S5-14`(a) IS APPLIED AND ITS PERTURBATION WAS SEEN FELLING IT.

### Ledger

| JobID | Fold | State | Exit | Elapsed | Artefact md5 |
|---|---|---|---|---|---|
| `1285584` | `es` | **COMPLETED** | `0:0` | `04:23:37` | `9d4193967d4d751d823678cb3f34e0ed` |
| `1285585` | `uk` | **COMPLETED** | `0:0` | `03:44:37` | `e2401535d61f4783021bbb0f4325afb3` |
| `1285586` | `it` | **COMPLETED** | `0:0` | `04:29:42` | `4234cd3004f2f89b18df3939ff859368` |

All three `temperature_calibration_<fold>.json` are now at `Step5_docs/outputs_step5/`, fetched by
`scp` and md5-checked against the cluster copies.

### `es` — the full nine-point grid

| T | H_gen | dH | at-home MAE pp | ACT TVD pp | parse | term | sum=1440 |
|---|---|---|---|---|---|---|---|
| 0.50 | 2.8394 | -0.7486 | 10.694 | 27.696 | 1.000 | 0.262 | 0.060 |
| 0.60 | 2.8719 | -0.7161 | 9.157 | 19.982 | 1.000 | 0.500 | 0.080 |
| **0.70** | 3.0826 | -0.5054 | **7.580** | 13.502 | 1.000 | 0.852 | 0.103 |
| 0.80 | 3.2777 | -0.3103 | 8.353 | 10.976 | 1.000 | 0.987 | 0.108 |
| 0.90 | 3.3716 | -0.2164 | 9.007 | 12.204 | 1.000 | 0.998 | 0.102 |
| 1.00 | 3.4718 | -0.1162 | 9.227 | 10.626 | 1.000 | 1.000 | 0.120 |
| 1.10 | 3.5058 | -0.0822 | 9.387 | 11.167 | 1.000 | 1.000 | 0.088 |
| 1.20 | 3.5345 | -0.0535 | 9.042 | 11.303 | 1.000 | 1.000 | 0.100 |
| **1.30** | 3.5892 | **+0.0012** | 9.400 | 11.477 | 1.000 | 1.000 | 0.097 |

`H_real` = 3.5880 over 136 activity codes. **`T_entropy` = 1.30 · `T_fidelity` = 0.70 ·
`agree` = FALSE (six grid steps apart) · `T_chosen` = 1.30 on entropy matching ·
🔴 `endpoint_entropy` = TRUE.**

🔴 **`dH` crosses zero AT the top of the grid.** It is still -0.0535 at 1.20 and +0.0012 at 1.30, so
the crossing is genuine and interior to the interval (1.20, 1.30] — but the argmin lands **on the
last grid point**. The pre-registered rule applies as written: **reported as an endpoint, the grid
is NOT extended to chase it.**

### `it` — the full nine-point grid

| T | H_gen | dH | at-home MAE pp | ACT TVD pp | parse | term | sum=1440 |
|---|---|---|---|---|---|---|---|
| 0.50 | 2.3753 | -1.1454 | 8.083 | 26.712 | 1.000 | 0.637 | 0.078 |
| 0.60 | 2.4856 | -1.0350 | 7.685 | 21.918 | 1.000 | 0.743 | 0.050 |
| 0.70 | 2.6290 | -0.8916 | 5.116 | 17.611 | 1.000 | 0.888 | 0.050 |
| **0.80** | 2.8902 | -0.6304 | **3.933** | 13.139 | 1.000 | 0.967 | 0.100 |
| 0.90 | 3.1472 | -0.3735 | 4.397 | 10.024 | 1.000 | 1.000 | 0.067 |
| 1.00 | 3.3097 | -0.2110 | 6.344 | 8.743 | 1.000 | 1.000 | 0.072 |
| 1.10 | 3.4276 | -0.0931 | 10.759 | 9.333 | 1.000 | 1.000 | 0.063 |
| **1.20** | 3.5258 | **+0.0052** | 13.124 | 10.743 | 1.000 | 1.000 | 0.062 |
| 1.30 | 3.5911 | +0.0704 | 16.889 | 12.890 | 1.000 | 0.998 | 0.068 |

`H_real` = 3.5207 over 122 activity codes. **`T_entropy` = 1.20 · `T_fidelity` = 0.80 ·
`agree` = FALSE (four grid steps apart) · `T_chosen` = 1.20 on entropy matching ·
neither endpoint.**

### 🔴 THE PICTURE THE THREE FOLDS GIVE, WHICH `uk` ALONE DID NOT

| fold | `T_entropy` | `T_fidelity` | gap | `agree` | `T_chosen` | endpoint |
|---|---|---|---|---|---|---|
| `es` | **1.30** | 0.70 | **0.60** | 🔴 **false** | 1.30 | 🔴 **upper endpoint** |
| `uk` | 1.10 | 1.00 | 0.10 | 🟢 true | 1.10 | no |
| `it` | **1.20** | 0.80 | **0.40** | 🔴 **false** | 1.20 | no |

**Two folds of three disagree, and the one that agrees is the one that had already been read.** The
pre-registered rule — *on disagreement, entropy wins* — fired on all three and was written before
any of this was seen; nothing here re-opens it. But the sweep's headline changes: this is **not**
"the two criteria agree", it is **"they agree on one fold of three, and the chosen temperature
rests on entropy matching alone on the other two"**. That sentence is what goes in the paper.

⚪ The three chosen values are 1.30 / 1.10 / 1.20 — all above 1.0, all within one grid step of each
other, and every fold is **under-dispersed** relative to its own real reference below its chosen
point. The per-fold calibration was not a formality: `es` needs the top of the grid and `uk` does
not.

### 🔴 `FINDING 67` REPLICATES ON ALL THREE FOLDS

Computed from the three JSONs, not quoted. Spearman with average ranks for ties:

| fold | all 9 points, rho(MAE, `sum=1440`) | rho(MAE, `term`) | 🔴 limb where `term` = 1.000 | rho(MAE, `sum=1440`) on that limb |
|---|---|---|---|---|
| `es` | -0.633 | +0.131 | T >= 1.00, n = 4 | **-0.600** |
| `uk` | -0.435 | -0.639 | T >= 0.90, n = 5 | **-0.900** |
| `it` | -0.201 | +0.113 | T >= 0.90, n = 4 | **-0.800** |

**On the limb where every diary terminates — the one competing explanation held fixed by the data
itself — the at-home MAE tracks the 1440 fraction negatively on all three folds.** `es` and `it`
add something `uk` could not: on those two, rho(MAE, `term`) over the whole grid is **positive**, so
termination is not even a candidate explanation there, and the association survives anyway.

⚪ One number is corrected rather than silently changed: the earlier entry reported `uk`'s
all-nine-point rho as **-0.483**; recomputed with average ranks for the tie at `sum=1440` = 0.092 it
is **-0.435**. The falling-limb **-0.900** is unchanged and it is the one the argument rests on.

### 🟢 `D-S5-14`(a) IS APPLIED — and its perturbation was SEEN felling it

`tools/4thJ_step5_temperature.py`, backup at `.bak_ds514`. `at_home_profile()` now returns four
things instead of two: `profile` (**the existing curve, bit-identical**), `profile_covered` (each
slot divided by the diaries that actually reach it), `coverage_curve` (144 counts) and `n`. Three
values are emitted per grid point — `at_home_mae_pp` **unchanged**, `at_home_mae_pp_covered`
**new**, `coverage_curve` **new** — plus `T_fidelity_covered_REPORTED_NOT_USED`, which makes it
visible whether the confound moved the fidelity optimum **without letting it move the decision**.
`agree`, `T_chosen` and every endpoint flag still read the pre-registered quantities.

**Selftest: `tools/4thJ_step5_athome_selftest.py`, 14 checks, all green.**

| # | Check | Result |
|---|---|---|
| 1 | legacy profile identical to a **verbatim copy of the old function**, all 144 slots | max abs diff **0.000e+00** |
| 2 | on full-day diaries the two bases coincide and coverage is flat at n | exact |
| 3 | 🔴 **PERTURBATION — diaries truncated at 1000 min** | coverage **10 -> 0 exactly at slot 100**; covered profile `None` after slot 100 |
| 3 | 🔴 **the two MAEs diverge** | legacy **25.000 pp** vs covered **0.000 pp**, gap **25.000 pp** |
| 4 | the same perturbation against the OLD function | one number, contaminated, **no coverage curve to inspect** |
| 5 | degenerate inputs (unparseable, empty, non-multiple-of-10) | all rejected exactly as before |

The 25.000 pp is arithmetic, not a fit: the truncated tail is 44 slots of which 36 are at-home in
the reference, and 36/144 = 25.0 pp of **phantom absence**. The covered basis, comparing only the
100 slots both curves reach, reads **exactly zero** error — the correct answer, because on those
slots the two diaries are identical.

### 🔴 THE ONE THING `D-S5-14`(a) CANNOT DO RETROSPECTIVELY

**The 27 finished generation passes did not save their text.** `generate_at()` returns to memory, is
scored, and is discarded. So `at_home_mae_pp_covered` and the coverage curve **do not exist for any
point on the three grids above and cannot be recovered without generating again.** The fix is in and
proven; the clean numbers arrive on the next generation run and on no earlier one.

That is not a reason to run anything extra: **`D-S5-13`(a) already schedules further generation** (4
seeded replicates x 3 grid points x 3 folds). If it is ruled that way, the clean numbers come back
**inside work already planned, at no additional GPU cost.** It is recorded here because "apply
`I-1`, then re-derive the curves" reads as free and is not.

⚪ **One additional line is owed in that re-run and is proposed rather than assumed:** persist the
generated texts, one `.jsonl` per grid point. Every future statistic — the covered MAE, `G6.8`'s
dwell times, anything not yet thought of — then becomes re-derivable **without a GPU**. Not saving
them is the defect that made this section necessary.

### ⚪ Scope of `D-S5-14` — the open verification is now CLOSED

The previous entry recorded *"I did not trace whether `G4.1` and Step 6 call this exact function"*.
**Traced.** `at_home_profile` is defined **once**, in `4thJ_step5_temperature.py`, and called
**twice**, both in that file. `G4.1` uses `at_home_share()` at `4thJ_step4_train.py:150`, which is a
**per-diary scalar normalised by that diary's own total minutes** — a short diary is scored on its
own length and grows no phantom tail. So `G4.1` never had `FINDING 67`, option (a)'s scope claim
holds **by construction and not merely by ruling**, and option (b) would have had nothing to change.

### WHAT I DID NOT VERIFY, this entry

* **No covered-basis number exists for any real grid point.** Everything about the corrected
  statistic comes from the selftest's synthetic diaries. Its size on real generations is unknown.
* The `es` endpoint was **not** probed beyond the grid, deliberately — the pre-registered rule
  forbids extending it, and this entry does not ask to.
* `FINDING 65` (one realisation per grid point) and `FINDING 66` (generation unseeded) are
  **unchanged and still open**; `D-S5-13` is what rules them.
* The modified script has **not** been copied to Speed. The jobs are finished so the block is
  lifted, but the `scp` waits until `I-5`'s seeding is in — one push, not two.

### 🟢 `D-S5-13` RULED (a) AND SUBMITTED — 2026-08-21 (evening)

**Author ruling: (a)** — seeded replicates at a narrow window around each fold's `T_chosen`.
`I-5` (`FINDING 66`) was applied first, as the ruling requires: replicates that are not seeded are
not replicates.

| JobID | Fold | Window (all points already on the pre-registered grid) | Seeds | State |
|---|---|---|---|---|
| `1285712` | `es` | 1.10, 1.20, **1.30** | 101-105 | SUBMITTED |
| `1285713` | `uk` | 1.00, **1.10**, 1.20 | 101-105 | SUBMITTED |
| `1285714` | `it` | 1.10, **1.20**, 1.30 | 101-105 | SUBMITTED |

Script `tools/4thJ_step5_temperature_replicates.sh`; engine
`tools/4thJ_step5_temperature.py` md5 **`e37f98e1bba9b6bc6f0a806fb823d780`**, identical on the
desktop and on Speed (checked both ends). Output goes to
`temperature_calibration_<fold>_replicates.json` — the `--tag` exists so a replicate run **cannot
overwrite the primary artefact**.

🔴 **FIVE seeds, not four, and the reason is recorded rather than assumed.** The decision text said
"4 **further** replicates", on the reading that the primary realisation is the fifth. `FINDING 66`
disqualifies it: the primary sweep never called `torch.manual_seed`, so it carries no seed, cannot
be reproduced, and cannot be labelled one of five. Cost is not materially higher — the window is
three points, not nine, and the high-`T` points are the fast ones (~15 min each, so ~4 h/fold).

🔴 **`es`'s window and the endpoint.** `T_chosen` = 1.30 is the top of the grid and has no
neighbour above, so the window is the chosen point plus the two nearest available **on the existing
grid**. The grid is **not** extended past 1.30 — the pre-registered rule forbids chasing an
endpoint and this run does not.

🟢 **`I-5` applied.** `torch.manual_seed(s)` (and `cuda.manual_seed_all`) immediately before each
`generate_at`; `s` recorded per row as `gen_seed`; the JSON key `seed` renamed to **`prompt_seed`**,
which is all it ever scoped. The artefact now carries a `reproducibility_claim` string, and it says
what may be claimed: *"pinned base revision + pinned adapter + recorded sampling seeds"* — 🔴
**never "reproducible"**. `RP05` requires `batch_size=1`, deterministic algorithms,
`CUBLAS_WORKSPACE_CONFIG` **and a fixed GPU architecture**; Speed schedules across nodes, so
bit-reproducibility is not available to us and must not be written.

🟢 **Generations are persisted this time** (`--save-gen`, one `.jsonl` per (T, seed), ~6 MB/fold).
The first sweep discarded its text, which is the only reason `D-S5-14`'s covered-basis numbers
could not be re-derived for its 27 passes. Every future statistic is now re-derivable without a GPU.

🔴 **The replicate run does not choose.** In replicate mode the script refuses to recompute
`T_entropy`/`T_fidelity` — argmin over (T, seed) rows would pick the luckiest realisation at the
luckiest temperature, which is the move the pre-registered rule exists to prevent. It emits the
per-`T` spread (mean, sd, min, max, range across seeds) and the comparison `G5.8` actually asks
for: **max step between adjacent `T` vs max re-run range at fixed `T`**, with an explicit
`🔴 NOISE DOMINATES` verdict when the latter wins.

**Next on this item:** poll `1285712`/`1285713`/`1285714`; `scp` the three `_replicates.json` and
the generation directories back; read the spread block; then `outputs_step5/temperature_calibration.md`
and `generation_config.json`, and Step 5 closes.

---

## 🔴 `FINDING 70` — a THIRD criterion is already in the artefact, it is unanimous across all three folds, and it is registered nowhere

Found while assembling `outputs_step5/temperature_calibration.md` from the three primary artefacts,
before the `D-S5-13`(a) replicates landed. `4thJ_step5_temperature.py:489` computes `act_tvd_pp` —
the total variation distance between the generated and the real **activity-mix marginal**, in
percentage points — stores it on every grid row and prints it. **Nothing reads it.** `grep` of the
val doc, the impl ledger and the battery returns the three lines in the generator and nothing else.

Its argmin is **`T = 1.00` on all three folds**, and on each it is a **strict interior minimum**
with the curve rising on both sides:

| fold | `T_entropy` | `T_fidelity` | **`T_act_tvd`** | tvd at 0.90 | **at 1.00** | at 1.10 | registered criteria agree? |
|---|---|---|---|---|---|---|---|
| `es` | 1.30 *(endpoint)* | 0.70 | **1.00** | 12.204 | **10.626** | 11.167 | **no** |
| `uk` | 1.10 | 1.00 | **1.00** | 7.968 | **6.752** | 7.182 | yes |
| `it` | 1.20 | 0.80 | **1.00** | 10.024 | **8.743** | 9.333 | **no** |

**Why this is worth writing down.** The two *registered* criteria disagree on two folds of three, and
one of them (`es` entropy) lands on a **grid endpoint**. The unregistered third one is unanimous, is
interior everywhere, and picks out **`T = 1.00` — the untempered distribution**, i.e. no temperature
scaling at all. That is the expected behaviour of a well-calibrated fine-tune, and it is a reason to
read the other two criteria with suspicion rather than a reason to trust this one:

* the **entropy** criterion is a *per-episode* entropy, and `episodes_per_diary` falls monotonically
  with `T` on every fold (`es` 103.9 → 19.2). It is matching a quantity whose base changes along the
  very axis being swept.
* the **fidelity** criterion carries `FINDING 67` — `sum_1440_frac` never exceeds **0.135** anywhere
  on any grid, so the at-home MAE is dominated by the phantom-tail artefact that `D-S5-14`(a) was
  written to remove, and the removal cannot be applied retrospectively to these 27 passes.

🔴 **This does NOT re-choose the temperature, and must never be used to.** `act_tvd_pp` was **not
pre-registered** as a selection criterion; `G5.8` registers exactly two curves. Selecting on a
criterion **discovered after seeing its results** is the precise failure the val doc's own sentence
forbids — *"re-selecting on the same criterion with better statistics is still selecting on that
criterion"* — and choosing on a *new* criterion is strictly worse than that. It is **reported**,
alongside the two registered curves, and `T_chosen` stays exactly where the primary artefacts put it
(`es` 1.30, `uk` 1.10, `it` 1.20).

⚪ **What it changes, concretely:** one paragraph of the write-up, and the honest framing of the
temperature section — the knob the two registered criteria fight over is a knob the activity mix says
should not be turned at all. **Any future project that wants `act_tvd_pp` as a criterion must
register it before running the sweep.**

---

## 🔴 `FINDING 71` — the replicate windows exclude the fidelity optimum on `es` and `it`

Checked from `4thJ_step5_temperature_replicates.sh:58-60` and the three primary JSONs, not recalled:

| fold | `T_entropy` = `T_chosen` | `T_fidelity` | replicate window | `T_fidelity` in window? |
|---|---|---|---|---|
| `es` | 1.30 *(endpoint)* | **0.70** | 1.10 / 1.20 / 1.30 | 🔴 no |
| `uk` | 1.10 | **1.00** | 1.00 / 1.10 / 1.20 | ⚪ edge |
| `it` | 1.20 | **0.80** | 1.10 / 1.20 / 1.30 | 🔴 no |

The windows are exactly what `D-S5-13`(a) pre-registered — *narrow, around `T_chosen`* — and
`T_chosen` is the **entropy** optimum. They are not wrong. But they mean `at_home_mae_pp_covered`
will exist at **3 of 9 grid points**, and on two folds only on the far side of the legacy fidelity
optimum, so `D-S5-14`(a)'s coverage curve arrives as a stub and
`fidelity_argmin_moved_under_D_S5_14` is **not evaluable on `es` or `it`**. Replicate mode does not
emit that key at all (`4thJ_step5_temperature.py:612-625` sits in the `elif usable` branch a
replicate run skips) — correct, because a replicate run must not choose.

🔴 **Changes no temperature and blocks no gate.** `T_chosen` is frozen by the primary run;
`G5.8` reads the reporting obligation and the sensitivity clause, neither of which touches the
covered basis. Open as **`D-S5-15`** in `IMP/2026-08-21_review-derived-improvements.md`:
**(a)** one extra single-seed full-grid `--save-gen` pass per fold (≈ 2.5 h each, no new code) so
`FINDING 67` becomes measured on all three folds — **recommended**; or **(b)** declare the curve a
three-point stub and `FINDING 67` a `uk`-only measurement plus a code-path argument.

---

## 🟢 `D-S5-15` — RULED (a) BY THE AUTHOR, 2026-08-21 night, AND SUBMITTED

The author ruled option **(a)** with one refinement that halves the cost: rather than a fresh
nine-point full-grid pass, run **only the six grid points the replicate window does not already
cover**, at seed `101` only, and **splice** them with the seed-`101` rows of the replicate artefact.
Every row records its own `gen_seed` (`4thJ_step5_temperature.py:478`), which is what makes the
splice a single-seed curve rather than a mixture of realisations.

| fold | already at seed `101` (replicates) | submitted here | job |
|---|---|---|---|
| `es` | 1.10, 1.20, 1.30 | 0.50, 0.60, 0.70, 0.80, 0.90, 1.00 | `1285777` |
| `uk` | 1.00, 1.10, 1.20 | 0.50, 0.60, 0.70, 0.80, 0.90, 1.30 | `1285778` |
| `it` | 1.10, 1.20, 1.30 | 0.50, 0.60, 0.70, 0.80, 0.90, 1.00 | `1285779` |

Launcher `tools/4thJ_step5_temperature_coverage101.sh`, copied to
`/speed-scratch/o_iseri/`. Held identical to both earlier passes: prompt seed 42, `n_prompts` 600,
`top_p` 1.0, `top_k` 0, `max_new_tokens` 1200, same pinned base repo and revision
(`allenai/OLMo-2-0425-1B` @ `a1847dff35000b4271fa70afc5db10fd29fedbdf`), same per-fold LoRA adapter.
No grid point is invented — all six are on the pre-registered uniform grid. Generations are persisted
with `--save-gen` into the same `generations_<fold>/` directory, keyed `gen_<fold>_T<x>_s101.jsonl`,
so no collision with the window points is possible.

🔴 **It chooses nothing, and that is enforced rather than promised.** `--gen-seeds` puts the script in
replicate mode, whose only branch prints *"no temperature is chosen here"* and sets
`choice_deliberately_not_recomputed`. `T_chosen` stays 1.30 / 1.10 / 1.20 and `at_home_mae_pp` — the
statistic the choice was made on — is not recomputed for any existing point.

⚪ **Two consequences to carry into `outputs_step5/temperature_calibration.md`.**

1. The splice must be **declared**, per the author's directive: the covered-basis curve is nine points
   at one seed assembled from two jobs, not one job's output.
2. `fidelity_argmin_moved_under_D_S5_14` is emitted only in the non-replicate branch, so it is
   **derived offline** from the spliced rows and reported in that document. That is the quantity
   `FINDING 71` said would not be evaluable on `es` and `it`; after this run it is evaluable on all
   three folds, which is the whole point of the ruling.

🔴 **What the ruling does NOT do.** It does not re-open `T_chosen`; it does not touch
`at_home_mae_pp`; it does not block Step 5 from closing. Boxes 2 and 4 close on
`1285712`–`1285714`; this is additive and lands after them.
