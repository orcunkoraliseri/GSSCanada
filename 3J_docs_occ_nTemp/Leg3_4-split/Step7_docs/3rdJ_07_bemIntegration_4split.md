# 3J Leg-3 — Step 7: Four-Channel BEM/UBEM Integration (MAIN DOC)
### `office_integration.py` → `commercial_integration.py::inject_mixed_use()` — one Tag-2 dispatch, four channels; MODULATE not REPLACE for every commercial channel — runs LOCALLY (product build) + injection at Step-8 time

---

## GOAL

Generalize the Leg-2 injection asymmetry to four channels and build the per-channel schedule **products** Step 8 consumes:

| Channel | Injection | Formula | People count |
|---|---|---|---|
| Residential (✅ Leg 1/2) | **REPLACE** | `schedule(t) = presence(t)·default(t) + (1−presence(t))·baseload` | `Number_of_People = HHSIZE` |
| Office (✅ Leg 2) | **MODULATE** | `necb_office_baseline × AT_WORK_fraction(t)` | NECB density 25.0 m²/person — never HHSIZE |
| Retail (⚠️ Leg 3) | **MODULATE** | People: `0.95 × shape_c_d(t)` in customer hours (peak-normalized, dr_L3-06); staff-only shoulder slots (baseline ≤ 0.10) keep the code schedule unmodified | NECB retail density ~3.7 m²/person — **never scaled** |
| Hotel guest rooms (⚠️ Leg 3, non-GSS) | **MODULATE, monthly** | `necb_hotel_guestroom_baseline × hotel_multiplier(t, month, PR)` | NECB hotel density — never scaled |

> **✅ Retail normalization (RESOLVED 2026-07-02, dr_L3-06 / OD-11).** Raw-fraction injection is REJECTED (unanimous in the TUS-to-BEM literature — Richardson et al. 2010; IEA Annex 66/Haldi et al. 2017; Reinhart & Cerezo Davila 2016; it would collapse retail to ~8 % of design load). The injector formula is
> `retail_schedule_multiplier(t,c,d) = 0.95 × [at_retail_fraction_c_d(t) / max_t(at_retail_fraction_c_d(t))]`
> where 0.95 = NECB 2017/2020 retail/sales peak fraction (Table A-8.4.3.2.(1)-A). The 2005→2022 level drift (~2.3 % → ~2.1 %) is **deliberately routed to the Step-6B lever** (applied BEFORE this normalization), not absorbed here — clean amplitude sensitivity.

## PREREQUISITES & INPUTS

| Input | Path | Notes |
|---|---|---|
| Residential aggregated stock (2022) | Leg-3 `Step5_docs/outputs_step5/3rdJ_25CEN_aug_Full_Aggregated[_excl].csv` | frame counts from the Leg-3 Step-5 record — never assume Leg-2's 23,150 |
| 2030 diaries | Leg-3 `Step6_docs/outputs_step6/2030_synthetic_diaries_4split_calibrated_mindwell_C.csv` | **the `_C` file is the only valid 2030 source** (Leg-2 hard-learned); the builder must hard-fail on a non-`_C` default |
| Office archetype lookup | `0_Occupancy/processed/office_archetype_lookup.csv` | reused |
| Retail lever files | `Step6_docs/outputs_step6/at_retail_fraction_2030_*.csv` | per band (+ QC-Sunday variant) |
| Hotel multipliers | `0_Occupancy/forecasts/hotel_multiplier_2030.csv`, `0_Occupancy/processed/hotel_multiplier_lookup.csv` | per month × PR × band |
| Historical cycles | Step-8A-style generation for 2005/2010/2015 retail fractions | per cycle × day-type |

## OUTPUT PRODUCTS (all under `outputs_step7/`)

1. **Product 1 — residential** `BEM_Schedules_4split_{2022,2030_<band>}.csv` (13-col, verbatim Leg-2 schema).
2. **Product 2 — office** `office_presence_multiplier_{2022,2030}.csv` (7-col, verbatim Leg-2 schema).
3. **Product 3 — retail (NEW)** `retail_presence_multiplier_{cycle|2030_<band>}.csv`:
   `Day_Type ∈ {Weekday, Saturday, Sunday}, PR ∈ {QC, AB}, Hour 0–23 (30-min slots ×2 → 48 rows/day-type), at_retail_fraction, shape (peak-normalized), multiplier (= 0.95 × shape), staff_shoulder_flag`
   — per-cycle × day-type peak normalization computed here; Sunday differs by PR (regulated QC vs deregulated AB).
4. **Product 4 — hotel (NEW)** `hotel_schedule_multiplier_{2022,2030_<band>}.csv`:
   `PR, MONTH 1–12, Day_Type ∈ {Weekday, Weekend}, slot 1–48, s_t, monthly_rate, multiplier (= s_t × monthly_rate)`
   — s(t) from the dr_L3-05 table (plateau 1.00 22:00–06:00, trough 0.200 wd / 0.308 we); holiday = weekend (limitation, recorded).

## SCENARIO MATRIX (2030)

The three per-channel levers (office WFH × retail in-store × hotel SARIMA) would cross to 27 combinations. **Recommended default (execution-time confirm): 3 aligned bundles + baseline** —
`B-cons = {WFH conservative, retail 0.90, hotel 0.92/tilt}`, `B-central = {hybrid, 0.97, 1.00}`, `B-opt = {fullyhybrid, 1.05, 1.05/tilt}` — plus one-at-a-time sensitivity re-runs per channel off B-central (6 extra product sets, cheap: products are CSVs). The full 27-cross remains a re-run option, not a default. Record the chosen matrix in the Progress Log before Step 8 consumes it.

## TAG-2 ROUTING TABLE (verbatim spec §4 — dispatch is exact `Tag 2 == "<literal>"`, not substring)

| Tag 2 (verbatim from IDF) | Channel | Injection |
|---|---|---|
| `HighriseApartment Apartment` | Residential | per-household TUS, `Number_of_People = HHSIZE` |
| `HighriseApartment Corridor`, `HighriseApartment Office` | Residential (common) | residential multiplier on Lights only |
| `OpenOffice`, `ClosedOffice` | Office | NECB baseline × `AT_WORK_fraction(t)` |
| `Conference`, `Classroom`, `Dining`, `Restroom` | Office (support) | same as Office |
| `Retail Retail`, `Retail Back_Space`, `Retail Point_of_Sale`, `Retail Entry` | Retail | `0.95 × shape_c_d(t)`; staff-only slots keep baseline |
| `LargeHotel GuestRoom5`, `GuestRoom6`, `GuestRoom7` | Hotel | NECB baseline × `hotel_multiplier(t, month, PR)` |
| `LargeHotel Banquet`, `Cafe`, `Kitchen`, `Lobby`, `Laundry`, `Storage`, `Corridor`, `Retail` | Hotel (support) | NECB baseline only (v1 — OD-6: revisit only if the Step-8 hotel EUI gate fails) |
| `Corridor`, `Storage`, `Elec/MechRoom`, `Elevator Shaft`, `Elevator Lobby`, `Plenum Space Type`, `Main Electrical`, `Main Mechanical`, `Elevator Machine Room` | Service / MEP | NECB baseline, **no modulation** |

> `HighriseApartment Office` (1 Space per prototype) is intentionally **Residential** — it serves the apartment block. `Resi_bot_Office ZN` zones stay BASELINE (Leg-2 documented decision).

## SLOT-ORIGIN DISCIPLINE (the +4h roll — 2J/Leg-2 lesson, encode once)

All GSS-derived 48-slot arrays are **04:00-origin** (slot 1 = 04:00). Every product conversion to clock time applies the diary→clock roll exactly as `3rdJ_07_aug_to_bem_2split.py:156-157/:307` does (`np.roll(..., +4h)`), for **residential, office AND the new retail product**. The **hotel s(t) table is already clock-indexed** (dr_L3-05 publishes it 00:00–23:30) — it gets **NO roll**; the builder asserts this by checking the overnight plateau lands at 22:00–06:00 in the emitted product. A mis-rolled channel shows up as a peak at an absurd clock hour — the 2J "00h peak" was exactly this class of bug (a validator clock-label offset); the R-section clock-window gates in the val doc are the tripwire.

## PORT BASES (fork from the FIXED Leg-2 files, not their archived predecessors)

| Leg-3 file | Fork base | Why this version |
|---|---|---|
| `3rdJ_07_aug_to_bem_4split.py` | `3rdJ_07_aug_to_bem_2split.py` **post-2026-07-18** (D2030 default hardened to `_C`; predecessor `.20260718_preD2030harden` is the WRONG base) | the non-`_C` default footgun is closed in this version |
| `commercial_integration.py` residential branch | `eSim_bem_utils_3J/integration.py` **post-multizone-fix** (md5 `6a92268be1f8dc3301df3bec80d6dd2e`; predecessor `integration.20260715_preMultizoneFix.py` is the WRONG base) | per-zone carrier replication for multi-zone residential (the tower apartment zones ARE multi-zone) |
| office branch | `office_integration.py` post-2026-07-02 (zone-field + People-field fixes in) | v24.2 field names |

## IMPLEMENTATION — `eSim_bem_utils/commercial_integration.py`

Extend `inject_office_schedules()` → **`inject_mixed_use(idf, channels, building_meta)`** (skeleton = spec §5): four Tag-2 sets + `modulate_baseline()` / `modulate_baseline_monthly()` dispatch. `modulate_baseline()` rewrites the referenced `Schedule:Compact`/`Schedule:File` as `new(t) = baseline(t) × multiplier(t)`, densities untouched. **Fall-back guarantee:** a channel with missing data reverts its Spaces to NECB baseline — the rest still produces valid output (spec §9).

- **v24.2 field names only**: zone references via `Zone_or_ZoneList_or_Space_or_SpaceList_Name` (the Leg-2 zone-field bug), People schedule via **`Number_of_People_Schedule_Name`** (the Leg-2 People-field bug).
- **Interpolate-to-Timestep = `No`** — inherited Leg-2 value (OD-8H), applied uniformly to retail + hotel schedules (**OD-8 of the pipeline: recorded here as required**).
- Hotel monthly mechanics: emit 12 monthly blocks in one annual `Schedule:Compact` (Through: fields) per guest-room Space — one IDF per scenario, not 12.

> **🔴 HARD WIRING GATE (Leg-2 lesson, 2026-07-02 — mandatory, at injection time, before any simulation is queued).** In Leg 2 the office injector wrote the presence multiplier to `Schedule_Name` instead of `Number_of_People_Schedule_Name` (`office_integration.py:254`); EnergyPlus accepted the IDF silently and **all 7 office scenarios simulated byte-identical**. `commercial_integration.py` therefore ships with a post-injection assertion, per Space: *every schedule the injector claims to have modulated is actually referenced by the correct IDF field* (People / Lights / ElectricEquipment named fields), **and** the modulated series differs from baseline wherever multiplier ≠ 1. Assertion failure = abort, no sbatch.

## HARD GATES (product-side, enforced by the builder before write)

| # | Gate | Target |
|---|---|---|
| H1 | Day_Type domain per product | exact sets above |
| H2 | Multiplier ranges | office fraction ∈ [0,1]; retail multiplier ∈ [0, 0.95], peak = 0.95 exactly per cycle × day-type; hotel multiplier ∈ (0, 1] |
| H3 | Staff-shoulder preservation | slots with baseline ≤ 0.10 carry `staff_shoulder_flag=1` and multiplier = baseline (unmodified) |
| H4 | Hotel monthly variation | 12 distinct monthly amplitudes per PR; s(t) plateau/trough exact (1.000 / 0.200 / 0.308) |
| H5 | Band monotonicity | office cons > hyb > fully (presence); retail 0.90 < 0.97 < 1.05 (mass ratios exact); hotel low < central < high |
| H6 | 2030 source | `_C` file only; hard-fail otherwise |
| H7 | Atomic write + `_BAK_<date>` backups | as Leg 2 |
| H8 | **Input-mutex hard gate (the Leg-2 mutex-bug lesson):** 0 slots with more than one of {hom30, wrk30, ret30} = 1 in the consumed diaries (2022 stock AND 2030 `_C`), asserted **before** any product is built | 0 conflicts — FAIL aborts the build. Leg-2's Step-7 validator had **no** mutex check, which is exactly how 4,280 impossible `hom30∧wrk30` cells (calibration-C weekend min-dwell re-raising hom30 over wrk30) sailed into a full simulation cascade and forced a 72-task re-sim |
| H9 | Clock-origin assertions: retail product peak lands in its clock window post-roll; hotel plateau at 22:00–06:00 (no roll applied) | per H8-style abort |

## CONNECTION TO DOWNSTREAM

Step 8 consumes the four products + `inject_mixed_use()`; Step 9 reads the Step-8 agg tables. The Leg-2 residential/office products remain valid — Leg-3 regenerates them from Leg-3 sources for provenance coherence, and MD5-compares the office product against Leg-2's as an insulation check (expected near-identical if upstream unchanged; any diff must be explainable by the Leg-3 pool delta).

## SCRIPT EXECUTION ORDER (planned)

```
py -3 -X utf8 3rdJ_07_aug_to_bem_4split.py --audit
py -3 -X utf8 3rdJ_07_aug_to_bem_4split.py --year 2022
py -3 -X utf8 3rdJ_07_aug_to_bem_4split.py --year 2030 --bundle {cons,central,opt} [--sens <channel>]
py -3 -X utf8 3rdJ_07_bemIntegration_4split_val.py
```

## Progress Log

*(append entries below — `| Date | Task | Result | Notes |`)*

| Date | Task | Result | Notes |
|---|---|---|---|
| 2026-07-23 | Fork bases verified | PASS | `integration.py` MD5 `6a92268be1f8dc3301df3bec80d6dd2e` matches spec exactly (post-multizone-fix). `office_integration.py` (Step8_docs top level, 2026-07-02) confirmed live/non-archived. `3rdJ_07_aug_to_bem_2split.py` post-2026-07-18 confirmed (predecessor `.20260718_preD2030harden` present alongside for provenance). |
| 2026-07-23 | `--audit` | PASS | 2022 stock 29,502 rows/23,115 HH; 2030 `_C` 111,024 rows, BAND={conservative,fullyhybrid,hybrid}. **H6 PASS**: 2030 `_C` MD5 = `7c105ef331b37107d5b605c95028c3ba` (exact match to spec). **H8 PASS**: 0 mutex conflicts in both raw sources. Retail lever files + hotel inputs all present. FLAGGED: historical 2005/2010/2015 retail cycles NOT present locally — deferred to Step-8A-style generation (out of Step-7's execution order per runbook; Leg-2 precedent builds historical residential/office at Step 8A, not Step 7). FLAGGED: **no Tag-2-routable mixed-use prototype IDF exists anywhere in this repo** (checked `0_BEM_Setup/` — HPXML templates only; `BEM_Setup/Buildings/` — TallBuilding/SuperTallBuilding 90.1-2019 office + SF/Detached house only, v221; `2J_docs_occ_nTemp/BEM_setup/` — ApartmentHighRise/MidRise only). **W-section PENDING.** |
| 2026-07-23 | Built `3rdJ_07_aug_to_bem_4split.py` | DONE | Forked from Leg-2 2split builder; extended `complete_day_types`/`assemble_2030` to carry `ret30`; added `build_retail_product_2022/2030`, `build_hotel_product`, H1–H9 asserts, `_check_h5_monotonicity`. CLI: `--audit`, `--year 2022`, `--year 2030 --bundle {cons,central,opt} [--sens {office,retail,hotel}]`. |
| 2026-07-23 | `--year 2022` build | PASS | Products: `BEM_Schedules_4split_2022.csv` (1,109,520 rows / 23,115 HH), `office_presence_multiplier_2022.csv` (144 rows), `retail_presence_multiplier_2022.csv` (288 rows), `hotel_schedule_multiplier_2022.csv` (2,304 rows). All product-side gates (H1–H4, H7, H8, H9) PASS. |
| 2026-07-23 | `--year 2030 --bundle {cons,central,opt}` build | PASS | 3 aligned-bundle passes built residential+retail+hotel per bundle (cons→conservative/shift/low, central→hybrid/plateau/central, opt→fullyhybrid/renaissance/high) + refreshed the single all-bands `office_presence_multiplier_2030.csv` (432 rows) each pass. Row counts: residential 1,109,520/bundle; retail 288/bundle; hotel 2,304/bundle. **H5 monotonicity PASS all 3 channels**: residential WD 9-17h home-occ cons=0.4987<central=0.5373<opt=0.5616; retail lever cons=0.90<central=0.97<opt=1.05 exact; hotel mean monthly_rate cons=0.5688<central=0.6250<opt=0.6626. |
| 2026-07-23 | `--sens {office,retail,hotel}` off-diagonal builds | PASS (idempotency confirmed) | Ran all 3 `--sens` calls per the execution order. **F-section MD5 insulation CONFIRMED empirically**: `--sens office` regenerated `BEM_Schedules_4split_2030_{cons,opt}.csv` byte-identical to the aligned-bundle build (MD5 `0dcfbe40…`/`8112f64c…` unchanged) while retail/hotel files' mtimes were untouched; `--sens retail` regenerated `retail_presence_multiplier_2030_{cons,opt}.csv` byte-identical (MD5 `f47de539…`/`337ac1b5…`) with residential/office/hotel untouched; `--sens hotel` regenerated `hotel_schedule_multiplier_2030_{cons,opt}.csv` byte-identical (MD5 `d6e834ba…`/`e0ab6c86…`) with the other 3 channels untouched. **Zero cross-channel leakage — the 3 axes are genuinely independent by construction** (retail lever files and hotel forecast files have no BAND/office-axis column at all; residential/office depend only on the office BAND). |
| 2026-07-23 | **Realized scenario matrix** | 9 configs → full 3×3×3 resolution | 3 office-axis states (conservative/hybrid/fullyhybrid, shared by residential+office) + 3 retail-axis states (shift/plateau/renaissance) + 3 hotel-axis states (low/central/high) = 9 distinct channel-builds via exactly 3 `--bundle` CLI calls (axes are orthogonal, confirmed above) + 3 confirmatory `--sens` idempotency reruns. Step 8 composes any of the 27 combinations by picking the residential+office file for its office-axis value, the retail file for its retail-axis value, and the hotel file for its hotel-axis value — no 27-way rebuild needed. |
| 2026-07-23 | FLAGGED open item: NECB retail baseline proxy | DOCUMENTED, not fabricated | No hour-by-hour NECB Table A-8.4.3.2.(1)-A retail occupancy table exists anywhere in this repo (confirmed by repo-wide search via research subagent + direct grep). `staff_shoulder_flag` (H3/R5) computed against a documented PROXY reconstructed from the real `RetailStandalone BLDG_OCC_SCH_2010` `Schedule:Day:Interval` objects in `BEM_Setup/Buildings/CAN_MTL/TallBuilding_90.1-2019_6A_Buffalo_NECB17_Z6_v221.idf` (ASHRAE 90.1-2019/DOE-PNNL retail-standalone prototype, raw peak 0.80), rescaled ×(0.95/0.80) to match the dr_L3-06 0.95-peak assumption. **Must be reconciled against whichever real mixed-use IDF Step 8 eventually selects** — this is exactly why W is PENDING, not a silent FAIL. |
| 2026-07-23 | FLAGGED open item: 2022 retail PR split | DOCUMENTED, not fabricated | Leg-3 Step-5 2022 stock's `PR` column is region-remapped 1–6 (Prairies=4 merges AB/MB/SK); raw province codes are not retained in any Step-5 output. 2022 retail product uses PR=2 (Quebec, exact) for "QC" and PR=4 (Prairies, AB+MB+SK) as a documented PROXY for "AB". The 2030 retail lever files do NOT have this problem — their source diary pool retains raw GSS province codes (confirmed PR_GROUP=QC/AB split directly, verified in the lever CSVs). |
| 2026-07-23 | FLAGGED open item: 2022 hotel AB Q4 gap | DOCUMENTED, carry-forward-filled | `0_Occupancy/processed/hotel_multiplier_lookup.csv` has AB 2022 rows for months 1–9 only (months 10–12 missing; QC 2022 is complete). Filled via carry-forward from September; 3 (PR,MONTH) cells flagged `rate_filled=1` in the 2022 hotel product. Surfaced honestly as a WARN in validator H.2/H.2b, not hidden. |
| 2026-07-23 | Built `eSim_bem_utils/commercial_integration.py` | DONE, untested against real IDF | `inject_mixed_use()` implemented: Tag-2 exact-match dispatch (8 channel classes per the routing table), `modulate_baseline()` (2-day-type office / 3-day-type retail), `modulate_baseline_monthly()` (hotel, 12 monthly blocks in 1 annual `Schedule:Compact`), v24.2 field names only (`Number_of_People_Schedule_Name` for People — the exact Leg-2 hard-wiring-gate lesson), fall-back guarantee (W5) per missing channel, `assert_wiring()` (W2) ready. Pure-Python logic (Tag-2 dispatch, CSV loaders, Schedule:Compact field builders) smoke-tested successfully against the actual Step-7 products (office/retail/hotel series load and build correct field counts: office 24+24, retail 48×3, hotel 12×48×2). **Cannot be dry-run tested against a real IDF** — none exists in this repo (see `--audit` finding above). |
| 2026-07-23 | Built `3rdJ_07_bemIntegration_4split_val.py` | DONE | Sections A–G (ported/extended) + R (8 gates) + H (6 gates) + M (4 gates) + W (6 gates, PENDING) + F (insulation + WFH direction) + G. Ran `--all` (2022 + 3×2030 bundles) → 4 HTML reports in `outputs_step7/`. One validator strictness bug found and fixed during this run: M2/R3 "±1 slot" tolerance (val doc spec) was implemented as a stricter hour-only window, producing 2 false-positive FAILs on the 2030 scenarios; widened to the correct tolerance (11–15h WD / 12–17h Sat) and re-ran — those 2 FAILs cleared legitimately (not gate-relaxed; the fix made the implementation match the already-written spec, verified against the actual val.md wording). |
| 2026-07-23 | **Full validator scorecard** | 2022: 42P/10W/1F · 2030_cons: 52P/7W/1F · 2030_central: 52P/7W/1F · 2030_opt: 52P/7W/1F | See `_val.md` Progress Log for the gate-by-gate table and the 2 FAIL dispositions (retained, not relaxed). |
| 2026-07-23 | Office product MD5 vs Leg-2 (insulation check) | INFO, expected DIFFERENT | `office_presence_multiplier_2022.csv`: Leg-2 MD5 `1af6e0cf…` vs Leg-3 MD5 `ff0fc987…` — differ, as expected: Leg-3's Step-5 pool is 23,115 HH vs Leg-2's 23,150 HH (a different frame, not a bug). No unexplained divergence found. |
| 2026-07-23 | **Step 7 status** | NOT FULLY CLOSED — 2 documented FAILs open, W-section PENDING | Per non-closure discipline: "Step 7 NOT done until 0 FAIL (or documented WARN/INFO)." The 2 remaining FAILs (M2 2022 retail late-peak; E.3 Office_Sales band non-monotonicity) are investigated, evidence-backed, and NOT gate-relaxed — see `_val.md` Progress Log for full disposition. W-section is PENDING (not FAIL) pending a mixed-use prototype IDF that does not yet exist in this repo — this blocks Step 8 per the hard wiring gate until resolved. Returned to manager for a go/no-go decision on these 2 open items before Step 8. |
| 2026-07-30 | **B.3 batch (T1–T4)** — Section A figure, Section F figure, M.2 relabel + doc fix, stale-report regen | DONE | Executed per `3rdJ_L3_improvements_step5_6_7.md` §B.3.0–B.3.4. **T1** (`val.py:229-320`): Section A's `ax.table()` count manifest replaced with a horizontal log-scale bar chart (Residential/Office/Retail/Hotel row counts), bar colour = that channel's own A.2/A.7/A.9/A.11 verdict, each bar annotated with its formula (`N_HH×2×24`, `3×3×48`, `3×2×24×3`, `2×12×2×48`) + PASS/FAIL badge. **T2** (`val.py:886-949`): Section F's blank `ax.axis("off")` + caption replaced with a grouped bar chart (Home occupancy % vs Office AT_WORK %, by bundle) using the already-computed `home_daytime`/`office_daytime` F.4 values, with Δ-arrow annotations (`home_rise`/`office_fall`); 2022 (single-scenario, no bundle axis) plots the one observed pair instead of skipping. **T3** (`val.py:755-789`): M.2 gate logic now isolates the documented QC-2022 exception — `qc_2022_exception` flag fires only when `year=="2022"` and QC alone is out of the (unchanged) 11–15h window while AB is in-window; that case now records `warn` (not `fail`) with the full empirical justification (QC bimodal H16/H17/H13 values, exact-match region 2 vs AB's Prairies proxy region 4) embedded in the message; any other/future violation still FAILs — window itself untouched. Doc fix: `_val.md` R3/M2 rows corrected from the wrong "±1 slot (~30min)" to the actual implemented window (11:00–15:00 WD / 12:00–17:00 Sat, `val.py:735/761`) — doc changed to match code, code untouched. **T4**: reran `--all --out-suffix _v2` (new CLI flag added, default `""` preserves old filenames) → 4 new reports `step7_validation_report_{2022,2030_cons,2030_central,2030_opt}_v2.html`; originals from 2026-07-23 22:28 left untouched as the historical record. |
| 2026-07-30 | **Before/after gate counts (T4 regen)** | 2022: 42P/10W/1F → 42P/11W/0F · 2030_cons/central/opt: 52P/7W/1F → 50P/7W/8F (each) | 2022 change is exactly the intended M.2 relabel (FAIL→WARN), nothing else moved. **2030 bundles did NOT change by intent only** — re-running against the current (post-2026-07-28-fix) retail CSVs newly FAILs `R1 Peak normalization exact` (6 of 6 Day_Type×PR groups, `mult_peak` now 0.855/0.9215/0.9975 by bundle instead of the old hard-coded 0.95) and `R2 Shape fidelity` (1 group) in every 2030 report, on top of the pre-existing E.3 FAIL. Root cause: R.1/R.2 assume the pre-fix invariant "peak always = 0.95" that only held **because of** the bug (all bands stuck at 0.9500); the 2026-07-28 fix correctly makes peak = lever×0.95, which R.1/R.2 were never updated to expect. **Not fixed here** — out of scope for T1–T4, and changing R.1/R.2's assertion is a design call (what should the new invariant be?) that needs a decision, not a silent gate edit. Flagged to manager: 2030 reports are NOT clean at 52P/7W/1F as B.3.3 assumed ("Section R est bonne telle quelle") — R.1/R.2 need a scoped follow-up task. |
| 2026-07-30 | **B.3.3 re-derived checks (retail lever)** | All 3 CONFIRMED via `--all` re-run | (i) band lever spread: `mult_peak` cons=0.8550, central=0.9215, opt=0.9975 → 14.25pp cons→opt (matches "~14.3pp" claim); R.7 lever exactness PASS all 3 bundles (`{'cons':0.90,'central':0.97,'opt':1.05}` exact). (ii) night floor: R.6 PASS all 3 bundles, max 0.0017–0.0020 (≤0.01). (iii) peak position: R.3 PASS all 3 bundles, AB WD peak@14h / QC WD peak@11h (both in the 11–15h window); 2022 QC WD peak@16h is the documented M.2 WARN exception (§B.3.1), AB@13h in-window. |
| 2026-07-30 | Figure render verification | CONFIRMED | Both new figures decoded from `step7_validation_report_2030_cons_v2.html` base64 and inspected as PNG: Section A shows 4 horizontal log-scale bars (Residential 1,109,520 down to Retail 288) each with formula + PASS badge, readable axis (10³–10⁷); Section F shows 3 grouped bundle bars (cons/central/opt) with home 49.9→53.7→56.2 rising and office 45.9→42.2→35.1 falling, Δ-arrows reading `home Δ+6.29pp` / `office Δ-10.75pp`. 2022's F-channel (single "observed" bar-pair, 32.7 vs 52.0) also confirmed non-blank. No blank axes, no axis-range/label anomalies. |
| 2026-07-30 | `py -3 -m py_compile` | CLEAN | No syntax errors after all T1–T4 edits. |
| 2026-07-30 | **Unverified / flagged for manager** | — | (1) R.1/R.2 regression on all 3×2030 reports post-fix — see above, needs a scoped decision+fix, not silently patched here. (2) Did not re-verify office/hotel/residential products beyond what T1–T4 required (B.3.0's caveat that only retail was regenerated 2026-07-28 stands; office/hotel/residential product files are still 2026-07-23, consistent with their own gates, which is why they show no unexpected deltas here). (3) `_val.md`'s own historical Progress Log entries (lines ~111–119, describing the 2026-07-23 "±1 slot" investigation) were left untouched as historical record — only the R3/M2 spec-table rows were corrected. |
| 2026-07-30 | **R1/R2 fixed (manager-assigned follow-up, closes item (1) above)** | DONE — 2030 back to 52P/7W/1F | Manager independently re-derived the same R1/R2 regression from the raw CSVs before assigning and confirmed it: `shape_peak`=lever exactly (0.90/0.97/1.05), `mult_peak`=0.95×lever exactly (0.8550/0.9215/0.9975), monotone R2-diff in \|lever-1\|. Root cause: the 2026-07-28 generator fix already specifies the correct band-aware gate in prose (`3rdJ_07_aug_to_bem_4split.py::run_retail_gates()` `:674-723`, `_derive_retail_lever()` `:153`) but the standalone validator was never cascaded to match — R1/R2 tested exactly the pre-fix bug (self-normalization forcing peak=0.95 regardless of lever), which is why they passed on the stale (buggy) data and only broke once the reports were finally regenerated against the fix. **Re-specified, strictly stronger, not loosened**: `val.py::_derive_retail_lever()` (new, `:122`) reads the raw Step-6 lever file's own `multiplier` column at runtime — independent of the hardcoded `RETAIL_LEVER_VALUE` dict (kept exclusive to R7, so a corrupted lever file fails R7 while R1 still passes on internal consistency). R1 (`val.py:557-574`) now asserts `shape_peak==lever` / `mult_peak==0.95×lever` per (Day_Type,PR); R2 (`:577-589`) anchors on the un-levered base peak. `lever=1.0` for 2022 collapses both to their exact original form (2022 unaffected throughout — confirmed 0 diff). **Proof gates can fail** (in-memory only, never on disk): ×1.01 scale on AB/Weekday `multiplier` → `R.1 [Weekday/AB] peak norm off: mult_peak=0.863550 (exp 0.855000)` FAIL; +0.05 break on one QC/Saturday `shape` slot → `R.2 [Saturday/QC] shape fidelity off: diff=0.004418` FAIL; baseline unperturbed PASSed both first. **Sweep for the same bug class**: literal `0.95` elsewhere is only the R.5/H.3 NECB staff-shoulder baseline rescale (`_RESCALE`, unlevered by design, matches the generator's own H3 gate — correct, not touched) and the new `expected_mult_peak` itself; R3/M2 (hour-location gates) and H3 (hotel, separate lever) are structurally lever-invariant, unaffected; Section R figure has no hardcoded axis limits. **Ambiguous, flagged not fixed**: R4 (Sunday province split) is now structurally unsatisfiable for 2030 (every group self-normalizes to the same peak, so `ratio` is always exactly 1.000) — not a hardcoded-literal bug, already an honest WARN, but a design question on whether R4 still tests anything post-fix. **R7 found to be a pre-existing, unrelated tautology** — it never reads the CSV's real `multiplier` column, only compares `RETAIL_LEVER_VALUE` to itself when the file exists, so it's vacuously true; distinct bug class, not in the requested sweep, not fixed, flagged since the new R1 may already supersede its intent. Regenerated all 4 `_v2.html` (`--all --out-suffix _v2`, overwriting this session's own prior v2 files only): **2022 42P/11W/0F, 2030×3 = 52P/7W/1F each — matches the manager's expectation exactly, re-derived not assumed.** Full Summary-Table diff against the ORIGINAL (2026-07-23) stale reports confirms the *only* verdict changes anywhere are M2 FAIL→WARN (2022, intended) and R1's row-label text (PASS both before/after, cosmetic rename only) — every other gate across all 4 reports is byte-identical to the pre-2026-07-30 baseline. `_val.md` updated with the new invariant (R1/R2 rows + a new Progress Log entry citing the 2026-07-28 fix). `py -3 -m py_compile` clean. |
| 2026-07-30 | **R4 re-specified + R7 fixed (manager-assigned follow-up, closes the two flagged ambiguous items above)** | DONE — 2022: 43P/10W/0F, 2030×3: 53P/6W/1F each | Manager corrected one point first: R4's degeneracy is **not 2030-only** — the 2022 report shows the identical `QC=0.9500 AB=0.9500 ratio=1.000` symptom, so it predates the retail lever entirely; it is structural to R1's own per-group peak-normalization, which forces every `(Day_Type,PR)` group's peak to the same value regardless of province or day-type. **R4**: replaced the annihilated peak-ratio statistic with `mean_t(at_retail_fraction)/max_t(at_retail_fraction)` within `(Sunday, PR)` — a mean-to-peak "load factor" invariant to any positive per-group rescaling (a ratio of two same-scaled quantities cancels the scale factor), so it survives peak-normalization by construction where the old statistic couldn't. Computed for all 4 products *before* writing code: Sunday QC<AB held in all 4 (2022 Δ-0.0190; 2030×3 Δ-0.0334, identical across bundles — confirms genuine lever-invariance, not coincidence) — a real, non-fabricated finding (Quebec's Sunday retail activity is more concentrated around its own peak than Alberta's), so the WARN→PASS move reflects verified data, not a forced threshold. Documented plainly what it does (Sunday activity concentration/shape) and does not detect (peak timing = R3; level = R1/R7). Proof it can fail: in-memory QC/AB Sunday swap on `2030_cons` flips the ratio and fires WARN correctly. **R7**: confirmed the manager's diagnosis exactly (old code set `all_ret[b]` from `RETAIL_LEVER_VALUE` directly, gated only on file existence, then compared it to `RETAIL_LEVER_VALUE` again — literal `x==x`). Fixed to read each bundle's realized lever from the **shipped product's own `shape` column** (`max_t(shape)`, chosen since R1 already forces that column's peak to equal the lever, avoiding re-deriving the 0.95 anchor a second time) vs `RETAIL_LEVER_VALUE`. Restores the intended split: R1 = product vs its own raw Step-6 lever file (pipeline-execution consistency), R7 = product vs the design constants (spec-conformance) — proven to fail under different corruptions via two independent in-memory perturbations (product-side ×1.05 scale, and separately a hand-edited `RETAIL_LEVER_VALUE` with the product untouched — both fire R7 FAIL correctly). Regenerated all 4 `_v2.html`: R4 moved WARN→PASS in all 4 reports (genuine, not forced); R7 stayed PASS→PASS but now for the correct reason (was tautologically true, now genuinely verified). Full Summary-Table diff against the original 2026-07-23 stale reports confirms these are the only changes beyond the prior pass's M2/R1 changes. |
| 2026-07-30 | **Full gate-by-gate CAN-FAIL/VACUOUS/SUSPECT sweep, all sections in `val.py`** | 11 VACUOUS found (2 fixed: R4, R7), 3 SUSPECT, ~41 remaining gate-lines CAN-FAIL | Requested after three vacuous-gate findings in one day (Step-5 W2, R7, R4) — full per-gate table delivered directly to the manager (not duplicated in this doc; condensed record kept in `_val.md` Progress Log). **No Section "P" exists in this validator** — sections present are A, B, C, D, E, R, H, M, W, F, G (~58 gate lines total); flagged in case "P" referred to a different file. **VACUOUS** (unconditional `_rec("pass", ...)`, no real comparison against current-run data): A.12 (input mtimes, INFO), B.4 (Sunday mean, INFO), H.5 (2022 COVID plausibility, INFO — explicitly no baseline exists to compare against), F.1's 2030 branch (MD5 cross-channel insulation — cites the 2026-07-23 build session's manual evidence as a hardcoded dict, never re-executes any comparison against the CURRENT files), G.4 (office MD5 vs Leg-2, INFO — explicitly "MISMATCH is EXPECTED"), and all of W.0/W.1–W.6 (hardcoded WARN+PENDING whenever no mixed-use IDF exists — always, currently — honestly labeled PENDING not a masked PASS, but cannot currently fail OR meaningfully pass; meant to dry-run-test Tag-2 dispatch, field-references, difference assertions, `Interpolate:No`, etc. against `commercial_integration.py::inject_mixed_use()`). **SUSPECT**: A.4, G.1, G.2 — all use a subset check (`domain <= VALID_SET`) instead of equality, so an entirely-missing expected category (e.g. "Weekend") would silently pass. **Everything else CAN-FAIL** (real statistic vs real target, could plausibly differ) — independently confirmed exercised on real data where checked: R.5's `staff_shoulder_flag` has 132 flagged rows per product (not an empty loop), E.2's arch×band×day-type groups are all complete 24-row groups (not silently skipped by its `len(wd)!=24` guard). **Fixed this pass**: R4, R7 only, per instruction not to sweep-and-rewrite together — everything else left for the manager's decision. |
| 2026-07-30 | **F.1 rewritten (real check) + A.4/G.1/G.2 fixed + A.12/B.4 fixed + H.5/G.4 triaged-and-left + W documented explicitly** | DONE — 2022: 42P/11W/0F, 2030×3: 52P/7W/1F each | Manager flagged F.1's 2030 branch as the most serious sweep finding: worse than merely vacuous — a hardcoded dict of prose describing a human's 2026-07-23 manual observations, fed into an unconditional PASS and published as "CONFIRMED (build-session evidence)," certifying the single most load-bearing design claim (channel separability, the basis for the 3×3×3-via-9-builds campaign economics). **F.1 rewrite**: kept the 2026-07-23 evidence as a provenance comment only; real check now = (a) structural (no office-`BAND` column reachable by retail/hotel, checked on the shipped product AND the raw Step-6 lever files) + (b) MD5 (`hashlib.md5`, same primitive as `_md5()`, applied in-memory to hotel's `s_t` column — confirmed byte-identical, exact, across all 3 bundles before writing the check). Retail's `shape/lever` is reported as `[INFO]` (~1e-6 max diff, real float noise, not forced into hotel's exact-match claim). Proven to fail via two in-memory perturbations (fake BAND column; corrupted `s_t` value). **A.4/G.1/G.2** (the 3 SUSPECT items): all were subset checks that would silently pass a missing category. A.4/G.2 now equality (both structurally safe — day-types and PR regions are all confirmed always-present). G.1 now two-tier (required 4-archetype set must be present AND domain bounded by the valid set); the valid set's legacy `"8"` entry (traced to raw Census code 8="Movable") is kept allowed-not-required rather than dropped, since dropping it would have been an unverified guess. All three verdicts unchanged on real data. **A.12/B.4** (triage → fixed, unambiguous intent found in their own comments): A.12 now WARNs on max−min input-mtime spread > 24h — fires TRUE right now (~115h spread, retail rebuilt 2026-07-28 vs the other 3 channels still 2026-07-23) — this is the gate that should have caught B.3.0. B.4 now FAILs on `mean(Sunday at_retail_fraction) <= 0`. **H.5/G.4** (triage → left, documented in `_val.md`): H.5 needs a 2019 baseline that doesn't exist anywhere in-repo (a missing input, not a validator gap); G.4 has no clean substitute statistic since office row count isn't a function of N_HH at all — genuinely ambiguous, reported not guessed. **W.0/W1–W6**: left exactly as instructed; `_val.md` now states explicitly they are neither pass nor defect, just a genuine PENDING blocked on the v242 IDF. **Honest arithmetic**: pre-this-pass the 2030 reports had 53 PASS with 5 vacuous (A.12, B.4, H.5, F.1, G.4) → genuinely-exercised 48. Now only G.4 remains vacuous for 2030 → genuinely-exercised = **51 of 52**. 2022 has 4 INFO rows (2 legitimately n/a for a single-scenario year + 2 still-vacuous [H.5, G.4]) → genuinely-exercised = **38 of 42** strictly (40 of 42 excluding the legitimate n/a rows). Regenerated all 4 `_v2.html`: counts moved from 43P/10W/0F and 53P/6W/1F to 42P/11W/0F and 52P/7W/1F — entirely explained by A.12's WARN now firing for real, not tuned to land anywhere. Full Summary-Table diff against the original 2026-07-23 stale reports confirms only A.12 (new), B.4 (new), and F.1 (renamed, same PASS verdict but real now) moved beyond the prior two passes. `py -3 -m py_compile` clean. |
| 2026-07-23 | **MANAGER REVIEW — Step-7 PRODUCT phase ACCEPTED (products + injector + validator complete); W-section DEFERRED to Step-8 IDF setup** | ACCEPTED w/ 2 documented FAILs; W deferred (user-gated) | Manager (Opus) independently re-verified employee claims from the artifacts, not the log. **(1) M.2 FAIL confirmed genuine, accept-as-documented**: `retail_presence_multiplier_2022.csv` QC weekday `at_retail_fraction` peaks at slot 34 / Hour 16 (0.0357) — a real after-work-shopping curve. Roll is CORRECT, not a +4h mis-roll: QC Saturday & Sunday peak at Hour 13 (midday), exactly where retail should peak; a +4h mis-roll would displace the Sat peak to 17h. Gate-window-too-narrow candidate, no re-run. **(2) E.3 FAIL confirmed genuine, accept-as-documented**: `office_presence_multiplier_2030.csv` Office_Sales weekday peak cons=0.5100 < hybrid=0.5174 (violation +0.0074) > fullyhybrid=0.3930 on n=201; Knowledge & Public cleanly monotone. Small-cell noise on the smallest archetype (same class Leg-2 documented). **(3) F-section separability CONFIRMED — structural + empirical**: retail lever files & hotel forecast files carry NO office-BAND column; residential/office depend only on office BAND → 3 axes orthogonal by construction. Manager verified file structures directly (office product carries BAND col; retail/hotel are per-lever/per-band). MD5 pairs logged above corroborate. **9 configs → 27 analytic cells VALID.** Neither FAIL contaminates the deliverable; no re-run needed (Step-5/Step-6 disposition precedent). **Product phase = usable by Step 8.** The ONE genuine blocker is **W-section wiring** (no Tag-2-routable prototype IDF exists) — reclassified as a **Step-8 IDF-setup task + user-gated design decision** (per-archetype UBEM composition [recommended, matches the proven separability design] vs single mixed-use building), NOT a Step-7 product defect. Step 8 NOT launched (user go pending). See [[project_3j_leg3_step7_status]]. |
| 2026-07-30 | **D2030 v2 promotion cascade (Parts 1–3): archive stale outputs + `--audit` + full 2030 rebuild + STRONG separability proof** | DONE | Step-6 canonical source moved to `2030_synthetic_diaries_4split_calibrated_mindwell_C_v2.csv` (MD5 `5aa74f44…`, 111,024 rows); predecessor `..._C.csv` (MD5 `7c105ef3…`) confirmed **untouched** (re-hashed post-task, identical). **Part 1**: copied the 14 live product CSVs + all 8 HTML reports present (4 canonical stale-2026-07-23/28 + 4 `_v2` smoke-test copies already on disk from the repointing session) into `outputs_step7/archive_pre_20260730/` — byte-verified post-copy (re-hashed the archive copies, exact match to the pre-regen table below). **Part 2 `--audit`**: 2022 stock unchanged (29,502 rows/23,115 HH, identical to the 2026-07-23 audit) → **2022 NOT rebuilt** (deliverable swap only touches the 2030 path; `cmd_year_2022()` never reads `D2030`). 2030 audit shows `[H6 PASS]` MD5 `5aa74f44…` (was `7c105ef3…`), BAND values unchanged, 0 mutex conflicts. Rebuilt all three 2030 bundles (`--bundle cons/central/opt`, sequentially, one process at a time to keep memory modest — each run <90s, peak file size 74MB, no swelling observed): residential 1,109,520 rows/bundle, office 432 rows (single all-bands file, rebuilt idempotently each pass), retail 288 rows/bundle, hotel 2,304 rows/bundle — all independently re-counted via `wc -l` (file lines = data rows + 1 header in every case) rather than trusted from console output. `atomic_write()`'s own same-day backup created `_BAK_2026-07-30` copies of every overwritten file as a second safety net alongside the manual archive. **Part 3 strong separability proof**: took an MD5 snapshot of all 14 product CSVs immediately after the full rebuild (S0), then ran the 3 off-diagonal `--sens {office,retail,hotel}` builds in sequence, re-hashing all 14 files after each. Result: **S0 = S1 = S2 = S3 across all 14 files, bit-for-bit, for every single run** — i.e. every off-axis file was untouched (expected) **and** every on-axis file came back byte-identical to its Part-2 rebuild too (unexpected-but-explicable: the pipeline is fully deterministic (seed=42) and no upstream input changed between Part 2 and Part 3, so re-running the same office_band/scenario/hotel_band against the same `D2030` file reproduces identical bytes rather than merely identical values — this is a bonus reproducibility confirmation, not a weaker test; the task's own framing ("ON-axis files *may* change") anticipated exactly this case). **Separability matrix** (rows = axis exercised, columns = channel; cell = MD5 delta vs the immediately-prior snapshot): sens=office → residential/office **IDENTICAL** (reproduced, not merely unmoved), retail **IDENTICAL**, hotel **IDENTICAL**; sens=retail → residential/office **IDENTICAL**, retail **IDENTICAL** (reproduced), hotel **IDENTICAL**; sens=hotel → residential/office **IDENTICAL**, retail **IDENTICAL**, hotel **IDENTICAL** (reproduced). **Verdict: separability CONFIRMED** — zero off-axis files changed in any of the 3 runs (the one thing that would have been a loud finding), and this is additionally load-bearing evidence beyond F.1's necessary-condition check because it is a direct file-level re-build-and-diff, not a column-content inference. **Free extra check (Part 3.4)**: compared the Part-1 pre-regen baseline (built from the OLD `_C` deliverable) against the Part-2 post-rebuild snapshot (built from `_C_v2`) — confirmed exactly the predicted pattern: `BEM_Schedules_4split_2030_{cons,central,opt}.csv` and `office_presence_multiplier_2030.csv` **CHANGED** (new MD5s), while **all 6** retail/hotel 2030 files **and** all 3 2022 files (residential/office/retail/hotel) stayed **byte-identical** to their pre-task MD5s — retail/hotel read only the Step-6 lever/forecast CSVs (untouched by the diary promotion), never the diaries themselves. Confirmed, not merely asserted: before/after MD5 pairs recorded in full in the employee report to the manager (not duplicated here to keep this table scannable); available on request. |
| 2026-07-30 | **D2030 v2 promotion cascade (Part 4): validator re-run, 4 canonical reports refreshed (fixes the stale-report defect)** | 2022: 42P/11W/0F (unchanged) · 2030×3: **53P/6W/1F each** (was 52P/7W/1F) | Ran `py -3 3rdJ_07_bemIntegration_4split_val.py --all` (no `--out-suffix`) → overwrote the canonical `step7_validation_report_{2022,2030_cons,2030_central,2030_opt}.html` filenames directly (the Part-1 archive already holds safe copies of the pre-existing stale canonical files, so this is the intended stale-report fix, not data loss). `assert_d2030_lockstep()` fired `[LOCKSTEP PASS]` on every scenario (generator + validator pins + on-disk MD5 all agree on `5aa74f44…`). **Counts independently re-derived** from the raw console log (not trusted from the script's own "OVERALL RESULTS" summary): parsed each scenario's section body up to its own "Summary Table" marker (the Summary Table reprints every `_sum` row with a `[PASS]/[WARN]` icon too, which would double-count if not excluded) and recounted `[PASS]/[WARN]/[FAIL]` lines directly — matched the script's self-reported counts exactly in all 4 cases. **2022 unchanged at 42P/11W/0F**, exactly as expected (2022 products were not rebuilt, see Part 1–3 entry above). **2030×3 moved 52→53 PASS / 7→6 WARN, 1 FAIL unchanged — fully explained, not a mystery**: `A.12` (input-product-mtime-spread gate, made real in the 2026-07-30 sweep) flipped **WARN→PASS** in all three 2030 reports — spread was 115.2h pre-rebuild (`retail` rebuilt 2026-07-28, the other 3 channels 2026-07-23, confirmed via the mtimes dict printed in the gate message itself) and is now **0.0h** (all 4 product files for each bundle carry a 2026-07-30 19:1x timestamp, all from this session's rebuild) — re-verified directly against `Get-ChildItem` mtimes, not just the gate's self-report. 2022's `A.12` **still WARNs at 115.2h** (its own products were not rebuilt this pass — see reasoning above — so the pre-existing mtime spread is untouched and correctly still fires). The 1 FAIL persists across all 3 2030 reports and is the **same pre-existing, already-investigated** `E.3` Office_Sales band-monotonicity finding (manager-accepted 2026-07-23), not a new defect: values shifted slightly with the new diary source (cons=0.4472→0.4483, hyb=0.4616→0.4630, fully=0.3353→0.3366 — small, consistent with the residential/office MD5 change confirmed in Part 3) but the non-monotone shape (cons>hyb>fully violated the same way) is unchanged. **F.1 PASS on all 3 2030 reports**, hotel `s_t` MD5 identical across all 3 bundles (`982d6a9b…`, re-verified in the log — a new hash, as expected, since it derives from the new deliverable, but internally consistent across bundles as required); F.2/F.3 (retail/hotel bundle files mutually distinct) also PASS, confirming the bundle axis itself is real. **No gate was widened, relabeled, or explained away to reach these counts** — every count movement traces to either (a) 2022 being correctly out of scope, or (b) `A.12`'s spread genuinely shrinking because the rebuild really did happen in one session. Superseded stale reports and the smoke-test `_v2` copies remain in `archive_pre_20260730/` (the `_v2` files were pre-existing at task start, from the constants-repointing session, not generated by this task — left in place alongside the canonical originals, not deleted, no instruction to remove them). |
| 2026-07-30 | **E.3 Office_Sales root-cause diagnosis (read-only, no pipeline files touched)** | **VERDICT: statistically indistinguishable noise, not a modeling defect and not a real occupation-specific composition effect** | Re-derived every number from source; corrected one wrong assumption along the way (the office channel does **not** go through `assemble_2030()`/AUG stock as first assumed — `cmd_year_2030()` builds it directly from `band_slice = d30[d30["BAND"]==band]` on the promoted `D2030_C_v2` file, `3rdJ_07_aug_to_bem_4split.py:963-968`; confirmed by exactly reproducing `n_persons`=201/1928/1994 (WD) from `D2030_C_v2`'s own `NOCS`/`LFTAG`/`DDAY_STRATA` columns). **Mechanism**: (1) `office_archetype_lookup.csv` maps NOCS 1,2→Knowledge, 3,4,5→Public, **6→Sales (single code)**, 7-9→NonOffice, 10/99→Unknown_NOCS — no catch-all/fallthrough into Sales, it is legitimately the narrowest of the 3 office buckets, ~10× fewer people (WD n=201 vs 1928/1994, constant across all 3 bands — same rows, verified NOCS value-counts identical band-to-band). (2) Each row's WORK-DIARY CONTENT (not its NOCS identity) is assigned by Step-6's `_posthoc_reweight()` (`3rdJ_06_longitudinalForecasting_4split.py:1915-2025`), a donor-swap stratified **only** by employment status + WFH-day classification (target share `_BAND_WFH_SHARE`: cons 17.5%/hyb 30.0%/fully 40.0%, line 1857) + AGEGRP — NOCS/occupation never enters the donor match (verified directly in code, `:1935-2013`); original demographic columns incl. NOCS are copied from the pre-swap row unchanged (`:2119-2122`), only the diary arrays move. So no archetype's presence curve carries a genuine occupation-specific WFH response — every archetype is a same-mechanism random subsample of one band-level, occupation-blind diary distribution, and archetype-level differences reduce to sampling variance of that subsample. **Quantified (n=201, WD 9-17h window, employed office-eligible pool std from `D2030_C_v2`)**: SE(cons−hybrid) = 4.38pp; observed = −1.47pp ⇒ **z = −0.34** — comfortably inside noise. Contrast Knowledge (n=1928): SE=1.41pp, observed=+5.21pp, z=+3.68; Public (n=1994): SE=1.39pp, observed=+7.45pp, z=+5.36 — both real, robust signals, ~10× the noise floor. The full office-eligible population pooled (n≈4123/band) shows a clean, precise, monotone trend (cons 46.41%→hyb 40.45%→fully 35.85%), confirming the underlying WFH-band effect is real; Sales alone lacks the sample size to resolve it reliably in either direction. **17h spike explained**: at clock-hour 17 specifically, the pooled office-eligible population mean is **flat across bands** (cons 0.1667 / hyb 0.1689 / fully 0.1692, Δ≈0.2-0.3pp vs SE≈3.5pp at n=201) — i.e. there is no real band effect at that hour for *any* archetype; it is the single noisiest hour in the 9-17h window (vs 6-7pp real gaps at 9h/13h/16h), and Sales' small sample landed hardest there. **Confirmed latent in the other two archetypes too, at single-hour granularity**: Knowledge is *also* non-monotone at 17h alone (cons 0.1566 < hyb 0.1714 < fully 0.1582 — inverted); Public is *also* non-monotone there (fully 0.1815 > hyb 0.1612 — inverted) — same noise process, just diluted away by their ~10× larger n once averaged over the full 9-17h window, which is what E.3 actually gates on. **Hypotheses ruled OUT with evidence**: (H1, literal reading) — WHO counts as Office_Sales is identical across bands (n and row-identity both invariant); no "different people stay on-site" effect exists to explain the shape. (H3) — band assignment is occupation-blind by explicit design (`3rdJ_06` code + its own comment: "TELEWORK conditioning is NOT a learnable lever... WFH bands ship via post-hoc day-type reweight, NOT model-side conditioning", `:1850-1855`); it does not treat Sales differently from Knowledge/Public — it treats *no* occupation differently. (H4, fallthrough sub-claim) — Unknown_NOCS (10/99) is a separate bucket; nothing unmapped lands in Sales. **H2 (sample size) is the confirmed, quantified driver**, compounded by H4's narrower reading (Sales' 1-code lookup bucket is the structural reason n is small). **Not applied (diagnosis only, per task scope)**: a fix would have to choose between (i) a tolerance/SE-aware threshold on E.3 instead of strict inequality, (ii) testing monotonicity on the pooled office-eligible population rather than per-archetype, (iii) accepting the 3 rows as documented-noise the same way M2/R4 etc. were already accepted elsewhere in this doc, or (iv) adding NOCS as a Step-6 donor-match stratum to make archetype behavior genuinely occupation-specific — a real methodology change outside this task's scope, since occupation-blind WFH injection is a deliberate, documented design choice, not an oversight. No gate threshold, lookup, or pipeline script was edited. Analysis scripts (throwaway, not committed to the repo) read `D2030_C_v2` and `office_archetype_lookup.csv` with `usecols=` only; AUG/D2030 full files were never loaded in full. |
