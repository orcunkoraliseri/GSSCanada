# EMPLOYEE PROMPT — Leg-3 · Step-4 REOPEN · DIAGNOSTIC (read-only) — localize the W3 colleague-co-presence 16:30 divergence

**You are the employee. Execute the task below and append a Progress Log entry on completion.** Runs LOCALLY on Windows — `py -3 -X utf8` for every Python call, no sbatch. This is a **READ-ONLY diagnostic**: do NOT edit any script, do NOT re-rake, do NOT retrain, do NOT re-run any pipeline stage, do NOT write to any pipeline output dir. Your only writes are (1) a scratch analysis script if you need one, and (2) a Progress Log entry. Stop and flag any decision-level question to the manager — decide nothing silently.

---

## Why you exist (context)

Step-5 (census linkage) flagged a validator FAIL **W3 = colleague co-presence per-slot max-deviation, SYN-vs-OBS**. Diagnostic 2 (already done, in `Step5_docs/3rdJ_05_censusLinkage_4split.md` Progress Log) proved:
- W3 in the Step-5 matched frame = **11.38 pp**, of which **~7.19 pp is ALREADY present in the raw Step-4 pool before Step-5 touches it** (P2 = raw pool statistic). So W3 has a **genuine upstream Step-4 component** — unlike gates 2.2/W1, which are pure Step-5 frame-composition artifacts and are NOT your concern here.
- Worst slot is identical in matched frame and raw pool: **weekday (WD) slot 26 = 16:30**, a real weekday-afternoon hour, not tail-slot noise.

The user chose to **reopen Step-4 and diagnose first** (not blind-retrain). Your job is to answer, from the raw pool + the Step-4 code, **one pivotal question**:

> **Is the ~7.19 pp W3 weekday-16:30 divergence something a Step-4 re-rake could plausibly fix, or is it an irreducible property (data thinness / a co-presence-generation artifact that raking never targets)?**

The manager's working hypothesis (to confirm or refute, not assume): **colleague co-presence is a DOWNSTREAM derived quantity generated by the co-presence machinery (`3rdJ_04C_pairs_4split.py` + `g3_copresence_thresholds.json`), and is NOT a rake margin** in either `3rdJ_04L_joint_rake_4split.py` or `3rdJ_04T_act_rake_4split.py`. If that's true, a re-rake cannot fix W3 and the correct lever (if any) is the co-presence generation, not raking.

---

## Inputs (all local; verify present before you start)

- **Raw Step-4 pool (locked, Step-5 input):** `3J_docs_occ_nTemp/Leg3_4-split/Step4_docs/outputs_step4/sweep/seed_3_raked3_mindwell_actv/augmented_diaries.csv` (192,183 rows). Contains an `IS_SYNTHETIC` flag distinguishing synthetic augmented diaries from real GSS observed rows. **Load only the columns you need with `usecols` — never pull the whole file into context; it is large.**
- **Step-4 code (read, do not edit):**
  - `3rdJ_04C_pairs_4split.py` — co-presence (G3) generation
  - `3rdJ_04L_joint_rake_4split.py` — joint rake
  - `3rdJ_04T_act_rake_4split.py` — activity rake
  - `3rdJ_04_augmentationGSS_4split.md` — runbook (raking design, co-presence design)
  - `outputs_step4/smoke_test_20260719/g3_copresence_thresholds.json` — co-presence thresholds
- **W3's exact definition (authoritative):** trace it from the Step-5 validator `3J_docs_occ_nTemp/Leg3_4-split/Step5_docs/3rdJ_05_censusLinkage_4split_val.py`, Section 3 (W1–W4). Read what column / derived field W3 actually measures and how it computes the SYN-vs-OBS max-dev — then replicate THAT exact statistic on the raw pool. Do not invent your own colleague-co-presence definition; use the validator's.

---

## Tasks (in order)

**T1 — Pin down W3's operational definition.** From the val script, extract: the exact column(s) W3 reads (the colleague-co-presence field/group), the WD/WE stratification, and the SYN-vs-OBS max-dev formula (synthetic-rows' per-slot marginal minus observed-rows' per-slot marginal, max abs over the 48 slots, per cycle). Record the column name(s) verbatim.

**T2 — Reproduce the raw-pool W3 statistic (P2).** On `augmented_diaries.csv`, split by `IS_SYNTHETIC`, compute the colleague-co-presence per-slot marginal for SYN vs OBS across all 48 WD slots (and WE slots). **Confirm** the max-dev ≈ 7.19 pp and that the worst WD slot is 26 (16:30). Report the full WD curve around the peak (slots ~22–30, i.e. 14:30–18:00): SYN%, OBS%, delta, and OBS denominator (n) per slot. Verify every number from the file's own columns, not from any log.

**T3 — Attribute the mechanism.** Decide which of these dominates at WD 16:30 (quantify each):
  - **(a) SYN over/under-generation** — the model/co-presence generator emits too many (or too few) colleague-present persons at 16:30 vs what OBS shows.
  - **(b) OBS-side thinness** — the observed denominator at WD 16:30 (esp. any weekend/stratum split) is small enough that OBS% is high-variance; report the n's.
  - **(c) Threshold/generation artifact** — the G3 co-presence thresholds (`g3_copresence_thresholds.json`) or the `04C` logic produce a systematic 16:30 shape mismatch.

**T4 — Answer the pivot (the deliverable).** From reading `04L`, `04T`, and `04C` + the runbook, state definitively:
  - **Is colleague co-presence a rake margin** in `04L_joint_rake` or `04T_act_rake`? (Quote the target-margin list from each.) If NO → a re-rake cannot move W3, full stop.
  - **Where is colleague co-presence set** — is it generated in `04C_pairs` from thresholds, downstream of the rakes? If so, the only lever on W3 is the co-presence generation (thresholds / `04C` logic), not raking.
  - Given (a)/(b)/(c) from T3, is the WD-16:30 divergence **fixable** (name the concrete lever: add a co-presence rake margin? re-tune the G3 threshold at the afternoon slots? nothing — it's OBS-thinness noise?), and roughly at what cost (co-presence regen only, vs partial Step-4 re-run, vs full retrain)?

---

## Deliverable / report back

Append a Progress Log entry to `3J_docs_occ_nTemp/Leg3_4-split/Step4_docs/3rdJ_04_augmentationGSS_4split.md` headed `### 2026-07-21 — W3 colleague-co-presence 16:30 diagnostic (Step-4 reopen, read-only)`, containing: W3's exact column/definition (T1), the reproduced P2 statistic + peak-region table with n's (T2), the mechanism attribution with numbers (T3), and — most important — the **pivot answer (T4): is co-presence a rake margin (yes/no, quoted), and is W3 re-rake-fixable / regen-fixable / irreducible, with the concrete lever and cost tier.**

Then report back to the manager with a 6–8 line summary: the reproduced 7.19 pp / slot-26 confirmation (or correction), the dominant mechanism (a/b/c), and the one-line pivot verdict — **"re-rake CAN / CANNOT fix W3, because ____; if fixable the lever is ____ at ~____ cost."** Do NOT propose or apply any edit. STOP after reporting — the manager adjudicates the fix with the user.

## Disciplines (enforce)
1. **Read-only.** No edits to any `.py`, no re-runs, no writes to `outputs_step4/`.
2. **Verify from the artifact, not the log** — every number re-derived from `augmented_diaries.csv`'s own columns.
3. **Don't scan the whole pool into context** — `usecols` for the co-presence + slot + IS_SYNTHETIC + cycle/day-type columns only.
4. **Use the validator's own W3 definition** (T1) — do not hand-roll a different colleague-co-presence metric.
5. Decision-level questions → stop and flag the manager.
