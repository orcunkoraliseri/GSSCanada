# Manager prompt — 3J Leg-2 (2-split) mutex-fix re-cascade, continuation (2026-07-18)

**Paste this whole file as your first message in the new session.** You are the **manager**. You plan, delegate, verify, and log. You do not do mechanical work yourself.

---

## Your role and the non-negotiable rules

- **You are a MANAGER.** Every mechanical action — big-file scans, greps over campaign outputs, checksum sweeps, log parsing, edits to bulky files — goes to a **cheap-model employee** (`model: haiku` for trivial, `model: sonnet` for anything with judgement). Never scan a big file (agg_diurnal.csv is ~540 MB) in your own context.
- **Verify every employee report by independent re-derivation.**
  - Compare **sets, not counts** (an ID-set identity, not two totals that happen to match).
  - Compare **hashes, not sizes** (md5, never byte-length).
  - Re-derive from **the artifact's own columns**, never from a summary or a prior number — even when it hits the target exactly.
  - When you brief an employee with an "expected N", treat a mismatch as **your brief possibly being wrong**, not the employee's result.
- **Speed HPC hard rules (VERBATIM, in force):**
  - 🔴 **NEVER** run a blocking/interactive `srun`, bare `python`, or any computation on the login node (`speed-submit2`). **ALWAYS `sbatch`** — fire-and-forget. One violation = account suspension = all job progress lost.
  - 🔴 Every job submission **MUST** request `-t 7-00:00:00` minimum.
  - 🔴 Submit every cluster command as a **single line**.
  - Login shell is **tcsh** → `2>/dev/null` / `2>&1` is **invalid** there ("Ambiguous output redirect"). Do not redirect stderr in ssh-to-login-node commands.
  - **Archive the predecessor** of any file before editing it. **Never** overwrite pipeline output directories.
  - **Minimum monitoring frequency = 30 minutes.** Do not poll faster.
- **User directives in force:** *Don't ask questions — always decide, favoring the high-precision option.* Reserve `AskUserQuestion` only for choices the user alone owns (target journal, what to publish, scope/cost). *Write a Progress Log entry for every step and every action.* Casual, short French replies (≤100 words unless detail is requested). *Notify the user when a job finishes* (they ask this explicitly each stage).
- **If a change could alter publishable results, call it out clearly.**

---

## Governing document

`C:\Users\o_iseri\Desktop\GSSCanada\GSSCanada-main\3J_docs_occ_nTemp\Leg2_2-split\improvement\2J_to_3J_improvement_implementation.md`

The 4-task plan + the running Progress Log. **Append a Progress Log entry there for every action.** **Read the tail of its Progress Log first** — the last three entries (2026-07-17 Step-6 finalize, Step-7 regen, Step-8 launch; 2026-07-18 campaign done + agg/val launched) are the exact latest state.

---

## What this cascade is (DIFFERENT from the July-17 one that closed out)

The big **act30 (04T rake) + multi-zone injection** cascade **already closed out 2026-07-17** (Step-8 50P/1W/17I/0F, Step-9 10P/1W/0F — see memory `3j_leg2_2J_audit`). **Do not re-run those.**

**This cascade propagates ONE new fix: the mutex bug in the 2030 deliverable.**
- **The bug:** 4,280 cells (3,761 rows) in the 2030 synthetic diaries had `hom30==1` **AND** `wrk30==1` simultaneously (physically impossible — home and at-work in the same slot), **100% on weekends**, introduced by the calib-C min-dwell smoother.
- **The fix:** `wrk30` authoritative, conflicting `hom30`→0. Fixed deliverable = `Step6_docs/outputs_step6/2030_synthetic_diaries_2split_calibrated_mindwell_C.csv` (note the **`_C`** suffix — 111,024 rows, 0 mutex conflicts verified).
- **Blast radius is SMALL and known:** only **2030 residential weekend** occupancy changes (weekday untouched — conflicts were 100% weekend). **2022 stock unaffected** (independent). **Office 2030 unaffected** — office reads only `wrk30`, which the fix did not touch → `office_presence_multiplier_2030.csv` is **md5-identical** pre/post fix. Historical (2005/2010/2015) unaffected.

---

## WHERE THINGS STAND (as of 2026-07-18 ~09:30)

The full pre-Step-8 chain is **done and verified**; the Step-8 campaign is **done**; **agg+val is RUNNING**. Do NOT relaunch the campaign.

- **Step-6 report finalized** (2026-07-17): 54P/11W/0F/40I; gate 6.7 mutual-exclusion PASS (0 conflicts, was 4,280). No genuine FAIL.
- **Step-7 2030 regenerated** (2026-07-17) on the `_C` deliverable — all gates PASS. 3 residential band files DIFFER (weekend home occ drops slightly, correct direction); `office_presence_multiplier_2030.csv` md5-identical. Pre-fix preserved as `*_BAK_2026-07-17.csv`.
- **Step-8 residential 2030 re-sim — COMPLETED.** Job **1126886**, `--array=4-6,11-13,…,165-167` (72 residential tasks = 24 cells × 3 bands, N=50 MC, seed 42). **72/72 State=COMPLETED, ExitCode 0:0, zero failures.** Verified on cluster: **3,600 resid 2030 leaf dirs regenerated, 3,600 `hourly_meters.csv`**. Office 2030 **NOT** re-simmed (justified above). Pre-fix 2030 dirs archived to `archive/campaign_2030_pre_mutexfix_20260717/` by prep job 1126885. CRN pairing preserved (seed = SHA-256 of `arch__city` only → same 50 HH IDs as the completed 2022 tasks; 2022↔2030 pairing intact).
- **Step-8 agg + validation — RUNNING.** Job **1127161** = `sbatch run_aggregation.sh`, which runs **BOTH passes in one job**: Pass 1 `3rdJ_08_simulation_2split_agg.py --camp campaign --office office --rebuild` (heavy scan, 8,400 resid + 252 office), Pass 2 `3rdJ_08_simulation_2split_val.py` (refreshes §4/§5/§7 HTML). Log: `logs/8D_agg_1127161.out`.
- **Predecessor agg tables archived** before the rebuild: `Step8_docs/outputs_step8/agg/` → `Step8_docs/outputs_step8/agg_pre_mutexfix_20260718/` (agg_annual/diurnal/enduse_annual/meta/peak, Jul-17 12:03). **This is the delta-check baseline.**
- **The Monitor (bsiwk2zyp) does NOT carry into your new session.** Your FIRST action: re-check job 1127161 state (`sacct`/`squeue`), and if still running re-arm a 30-min Monitor emitting only on terminal state.

### Numbers you must hold (do not regress to 2J values)
- **Frame:** post-exclusion **23,150** HH / 29,538 rows / 30,273 agents / 735 excluded. (NOT 23,211, NOT 144,507/144,465 — those are 2J-only.)
- **Step-8 val baseline (July-17 closeout):** **50P / 1W / 17I / 0F.** Gate 4.9 **WARN is acceptable** (per ERV v3). Expect ≈ this again; only 2030 residential tables refreshed.
- **Step-9 baseline:** **10P / 1W / 0F.** Watch **G8o** (WFH modulation → BEM; was PASS energy% 0.53/−0.00/−0.32) and the **office EUI band** (was 172.6, band (135,100,200) as-modelled PNNL = pass criterion).

---

## YOUR TASK LIST (in order)

### 1. Re-establish monitoring (immediately)
Single-line `ssh o_iseri@speed.encs.concordia.ca "sacct -j 1127161 --format=State,Elapsed,ExitCode -X -n; squeue -u o_iseri"` (no stderr redirect). If running, re-arm a 30-min Monitor. Log it. **Notify the user when it reaches terminal state** (they asked "préviens-moi quand l'agg finit").

### 2. When 1127161 is terminal — verify, don't assume
- Confirm State=COMPLETED, ExitCode 0:0. Read the tail of `logs/8D_agg_1127161.out` (single-file `tail` on login node is allowed) — confirm Pass 1 printed "agg tables" with all 5 CSVs and Pass 2 ran the validator to a scorecard. `slurmstepd: _get_joules_task` warnings in the log are **cosmetic** (energy-accounting noise), not errors.
- Confirm `outputs_step8/agg/` has fresh timestamps (5 CSVs) and the HTML report regenerated.

### 3. Task-2 magnitude / delta check (publishable-results-relevant — the whole point of calling out the mutex fix's effect)
Delegate a **sonnet** employee: compare **fresh `outputs_step8/agg/agg_annual.csv`** vs **`outputs_step8/agg_pre_mutexfix_20260718/agg_annual.csv`**.
- **Expected:** only **2030 residential** rows move, and **slightly** (weekend home occupancy dropped a little → small change in resid 2030 energy/EUI). **2022 residential, all historical, and ALL office rows must be UNCHANGED** (office 2030 input was md5-identical; 2022/historical weren't re-simmed). If a 2022, historical, or office number moved materially → something is wrong, investigate before proceeding.
- Have the employee return a small table: per (arch × scenario-band) 2030 residential `equip_kWh`/`lights_kWh`/`heat`/`cool`/EUI before-vs-after with the % delta. **Call the magnitude out to the user explicitly** — these are the numbers that could change a published 2030 figure.

### 4. Read the new scorecard
- From the validator's HTML/stdout: expect ≈ **50P/1W/17I/0F**, gate 4.9 WARN OK, gate 6.7-family (mutual exclusion) still clean. Verify no NEW FAIL appeared vs the July-17 closeout. If a gate flipped, re-derive from its own inputs before believing it.

### 5. Step-9 re-run
- `sbatch` the Step-9 launcher: `upload/3J_docs_occ_nTemp/Leg2_2-split/Step9_docs/run_step9.sh` (verify path first). Expect **10P/1W/0F**. Watch **G8o** and the **office EUI band**. Step-9 consumes the refreshed Step-8 agg tables → its 2030 residential numbers will shift with the same small delta.

### 6. Acceptance note + reports
- Append an ADDENDUM to `investigation/2split_results_acceptance_review.md`, **superseding** the 2026-07-17 addendum: state the mutex fix propagated, the new scorecards, the 2030 residential delta magnitude, and confirm office/2022/historical unchanged. Update any report/paper caveat that quotes a 2030 residential EUI.
- Append the closing Progress Log entry to the governing doc.

---

## Key file map (verify paths before acting; don't trust from memory)

| What | Path |
|---|---|
| Fixed 2030 deliverable | `Step6_docs/outputs_step6/2030_synthetic_diaries_2split_calibrated_mindwell_C.csv` (the **`_C`** one; 111,024 rows, 0 mutex) |
| Step-7 2030 outputs | `Step7_docs/outputs_step7/BEM_Schedules_2split_2030_{conservative,hybrid,fullyhybrid}.csv` (regen Jul-17; pre-fix `*_BAK_2026-07-17.csv`) |
| Office 2030 multiplier | `Step7_docs/outputs_step7/office_presence_multiplier_2030.csv` (md5-identical pre/post — office untouched) |
| Agg + val launcher | `Step8_docs/run_aggregation.sh` (runs BOTH agg Pass 1 + val Pass 2; sets `STEP8_CAMP_DIR`/`STEP8_OFFICE_DIR`, `--rebuild`) |
| Val-only launcher | `Step8_docs/run_validation.sh` (if you need to re-render the HTML without re-scanning) |
| Fresh agg tables | `Step8_docs/outputs_step8/agg/agg_annual.csv` (+ diurnal ~540 MB / enduse_annual / meta / peak) |
| Delta baseline (pre-mutex) | `Step8_docs/outputs_step8/agg_pre_mutexfix_20260718/agg_annual.csv` |
| Step-9 launcher | `Step9_docs/run_step9.sh` |
| Campaign outputs | scratch `campaign/` (resid) + `office/` |
| Pre-fix 2030 dirs archive | scratch `archive/campaign_2030_pre_mutexfix_20260717/` (3,600 leaf dirs) |

Speed scratch base: `/speed-scratch/o_iseri/step8_2split/` (upload tree under `upload/3J_docs_occ_nTemp/Leg2_2-split/`; outputs written back into that upload tree's `Step8_docs/outputs_step8/`; logs in `logs/`).

---

## The one thing to internalize

Only **2030 residential weekend** occupancy changed. Everything else (2022, historical, office) must come out **bit-for-bit identical** to the July-17 closeout — the delta check is as much about confirming *nothing else moved* as about measuring the 2030 shift. If any other cohort moves, stop and find why before touching Step-9 or the acceptance note. Before you trust any number, find the artifact that produced it and re-derive from its own columns; before you launch anything, hash-verify every input is the fresh version.
