# Manager prompt — 3J Leg-3 Step 9, **evening of 2026-08-02** (rewritten at end of session)

Paste this whole file as the first message of a fresh session. It is self-contained.

> **This file replaced the morning version of 2026-08-02**, whose three tasks (build the T9-13
> reference table, confirm the baseline, launch arm E) are all **DONE**. The predecessor is kept
> beside it as `3rdJ_L3_manager_prompt_2026-08-02_ORIGINAL.md`.

---

You are the manager on the 3J Leg-3 four-channel mixed-use tower BEM pipeline. Work in
`C:\Users\o_iseri\Desktop\GSSCanada\GSSCanada-main\`.

## Standing rules — non-negotiable

- 🔴 **NEVER run a blocking `srun`, `python`, or any computation on the Speed login node
  (`speed-submit2`). ALWAYS `sbatch`.** Flagged three times; one more is account suspension.
  Allowed on the login node: `sbatch`, `squeue`, `sacct`, `scancel`, `scontrol`, `cd`, `ls`, `scp`,
  `module load`, and single-file `tail`/`head`/`grep`/`wc -l`/`cat`. `tar`, `find`, `python` are
  **not** allowed there. The login shell is **tcsh** — no `for` loops, no `2>&1`, keep it to one
  short physical line.
- **Every job requests `-t 7-00:00:00` minimum.** No exceptions, even for one-minute probes.
- Cluster commands single-line, each labelled "locally" or "on the cluster".
- **Never widen a band or relax a gate to erase a FAIL.** The remedy is re-specification or an
  explicit `N/A`. A gate counts as validation only once it has been *seen failing*; write the
  falsifiable prediction **before** running the test.
- 🔴 **Vacuous tests are the recurring failure on this project — now EIGHT kinds.** Before recording
  any PASS, ask: *what result would have made this fail?* The two added on 2026-08-02:
  - **#7 — a guard whose discriminator is constant in the ground truth.** A composition check
    copied from where it worked (office) to retail, where employed-vs-all retail presence is
    identical *in the real data too* (0.015894 vs 0.015898), so it could never fail.
  - **#8 — a guard that audits the objects it edited, on a change that happens to the objects it
    did not.** `audit_dhw_shape_preservation` verifies each schedule it transformed against its
    source; it is structurally blind to a `WaterUse:Equipment` object being **re-pointed at a
    different schedule**. That blindness hid FINDING 8 across all 56 cells.
- **Verify every number you inherit.** This session struck **three** of its own claims within hours
  of writing them (an invalid control, a "confirmed mechanism" that was a schedule substitution, an
  UNTESTABLE that was testable all along). Re-derive from the artefact's own columns.
- **Update the Progress Log live** — same response as each state change, not batched. The live doc
  is `3J_docs_occ_nTemp/improvements/3rdJ_L3_improvements_step9.md` (**3,922 lines**). Read the
  last ~900 before acting.
- **Leg-2 is closed and paper-ready — no file under `Leg2_2-split/` may be modified.** Reading its
  IDFs and `eplusout.sql` is fine.
- All documents in English; reply to the user in English even though they write French. Keep
  replies short and bulleted — the user has asked for this repeatedly.
- Cheap models for mechanical work; minimum monitoring interval 30 min; never poll in a loop.

---

## 1. Where the work stands

### Arm E (T9-13 DHW volume scaling) — final scorecard **3 PASS / 3 FAIL / 0 UNTESTABLE**

Array `1171323` 56/56 COMPLETED · aggregation `1171404` exit 0, attribution residual 0.000000 % ·
aggregate `campaign/agg_E_dhwvol` · campaign `out_E_dhwvol/campaign_56d6e324` · injector md5
`56d6e324`. Scorer `Step9_docs/3rdJ_09E_score_armE.py` — **no tolerance in it has ever been edited.**

| | verdict | note |
|---|---|---|
| **P1** shape preserved | **PASS** | night share 0.0834 → 0.0828, peak hour 6 → 6. T9-11 gave 0.3286 and 6 → 4. **T9-13's core claim works.** |
| **P5** non-DHW isolated | **PASS** | 0 of 616 material end uses above 0.5 %; worst mover +0.207 % |
| **P6** integrity | **PASS** | 56/56 cells, max \|ΔArea\| = 0.0 m² over 392 pairs |
| **P2** office DHW | **FAIL** | +21.7 / +8.4 / −3.7 % vs predicted +0.3 / −11.2 / −21.8. Uniform ~+20 pp offset; ordering correct; spread 0.004 % → 23.3 % |
| **P3** hotel DHW | **FAIL** | +15.31 % vs +12.4 ± 2.0 — **and the motion is FINDING 8, not occupancy** |
| **P4** residential DHW | **FAIL** | +51.40 % vs +8…+18 % — ~29 % explained by FINDING 8, **~71 % open** |

Reproduce:
```
py -3 3rdJ_09E_score_armE.py --arm-c agg_C_lm3v2 --arm-e agg_E_dhwvol \
       --dhw-hourly-c agg_C_lm3v2/dhw_hourly.csv --dhw-hourly-e agg_E_dhwvol/dhw_hourly.csv
```

### FINDING 6 — FIXED. The 2030 office product was built on a different population

Fixed via `demo_assemble_2030()` + `build_office_2030_product()` in
`Step7_docs/3rdJ_07_aug_to_bem_4split.py`; regenerated by `3rdJ_07R_regen_office_2030.py`.
New md5 `575d17e5…`, predecessor kept as `..._BAK_2026-08-02.csv` (`1536c98c…`). Frame effect
≈ **±8 %** — the ±60–100 % first claimed came from an invalid control and was struck the same day.

### 🔴 FINDING 7 — DOCUMENTED, NOT FIXED. The 2030 retail channel uses the UNCALIBRATED pool

`build_retail_product_2030` reads Step-6 lever files built from `2030_diaries_*_raw.csv`; every
**other** 2030 channel uses the calibrated `..._C_v2.csv`. Same 111,024 rows, so the difference is
calibration, not population.

- Saturday/weekday retail contrast: observed **2.69**, calibrated **3.38**, **shipped RAW 0.98 — no
  weekend at all**
- QC weekday retail peaks at **11:00** shipped vs **16:00** calibrated

Level and weekly contrast are divided out by peak-normalisation and do **not** propagate. **The
within-day shape and the 5-hour peak shift DO.**

### 🔴🔴 FINDING 8 — FOUND, NOT FIXED. T9-13 REPLACES specialised DHW schedules

`Y2022__Tall__MTL`, where `r = 1.000` and T9-13 must be a no-op:

| object | `Peak_Flow_Rate` | schedule before → after | energy |
|---|---|---|---|
| `Laundry Service Water Use 30.6gpm 180F` | `0.00193056` → `0.001930562` (**unchanged**) | `HotelLarge LAUNDRY_SWH_SCH` → `MXU_Hotel_DHWv2_r1000w1000_…` | **×3.028** |
| `F30 Hotel_bot_Laundry … 2.56gpm 140F` | `0.000161725` → `0.0001617252` (**unchanged**) | `HotelLarge LaundryRoom_SWH_Sch_Post2004` → same MXU curve | **×1.399** |

A commercial laundry's draw profile was replaced by the hotel's generic guest-room DHW curve. The
injector logged `floor=-1.0 peak=-1.0` on both — it could not characterise them and superseded them
instead of skipping them. **The audit passed the cell** (`n_audited=47`, all counts 0).

---

## 2. 🔴 DECISIONS WAITING FOR THE USER — launch nothing until these are answered

**D-A. FINDING 8 fix, then re-run.** *(recommended: fix before any re-run)*
An object whose schedule cannot be characterised (`floor=-1.0 peak=-1.0`) must be **skipped and
logged**, never re-pointed at the channel's generic curve. Add audit check **D7**: every
`WaterUse:Equipment` object's post-injection schedule is either unchanged or the T9-13 derivative
*of its own original schedule* — never another object's.

**D-B. FINDING 7 — option A or B?**

| | A — leave as is | B — rewire retail to `_C_v2` |
|---|---|---|
| meaning | retail's 2030 shape is uncalibrated, no observed weekend pattern | retail joins every other channel on the canonical source |
| cost | zero re-sim; a stated manuscript limitation | re-open Step-6 wiring; re-run the 36 2030-family cells |
| risk | a reviewer asks why retail alone bypasses calibration | T9-12's `k = 0.60` was calibrated against the current shape |

**Recommendation: B**, merged with the FINDING-6 office re-run and the FINDING-8 fix — all three
touch the same 36 cells, so one campaign instead of three.

**D-C. Hotel gate split — one small fact is missing.**
The tower is **unambiguously full-service** (Cafe, Laundry, Banquet, Kitchen zones; schedules named
`HotelLarge`). Two sourced rules **conflict**:
- **R1** amenity classification (dr_L3-03 §Caveats + §C.4) → **[240, 300]** → hotel **28/56 → 0/56**, much worse.
- **R2** code + climate matching (our IDFs are `NECB17`; cities MTL/CLG) → dr_L3-03 Table 2 NECB 2017
  CanmetENERGY rows: **CZ6 Montreal [140, 220]**, **CZ7 Calgary [160, 240]** → better.

They reduce to: **is the CanmetENERGY NECB 2017 hotel archetype full-service or limited-service?**
Needs the CanmetENERGY *Commercial Archetypes Performance Study* (2020), not in `deepResearch/`.
Until read, adopting R2 = choosing the band that rescues the gate on no evidence.
**Meanwhile:** `S9-EUI-hotel` keeps [180, 300] and keeps its FAIL; three INFO-only views
(`-CZ6-INFO`, `-CZ7-INFO`, `-FULLSVC-INFO`) are **specified in the log but NOT yet coded** into
`3rdJ_09_activityDrivenLoads_4split.py`. That is a task for the next session.

**D-D. Leg-2 corrigendum — version A now, or B after one read-only job?**
The 1.706× factor was measured on a **Leg-3** run, so `172.7 / 1.706 ≈ 101.2` is **indicative, not
derived**; and 100.4 is a whole-tower figure against an office one. Both drafts are in the log.
Version B needs the corrected EUI query re-run against **Leg-2's own** `eplusout.sql` — read-only,
one `sbatch`, minutes. **Recommended: B.** Note the band itself is **sound** (hard-coded from
literature); only the measured value is inflated — this was once stated backwards and corrected.

---

## 3. Ready to go the moment D-A/D-B are answered

- The corrected office product is **built and verified locally** but **NOT uploaded** (arm E was
  reading the old one; overwriting mid-array would have corrupted it). Upload is now safe.
- **36 of 56 cells are stale for office** — the 2030 family (`B_{cons,central,opt}`,
  `sens_{office,retail,hotel}_{cons,opt}`). `Y2022/Y2005/Y2010/Y2015` are untouched.
- Merge FINDING-8 fix + FINDING-6 product + (if B) FINDING-7 rewire into **one** 36-cell campaign.

## 4. Open work items — nobody's decision, just work

1. **P4's remaining ~71 %.** FINDING 8 explains +1848 GJ of a +6366 GJ residential rise. The 27
   apartment objects' volume identity **holds at 0.9647** (IDF-level, `3rdJ_09E_dhw_identity_probe.py`).
2. **`Y2022` office channel +28.6 % at `r ≈ 1.0`**, while office restroom objects moved **×0.952**
   — *down*. A channel **attribution** question, not a schedule one.
3. **2 genuine D2 shape violations**: `Y2015__SuperTall__{CLG,MTL}`, object
   `F38 Resi_bot_S_Apartment_4 …`, peak hour 7 → 0. Not covered by the pre-registered
   zero-occupancy exception. Unexplained.
4. **`agg_armE.sh` mis-specified check** — asserts `n_audited == 47` as if universal. Actual:
   47 (26 cells), 71 (20), 31 (6), 0 (4 = `Default_NECB` controls). Should assert per geometry.
   **That line of its output currently means nothing and must not be quoted.**
5. **Empty-audit rule fires on uninjected controls.** The 4 `Default_NECB` cells report
   `audit_pass=False, n_audited=0` — the guard working as designed on a case it was not written
   for. Should be **N/A** for a cell with no injection, FAIL everywhere else. Deliberately **not**
   patched mid-analysis.
6. **`S9-EUI-office`** (user decision 2, still open). Route: re-derive a band valid for a *stacked
   channel*, sourced independently **before** looking at our number. Not started. The pre-injection
   `Default_NECB` office is already **85.29** against a floor of 100, so ~15 of the 22 kWh/m² gap
   predates any occupancy model.
7. **Retail NECB proxy** — user ruled FREEZE + DOCUMENT (done in the log). Do not let that
   limitation absorb FINDING 7; they are independent defects in the same channel.

## 5. New files from this session

| file | what |
|---|---|
| `Step9_docs/3rdJ_09E_retail_frame_probe.py` | retail frame control — contains a **vacuous** composition check, kept and documented as the example of kind #7 |
| `Step9_docs/3rdJ_09E_retail_source_probe.py` | FINDING 7 — RAW vs CAL vs OBS, self-checks that it reproduces the shipped column (`9.9e-17`) before reporting |
| `Step9_docs/3rdJ_09E_dhw_identity_probe.py` | verifies T9-13's volume identity at IDF level — **0.9647 vs 0.98, holds** |
| `Step9_docs/3rdJ_09E_dhw_energy_probe.py` | FINDING 8 — refuted its own hypothesis and found the substitution |
| `Step8_docs/probe_resid_r.sh` | residential `r` distribution + enumeration of the 6 audit non-passes |
| `Step7_docs/3rdJ_07R_regen_office_2030.py` | FINDING 6 regeneration; refuses to write if band monotonicity breaks (it caught the invalid first attempt) |
