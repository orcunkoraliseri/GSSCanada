# Figure S3 -- The two-channel construction pipeline, in full

> ⚠ **Paste ONLY the fenced code block below into the image tool.** Everything outside the
> fence is authoring notes, corrections and provenance. Pasting the whole file hands the
> generator text it is not supposed to draw.

**Target:** web image-generation LLM · **Style family:** flat-2D horizontal (shared across all schematics)
**Purpose:** give a reader the internals of the **two-channel construction stage** this paper builds
on, rather than the one-line summary in §1.4. Figure 2 draws the three stages as nested; this figure
opens the middle one.
**Source:** the two-channel stage's step docs under `3J_docs_occ_nTemp/Leg2_2-split/`; §1.4 and the Methods chapter
of this paper, which describe the same stage in prose.

> ⚠ **Schematic, not a data figure.** Nothing on it is a measured result, so it may be drawn. But it
> must not display any EUI, share, hour or gate count: that stage's numbers are deliberately absent from
> this paper's front matter, and a number added here would be a result of that stage appearing where
> the paper states there is none.

> 🔴 **Naming rule, 2026-08-11.** The project's internal stage names -- the word "Leg" followed by a digit,
in any spelling or punctuation -- must
> not appear anywhere in the generated image. They are this project's internal names for its own
> construction stages; the manuscript was rewritten on 2026-08-11 to remove them from every sentence,
> so a reader has no way to resolve them. Use "the two-channel construction stage" and "this study".
> Colour names are styling instructions and must never be drawn as label text -- the 2026-08-11
> Figure 1 printed the literal word "amber" inside two boxes. Crop tight: no large empty band on any
> side, even margin of roughly 2% of image width, 500 dpi or better for the printed width.

## 🔴 TERMINOLOGY LOCK (v2) -- applies to every string drawn in this image

The project's internal stage codes must **not** appear anywhere in the artwork, in any spelling or
punctuation: `Leg-1`, `Leg 1`, `Leg1`, `Leg-2`, `Leg-3`, `leg`, `2J`, `3J`, `1J`, `0J`.
They are internal build names, undefined for any reader, and the manuscript text was cleared of them
on 2026-08-11. If a stage must be named, use the descriptive form:

| do NOT draw | draw this instead |
|---|---|
| Leg-1 | `single-channel stage` (or `Residential only`) |
| Leg-2 | `two-channel stage` (or `Residential + Office`) |
| Leg-3 / 3J / this leg | `this study` (or `Residential + Office + Retail + Hotel`) |
| "three legs" / "the legs" | `three stages` |
| 2J / 1J / 0J | `the predecessor study` |

`**Source:**` lines elsewhere in this file cite real folder names on disk (`Leg3_4-split/...`).
Those are provenance for the authors and are **outside the fenced prompt** -- they are never drawn.

**Resolution.** Elsevier requires 500 dpi for combination art. Placed at 7 in wide that is
**3500 px minimum on the long edge**; generate as large as the tool allows and do not upscale
afterwards. The 2026-08-09/11 images came back at 1376 x 768 (~197 dpi), which fails.

---

## Prompt (paste into the image LLM)

```
Clean flat 2D vector pipeline diagram in the style of a polished journal supplementary figure. Strictly flat: NO 3D, no isometric, no perspective, no drop shadows, no gradients. Horizontal left-to-right reading order, landscape composition, white background, sans-serif labels, generous whitespace. A single row of nine rounded rectangular stage boxes connected by thin arrows. The boxes are, in order: "Data Collection", "Harmonization", "Merge & Tiling", "Two-Head Transformer", "Archetype Linkage", "Forecast 2030", "BEM Integration", "BEM Simulation", "End-Use Loads". Two channels run through the pipeline and are shown as two coloured lanes inside the stage boxes from "Merge & Tiling" onward: an upper lane in slate-blue labelled "residential" and a lower lane in muted teal labelled "office". At the "BEM Integration" box, show the two lanes taking DIFFERENT routes into the building: the residential lane carries a small solid icon labelled "REPLACE" and the office lane carries a small outlined icon labelled "MODULATE", with the two routes visibly distinct. A small flat magnifying-glass or checkmark badge sits under "BEM Integration" labelled "wiring verification". A two-entry legend at the lower right: slate-blue "residential", muted teal "office". No numbers anywhere on the diagram.

SCENE: nine-stage horizontal pipeline, two coloured channel lanes from stage 3 onward, the two lanes diverging at BEM Integration into REPLACE and MODULATE, a wiring-verification badge beneath that stage.
```

## Annotations to overlay afterward (exact text -- keep OUT of the AI image)

- Figure caption line: `The two-channel construction stage in full: residential and office presence generated by a two-head conditional generator and injected by two different mechanisms.`
- Stage-4 label: `Two-head conditional Transformer -- one decoder head per channel; the direct ancestor of this paper's three-head design`
- REPLACE route: `residential presence REPLACES the baseline schedule, per household`
- MODULATE route: `office presence MODULATES a code-of-record density rather than overriding it`
- Badge: `the wiring-verification lesson carried forward into this study`
- Closing line: `The two-channel stage is a construction step in this project, not a second headline result; no magnitude from it is reported in this paper.`

## Layout notes

- Aspect ratio: wide landscape (16:9), reading direction left to right.
- Style: flat 2D, matching Figures 1 to 6 exactly - same box radius, same arrow weight, same label size.
- **Two channels, not four.** The whole point of this figure is that the stage it documents had two.
  Do not add retail or hotel lanes; their absence is the contrast with Figure 1.
- The colour convention differs from the results figures on purpose: this is a schematic in the
  schematic palette, not a plot in the channel palette.

## 🔴 What the shipped version gets wrong

The content is right -- nine boxes, correct order, no duplicates, no stage code, two lanes not four.
Two things are wrong, and both are mechanical:

1. **32% of the image height is empty white above the diagram** (246 px of 768). The artwork occupies
   the middle band only. Crop so the diagram fills the frame with an even margin of roughly 2% of the
   image width on all four sides.
2. **The resolution claim in this file was wrong, and the real number is half of it.** The line below
   used to say the file being replaced was 2752 x 1536 px. It is **1376 x 768 px**, which is about
   **197 dpi** at a 7 in printed width, against Elsevier's 500 dpi minimum for combination art. Every
   one of the nine generated schematics in this set has the same 1376 x 768 geometry, so this is not a
   defect of one image.

Note that three of the regenerated files declared `300` dpi in their PNG `pHYs` chunk while still
being 1376 x 768. That field is a printing hint; it adds no pixels. Only a larger render fixes this.

## Output requirements

- **Minimum 4000 px on the long edge.** The file this replaces is 1376 x 768 px, roughly 197 dpi at
  page width -- well below Elsevier's 500 dpi for combination art. 4000 px on the long edge gives
  about 570 dpi at a 7 in placed width.
