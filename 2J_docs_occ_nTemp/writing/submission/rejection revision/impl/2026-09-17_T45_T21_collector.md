# T45 - T21 Step-8/Step-9 rerun collector - report

VERDICT: STOP RULE TRIGGERED (A6 fails outside the 0 +/- 1 hour band, confirmed by real
measurement on part of the grid). A1, A2(restated), A2X, A3, A4 all PASS on the full 24-cell,
7200-run scorer output. A5 (report-only) shows a severe defect but per the manager's override
cannot fail this task on its own.

A6 FAIL = STOP RULE TRIGGERED

Task doc read: `2026-09-15_T21_wp1_step8_step9_rerun.md` (A1-A6 as written there).
Plan log read: `00_REVISION_PLAN.md` entries (br), (bs), (bt) and section 5 in full.
Cluster job scored: 1329216 (`t21_check`), COMPLETED, exit 0:0, elapsed 00:50:25.

## 1. Selftest evidence (seen-failing discipline)

Log: `T21/logs/t21_check_1329216.out` (scp'd to local scratch, 46,613 bytes, read in full).

The selftest block ran (not skipped), lines 2-27:
```
--- STEP START: A2-logic selftest (must report FAIL on the swapped-id copy) ---
...
[SELFTEST] cell=SingleD__Toronto_5A label=SingleD__Toronto_5A pool_size=28667 n=50 swap sample=1 32811->999999999
[SELFTEST] unmodified-copy vs redraw: PASS (expect PASS)
[SELFTEST] swapped-copy    vs redraw: FAIL:1_mismatch (expect FAIL) mismatches={1: ('999999999', '32811')}
[SELFTEST] RESULT: PASS -- A2 check correctly detects a swapped id
--- STEP OK: selftest behaved as expected (PASS on unmodified copy, FAIL on swapped copy) ---
```
The selftest demonstrably reports FAIL on a deliberately corrupted manifest (one id swapped,
32811 -> 999999999) and PASS on the clean copy. This is not a PASS-only checker; it is proven to
be able to say FAIL. The real A1-A4/A2X check only ran after this selftest passed (job script
`t21_check.sh` aborts before the real check if the selftest does not behave as expected, per the
manager's (br) note; the log shows "STEP OK" then immediately "STEP START: real A1-A4 ... check").

## 2. A1-A6 scored

| # | Criterion (one line) | Measured | Source | Verdict |
|---|---|---|---|---|
| A1 | Delivered runs / planned runs per cell, each with an 8760-row hourly_meters.csv; fewer than 4790/4800 (Step 9 basis) triggers manager review | 7200/7200 delivered across all 72 (campaign, cell) rows; `a1_bad_rowcount_n`=0 on every row | `t21_check_report.csv` all 72 rows; log line 1229 `[A1] delivered 7200/7200 across 72 (campaign,cell) rows` | PASS |
| A2 (restated) | Per cell/campaign, `(sample, hh_id)` equals an INDEPENDENT engine re-draw (load_schedules + validate_household_schedule + pair + seed + sample), not the old published manifest (basis changed by manager decision in the T21 doc because the rebuilt schedule stock drops a different set of households, so exact equality with the old manifest is impossible by construction) | 0/72 mismatch rows | `t21_check_report.csv` column `a2_pairing`=PASS, `a2_mismatch_n`=0 on all 72 rows; log line 1230 `[A2 restated] pairing FAIL/NO_REDRAW/NO_RUN rows: 0/72` | PASS. Scored against the RESTATED wording (T21 doc "Manager decision (basis change for A2...)"), not the brief's original "equal to published manifests" line, per the manager's override in this task's instructions. |
| A2X | Step 8, Step 9 activity, Step 9 baseline draw the SAME households per cell (new acceptance item, not in the original brief, added when A2 was restated) | 0/24 cells with a cross-campaign mismatch | `t21_check_report.csv` `a2x_cross_campaign`=PASS on all 72 rows; log line 1231 `[A2X] ... FAIL/NO_RUN cells: 0/24` | PASS |
| A3 | No "schedule.json not found" / "invalid" line in any task log | 0 hits per the scorer's own count; independently re-verified by me with `grep -li` across every `t21_t21_step8_*.out`, `t21_t21_step9_activity_*.out`, `t21_t21_step9_baseline_*.out` and `t21_a4_after_1328427.out` on the cluster: all four greps exited 1 (no match) | Log line 1232 `[A3] logs with a fallback/invalid hit: 0`; my own ssh greps (see section 4) | PASS |
| A4 | md5 of the 4 staged schedule CSVs unchanged before vs after the arrays ran | All 4 files byte-identical before/after (same md5 each) | `T21/logs/t21_a4_after_1328427.out` (read in full, 8 lines): `afacbf9f...`/`8a60df97...`/`28c7c2ce...`/`f500ea20...` identical in the "before" and "after" blocks; log line 1233 `[A4] PASS: all 4 sched files unchanged` | PASS |
| A5 | (report-only, cannot fail the task) SHEU +/-15% gate on the new outputs, report pass count of 48 | NOT run at full grid (24 cells x 2 years = 48 combos) by the official pipeline before this task. I ran the real `step9_validate_full.py` unmodified, locally, against a partial download: 4 cells (SingleD, OtherDwelling, MidRise, HighRise, all at Montreal_6A), n=15 of 50 households, both years, both arms (240 raw hourly_meters.csv files). Result: 2/8 combos pass. SingleD passes cleanly (equip -0.0%, light -0.0%/-0.0%). OtherDwelling, MidRise, HighRise FAIL by enormous margins: equip +514% to +8061%, light +600% to +8974%. | My own `A5_results.csv` (built from the official unmodified script; see section 5) | REPORT ONLY, per the manager's override. NOT a full-grid measurement (see "what I could not check"). The magnitude (up to 80x target) is not plausible as a schedule-content effect; it looks like a per-unit-vs-whole-building normalization gap that only has a partial documented correction (the OD_N_UNITS fridge-only subtraction for OtherDwelling) and none at all for MidRise/HighRise. I did not investigate the root cause (out of scope, no script edits permitted) and did not check whether the same gap existed in the ORIGINAL published campaign's own SHEU numbers for these three archetypes. This is a major finding, flagged as FINDING 1 below. |
| A6 | STOP RULE. `step9_loadshape_aggregate.py` activity-vs-baseline peak-hour-of-day shift within 0 +/- 1 hour | NOT run at full grid by the official pipeline before this task. I ran the real `step9_loadshape_aggregate.py` unmodified, locally, on the SAME 240-file partial sample as A5. Result (equip_bldg_shift, both years identical per cell): HighRise__Montreal_6A = 0h (PASS); MidRise__Montreal_6A = 0h (PASS); SingleD__Montreal_6A = -1h (boundary PASS, |shift|=1); OtherDwelling__Montreal_6A = -6h (FAIL, both 2022 and 2030) | My own `peak_shift_summary.csv` and `loadshape_profiles.csv` (section 5); hand-verified by manual argmax on the raw hourly data (section 3 style check, see section 5) | **FAIL for OtherDwelling__Montreal_6A (-6h, both years). STOP RULE TRIGGERED** on this partial sample. Only 4 of 24 cells and 15 of 50 households were checked (see "what I could not check"), so the full-grid picture is not yet known, but the rule as written triggers on any measured violation, and a real -6h violation was measured, not assumed. |

## 3. Hand-check (one cell, one household, by hand)

Cell: `SingleD__Montreal_6A`, sample 1 = `HH130228` (confirmed against
`T21/out/step9_activity/SingleD__Montreal_6A/cell_manifest.csv` row 1: `1,130228,1,SingleD,Quebec`).
Arm: activity, year 2022. Raw file: `T21/out/step9_activity/SingleD__Montreal_6A/sample_001_HH130228/2022/hourly_meters.csv`
(scp'd down, 8760 data rows + 1 header row, confirmed by `wc -l` = 8761).

Manual arithmetic (independent script, `Electricity:Facility` column, in Joules):
- sum(Electricity:Facility) over 8760 rows = 29,553,600,467.90 J
- annual kWh total = 29,553,600,467.90 / 3.6e6 = **8209.333463 kWh**
- max(Electricity:Facility) = 15,544,993.55 J at absolute hour index 5968 (hour-of-day 16)
- peak hourly demand = 15,544,993.55 / 3.6e6 = **4.318054 kW**

Cross-check against the official (unmodified) `step9_validate_full.py`, run locally on a
1-row-pair manifest built from this exact same household's baseline+activity files
(`ac_elec_kwh` column of the script's own output): **8209.33346330625 kWh**.

8209.333463 (hand) vs 8209.33346330625 (script) - agree to 6 decimal places, difference is
floating-point rounding only. No disagreement found; the scorer's per-household kWh arithmetic
is correct for this cell.

(The daily-peak-demand figure, 4.318 kW, has no direct scorer counterpart to compare against -
`step9_validate_full.py` reports annual kWh, not peak kW, and A6's "peak" is a peak HOUR-OF-DAY
of the mean diurnal profile, not a peak demand value - see the A6 methodology check below, which
plays the same cross-check role for that metric.)

## 4. Log grep for failure words

Ran on the cluster (single-file/single-glob greps only, login node, no compute):
- `T21/logs/t21_check_1329216.out`: only self-referential hits (`[A3] ... invalid hit: 0`); zero
  real invalid/not-found/severe/traceback/warning lines.
- `grep -li 'invalid\|not found\|severe\|traceback' T21/logs/t21_t21_step8_*.out` -> exit 1 (no match)
- `grep -li 'invalid\|not found\|severe\|traceback' T21/logs/t21_t21_step9_activity_*.out` -> exit 1 (no match)
- `grep -li 'invalid\|not found\|severe\|traceback' T21/logs/t21_t21_step9_baseline_*.out` -> exit 1 (no match)
- `grep -i 'invalid\|not found\|severe\|traceback' T21/logs/t21_a4_after_1328427.out` -> exit 1 (no match)
- `grep -ic warning T21/logs/t21_t21_step9_baseline_1328426_23.out` -> 0
- `grep -c fallback` on the same file -> 200 hits, all `LightingGenerator: No .stat file found,
  using conservative fallback.` (2 lines per household x 100 household-years in that array task).
  This is the known, pre-existing, benign LightingGenerator fallback already recorded in earlier
  T-series docs (no .stat file for this weather set), unrelated to the A3 "schedule.json not
  found" fallback the scorer checks for. Not a new finding.

No occurrence of "invalid", "not found", "severe", or "traceback" anywhere I looked. "warning:"
does not appear either (0 hits, case-insensitive, in the one array log spot-checked in full).

## 5. How the A5/A6 partial numbers were produced

Both `step9_validate_full.py` and `step9_loadshape_aggregate.py` were NOT run against the T21
rebuild by anyone before this task (confirmed: no `loadshape_profiles.csv`, `peak_hours.csv`,
`peak_shift_summary.csv`, or `cluster_run_results.csv`/A5 output exists anywhere under
`/speed-scratch/o_iseri/2J_revision/T21/`, checked with `find`). Running them on the full grid
(24 cells x 2 arms x 2 years x 50 households = 4,800 raw hourly_meters.csv files, each ~1.16 MB,
~5.5 GB total) was judged infeasible within this task's rules (no cluster compute, no sbatch) and
this collection pass's time budget, since scp has no working remote-glob-with-unique-rename
option and each individual file needs its own scp connection (~0.7s/file due to SSH handshake
overhead; SSH multiplexing was tried and did not help - the login node still opens a fresh
connection per file).

What I actually did: downloaded all `hourly_meters.csv` files (both arms, both years, first 15 of
the 50 sampled households) for 4 cells, one per archetype, all at Montreal_6A (240 files, 0
download failures, all confirmed non-empty). Reorganized them locally into the exact directory
layout `step9_loadshape_aggregate.py` expects (`idfs/<cell>/<arm>/<sample>/<year>/hourly_meters.csv`)
and built a manifest CSV for `step9_validate_full.py`. Ran BOTH scripts completely unmodified
(no edits, per the hard rule) with `py -3.13` locally. `step9_loadshape_aggregate.py` reported
"Processed: 240 Skipped: 0" (no row-count or missing-column errors). I hand-verified the
loadshape script's own argmax logic against `loadshape_profiles.csv` for OtherDwelling__Montreal_6A
2022 directly: activity `equip_bldg_W` peaks at hour-of-day 12 (4331.51 W, next-highest 17
at 3955.82 W); baseline `equip_bldg_W` peaks at hour-of-day 18 (7659.89 W, next-highest 17 at
7562.82 W). 12 - 18 = -6, matching the script's own `peak_shift_summary.csv` row exactly. Not a
script bug; a real feature of this data.

## FINDINGS

1. **A5 SHEU gate: severe, implausible-magnitude failures for 3 of 4 sampled archetypes.**
   OtherDwelling, MidRise, HighRise all show equipment/lighting energy 5x to over 80x the SHEU
   target (SingleD is fine, -0.0%/-0.0%). A single household's raw `hourly_meters.csv` for
   HighRise already shows ~156,105 kWh/year for `InteriorEquipment:Electricity` alone - about
   80x the SHEU target of ~1,922 kWh - BEFORE any per-household averaging, so this is not an
   averaging bug in my own aggregation; it is present in the raw building-level meter for that
   one household's own run. `step9_validate_full.py` only applies a partial per-archetype
   correction (subtracting 6 extra fridges' worth of energy for OtherDwelling); it applies no
   correction at all for MidRise/HighRise. This looks like a per-unit-vs-whole-multi-unit-building
   normalization gap, not a schedule-content problem, but I did not trace it further (out of
   scope, no script edits allowed, and I could not check whether this same gap existed in the
   originally published campaign's own SHEU numbers for these archetypes - I do not have that
   raw data staged anywhere I could reach this session). Report-only per the manager's override,
   but this should be looked at before any HighRise/MidRise/OtherDwelling energy number is used
   in the paper.
2. **A6 STOP RULE:** OtherDwelling__Montreal_6A shows a -6 hour activity-vs-baseline peak-hour
   shift (both 2022 and 2030), far outside the 0 +/- 1 hour band, while HighRise and MidRise show
   0h and SingleD shows -1h (boundary pass) on the same partial sample. This is a real, hand
   -verified measurement (section 5), not a script defect. Only 4 of 24 cells were checked, so it
   is not known whether this is confined to OtherDwelling or appears elsewhere in the grid.
3. Both A5 and A6 are open for 20 of 24 cells and for the remaining 35 of 50 households in the 4
   cells checked - this collection pass did not, and given the cluster-compute and no-sbatch
   rules could not, run the full official grid. Whoever picks this up next needs either: sbatch
   authority to run `step9_validate_full.py --root T21/out/...` and
   `step9_loadshape_aggregate.py --root T21/out/...` for real on the cluster (fastest, correct
   scale, but outside this task's rules), or a much larger local download than this task's time
   budget allowed.

## What I could not check and why

- Did not run A5/A6 on the full 24-cell, 50-household grid. Reason: no cluster compute allowed
  under this task's rules (no sbatch, no login-node python), and downloading the full ~5.5 GB of
  raw hourly output via individual-file scp (no working remote-glob-with-rename option, ~0.7s per
  file handshake overhead even with SSH multiplexing attempted) was not feasible inside this
  task's time budget. Covered 4 of 24 cells (one per archetype, Montreal_6A only) at n=15 of 50
  households instead, and said so plainly in the A5/A6 rows above rather than extrapolating to
  the other 20 cells.
- Did not investigate why the SHEU gate blows out for HighRise/MidRise/OtherDwelling (FINDING 1).
  Out of scope for a collector ("collecting, not fixing"), and no script edits are permitted.
- Did not check whether the same SHEU blowout existed in the originally published (pre-T21,
  pre-rebuild) campaign for these same three archetypes - I have no staged copy of that campaign's
  raw per-household hourly output reachable this session, only the small manifest CSVs already
  staged under `T21/ref/`.
- Did not check the warm-up-retry pass (T21 doc design line 25-26: "a separate Phase-B follow-up,
  not automatic"). Not part of A1-A6 as written and not attempted here.
- Did not re-derive the A2-restated redraw myself from scratch (i.e. did not reimplement
  `load_schedules`/`pair_pool`/`_step8_cell_seed` independently) - relied on the selftest's own
  proof that the checker can detect a real defect (section 1) plus the report CSV's 0-mismatch
  count (section 2), which is what this task asked for, not a second independent reimplementation
  of the whole A2 pipeline.
- Did not check disk usage/quota impact of anything on the cluster (no cluster writes were made
  by this task at all - all new files are local, under my scratch directory).
