# T85 — WP10: clean up the merged Supplementary Information — implementation state

Task doc:   this file. Plan: `../00_REVISION_PLAN.md` §3 WP10, plan log (ds)-(dz) BINDING, §5 item 28.
Status:     DONE
Agent:      fresh Sonnet employee, text only. No cluster, no web, no literature search, no python.

## Goal
Make `../manuscript/2J_SI_AE_revised.md` a reader-facing Supplementary Information file: complete,
no internal notes, every Figure/Table S cited from the main text present once. Then rebuild its Word copy.

## Inputs (read these)
- `../manuscript/2J_SI_AE_revised.md` (the file you edit)
- `../manuscript/2J_manuscript_AE_revised.md` (READ ONLY; its SI citations: Results 3.7 cites Figure S1,
  Results 3.8 cites Table S1 and Figure S2, Table S2 cited for clustering intervals)
- `../../tables/SI/Table_B1_B2.md` (content for Table S1), `../../tables/SI/Table_SI_glossary.md`
  (plan §5 item 28 glossary, written by `2026-09-18_T56_si_glossary_table.md`)
- `2026-09-21_T75_wp11_figure8_n200_convergence.md` + `T75_out/` (Figure S1),
  `2026-09-21_T74_wp11_figure9_threshold_sensitivity.md` + `T74_out/` (Figure S2)
- `../manuscript/prep/assembly_log.md` (SI items 12, 13, open items 4, 5, 10; last two manager lines)
- `../manuscript/prep/results_number_sheet.md` and accepted impl docs, only to fill `[VALUE PENDING]`

## Edits to make (only these)
1. **Table S1.** Replace the SI S1 sentence at about line 30 (the one saying the content is NOT merged)
   with a normal pointer, and insert the reader-facing content of `Table_B1_B2.md` as Table S1 (and
   Table S1b / next S number if B2 is a separate table; renumber S-tables in order of first appearance
   and fix every SI cross-reference). Check it agrees with main-text Results 3.8 ("Four candidate models
   clear all four selection checks"; scores 0.0191, 4.57, negative 2.03, 0.6355). Report any disagreement,
   do not resolve it.
2. **Glossary.** Add `Table_SI_glossary.md`'s table as one SI table (next free S number), placed near
   the start of the SI with a one-line lead-in. Main-text edits are NOT yours: if the main text should
   point to it, list that under Next.
3. **Figures S1 and S2.** Add a final section `## S11 Supplementary figures` with:
   `![](../impl/T75_out/fig08_n200_convergence.png)` + caption **Figure S1.** and
   `![](../impl/T74_out/figures/fig09_threshold_sensitivity.png)` + caption **Figure S2.**
   Write each caption from what the T75 / T74 docs say the figure actually plots (axes, cells, city,
   sample sizes, number of scenarios), consistent with Results 3.7 / 3.8. Re-derive every number in a
   caption from the impl doc or its CSV; if the doc and the main text disagree (e.g. "24 cells" vs
   "Montreal only"), write what the artifact shows and report the disagreement.
4. **`[VALUE PENDING]` (3, in S6 x2 and S8 x1).** Fill only if the exact value is stated in an accepted
   impl doc or `results_number_sheet.md`; write the source path in the log. Otherwise leave it.
5. **Internal notes out.** Remove or rewrite:
   - "forecast" in S7 (about lines 196-197): use "2030 scenario year" / "2030 scenario".
   - em/en dashes (about lines 256, 318, and any other): rewrite with commas, colons or parentheses.
     Double hyphen `--` used as a dash also counts; replace it.
   - "(T21, ...)" at about line 322: say "rebuilt 2022/2030 paired simulation data" with no task ID.
   - "(Ruling 5)" and the sentence "Whoever writes the main-text point estimate ..." in S10: delete.
   - The whole paragraph "A retired, invalid number, kept only for the record (DO NOT QUOTE)" in S10:
     delete from the SI (it stays in `draft_SI_clustering_ci.md`). Its 1.0-3.3 % figure must not appear
     anywhere in the SI (plan item 39).
   - Any remaining task IDs (T-numbers), gate IDs, script names/paths (`.py`, `impl/`), "plan §",
     "FINDING", "COLLECT_MODE", bare "J3" without its gloss, "Tier-1", and the word "failure" in prose.
   Do NOT rewrite other prose, and do NOT change any number other than filling item 4.
6. **Rebuild Word.** From `../manuscript/`, run:
   `pandoc 2J_SI_AE_revised.md -f markdown -t docx --resource-path=. -o 2J_SI_AE_revised.docx`
   Record any pandoc warning. Confirm with `unzip -l 2J_SI_AE_revised.docx | grep media/` that 2 images
   are embedded.

## Checks (record results under Verified)
- grep the SI .md for: `—`, `–`, ` -- `, `forecast`, `failure`, `\bT[0-9]+\b`, `impl/T` (allowed only in
  the two image lines), `\.py`, `plan §`, `Ruling`, `DO NOT QUOTE`, `1.0-3.3`, `VALUE PENDING`, `J3`.
- every Figure S / Table S cited in the SI or the main text exists exactly once as a caption.
- `wc -w` of the SI before and after.

## Rules
Edit only `../manuscript/2J_SI_AE_revised.md`, its `.docx`, and this doc. Append one line per edit to
`../manuscript/prep/assembly_log.md` under a new heading `## T85 SI clean-up` (where, before, after, why).
Fill Verified / Decisions / Next / WHAT I DID NOT VERIFY, set Status DONE, end the turn.

## Ledger
(no cluster jobs)

## Verified
- Full grep sweep of `2J_SI_AE_revised.md` for every pattern in the task doc's "Checks" section: zero
  hits for em/en dash, ` -- ` (double hyphen as dash), `forecast`, `failure`, `\bT[0-9]+\b`, `.py`,
  `plan §`, `Ruling`, `DO NOT QUOTE`, `1.0-3.3`. `impl/T` found exactly twice, both the two allowed
  image lines in the new S11 section. `VALUE PENDING` found 3 times (S6 x2, S8 x1), all pre-existing,
  none fillable from any source read (see assembly_log.md item 13), left as-is by design. `J3` found 3
  times, all glossed in place (Table S1's own gloss, Table S3's glossary row).
- Every Figure S / Table S cited in the main text (`Table S1`, `Table S2`, `Figure S1`, `Figure S2`, at
  `2J_manuscript_AE_revised.md:830-853`) has exactly one matching caption in the SI file, confirmed by
  grep of both files.
- `wc -w`: SI file 4,470 words before edits, 6,438 words after.
- `pandoc 2J_SI_AE_revised.md -f markdown -t docx --resource-path=. -o 2J_SI_AE_revised.docx`: exit 0,
  zero warnings printed. `unzip -l 2J_SI_AE_revised.docx | grep media/` shows exactly 2 embedded PNGs.
- Table S1's four selection-check numbers (0.0191, 4.57, ~2.03, 0.6355) checked against main-text
  Results 3.8's own numbers (0.0191, 4.57, negative 2.03, 0.6355): no disagreement found.

## Decisions
- Table numbering: Table S1 = generator model card (main-text-cited, unchanged label), Table S1b =
  activity codebook (uncited, task doc's own suggested naming), Table S2 = clustering CI (unchanged,
  main-text-cited, could not be renumbered since the main text is read-only), Table S3 = glossary
  (next free integer S number, placed physically near the start of the document even though its number
  is not in strict page-order sequence with Table S2 -- flagged for the manager/author to confirm).
- Kept `COLLECT_MODE` and related technical labels inside Table S1 (data-dictionary table) and Table S3
  (the glossary itself); item 5's "COLLECT_MODE out of prose" instruction was read as applying to the
  SI's flowing S1-S10 narrative text, not to the two technical tables items 1-2 explicitly direct this
  task to merge in -- consistent with the Checks-section grep list, which does not require `COLLECT_MODE`
  or `Tier-1` to be zero.
- Did not carry in Table_SI_glossary.md's own "Terms considered and left out" / "Note for whoever
  assembles" sections; item 2 asks for "the table" (singular), and those sections are drafting-process
  commentary, analogous to the trailer sections T84 already excluded elsewhere in this SI.

## Next
- Manager: rebuild the SI `.docx` with the Building Simulation-era reference-doc template (the richer
  build already used for the main manuscript), once this clean-up is reviewed.
- Manager/author: confirm or override the Table S3 numbering choice, and the "24 cells" vs "Montreal
  only, 24 cell-by-metric combinations" wording gap flagged in Figure S1's caption and in
  `assembly_log.md` (main text is read-only in this task, so it was not edited).
- Author/cluster: the three `[VALUE PENDING]` items (S6 x2, S8 x1) still need a real run to produce a
  number, or a decision to drop those sentences.

## WHAT I DID NOT VERIFY
- Did not re-derive any number in Table S1/S1b from `Table_B1_B2.md`'s own underlying sources (raw
  training logs, CSVs); copied the table's own already-stated numbers verbatim, per the task's "text
  editing only" scope (no python, no cluster).
- Did not verify Table B2's five "check source" raw-code-magnitude cells against any raw file; carried
  them unchanged as inherited placeholders (flagged in assembly_log.md, not resolved).
- Did not verify the glossary's cross-references to "Table C1", "Table C2" and "Appendix D" (from
  `Table_SI_glossary.md`'s original rows for `gate`, `PASS/WARN/INFO/FAIL`, `Tier 1-4`, `FailSafe`,
  `occACT`, `Step-8/Step-9`) since those tables do not exist anywhere in this merged SI file; reworded
  the "where used" column to point at this SI's own generic "supplementary scorecard/match-coverage/
  harmonisation tables" language instead of a nonexistent table label, rather than leave a dangling
  cross-reference -- this is a wording choice, not a verified fact about where those tables will
  eventually live.
- Did not check the rebuilt `.docx`'s visual layout/pagination (fonts, spacing, table breaks) beyond
  the image-count check the task doc asked for; the manager's later reference-doc rebuild is expected
  to change that layout again.
- Did not check whether Applied Energy's own SI house style expects a different table/figure numbering
  convention (e.g. "Table S1a" instead of "Table S1b") than the one chosen here.
