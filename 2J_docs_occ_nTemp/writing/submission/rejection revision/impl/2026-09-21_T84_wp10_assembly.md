# T84 — WP10: assemble the revised manuscript into one file — implementation state

Task doc:   this file. Plan: `../00_REVISION_PLAN.md` §3 WP10, plan log (ds)-(dx) BINDING.
Status:     DONE
Agent:      fresh Sonnet employee, text only. No cluster, no web, no literature search.

## Output
1. `../manuscript/2J_manuscript_AE_revised.md`: one file, in this order: title (use the plan's candidate,
   Title option 1 in `draft_S1_introduction.md`, marked `[TITLE: AUTHOR TO CONFIRM]`), Abstract,
   Highlights, Keywords, 1 Introduction, 2 Proposed modelling and simulation framework, 3 Results,
   4 Discussion, 5 Limitations (from `draft_S7_limitations.md`, renumbered), 6 Conclusion, References.
2. `../manuscript/2J_SI_AE_revised.md`: the three SI drafts merged, SI sections numbered S1, S2, ...
   in one sequence, figure/table labels `Figure S1`, `Table S1`, ...
3. `../manuscript/prep/assembly_log.md`: every change made while assembling (one line each: where,
   before, after, why), plus the trailer sections listed below.

**Copy section text as it stands. Do not rewrite prose.** The only edits allowed are the ones below.
Every draft's own trailer (trace tables, open items, WHAT I DID NOT VERIFY, manager addendum) is NOT
copied into the manuscript; it stays in the draft files.

## Allowed edits
1. Section numbers and every cross-reference ("Section 7" -> "Section 5", "S7" -> "5", subsection
   numbers inside Limitations, "Section 2.x" pointers checked against the framework draft's own headings).
2. **Figure numbering.** Build ONE main-text figure list in order of first mention. Figure 1 is the
   workflow diagram (`../../figures/Figure_01_workflow.png`, never `Figure_01_pipeline.png`). Check what
   figures `draft_S2_framework.md` references and whether each has a file in `../../figures/`. The Results
   placeholders (`[R1-athome]`, `[R3-enduse]`, `[R4-loadshape]`, `[R4-peak-lf-ramp]`, `[R4-heatmap]`,
   `[R5-comparison]`, `[R6-measured]`) map to WP11 outputs: `impl/T76` (fig01), `impl/T71_out` (fig02-05),
   `impl/T77*` (fig06), `impl/T73_out` (fig07); `[R7-n200-SI]` and `[R8-threshold-SI]` go to the SI
   (`impl/T75*`, `impl/T74_out`). Write the final number in the text and a table in `assembly_log.md`:
   Figure N, caption source, image file path (or `FILE NOT FOUND`). Do not move or copy image files.
3. **Tables.** Same treatment: Table 1 is the Introduction's comparison table; number the rest in order.
4. **Introduction 1.5 `[NUMBER FROM RESULTS]` placeholders (4).** Fill one only if the number is stated
   in an accepted draft or an accepted impl doc; write the source in the log. Otherwise leave the
   placeholder. The "agreement across dwelling-by-year cells" one is "48 of 48 cells within the plus or
   minus 15 percent band around the fitted target" (Results 3.3). Households: 50 per cell, 24 cells
   (check Results/framework wording). Do not guess the architecture count or run count.
5. **References.** One list, author-year style, built from the archive's reference list
   (`../../archive/2J_manuscript_submission.md` from line ~491, read only), keeping ONLY works cited in the
   assembled text, plus any work the drafts cite that the archive list lacks marked
   `[REFERENCE ENTRY NEEDED]`. List every `[CITATION NEEDED]` left in the text in the log.
6. Remove duplicated sentences that appear in two sections only if word-for-word identical; log each.

## Checks to run on the assembled main file (record results in the log)
- no "forecast"; no em or en dash (byte-level); no "failure"; no internal labels (J3, S-None, S-Full,
  S-Partial, S-Revert-std as bare words outside parentheses, T-numbers, gate IDs, COLLECT_MODE, Tier-1);
- no retired number: "2.2 to 3.9", "5.2 p", "17.0-17.7", "2.7 %", "2.7 percent", "6,000", "64,061",
  "144,507";
- every figure and table number cited in text exists, and each exists once;
- word count of the main text (Abstract to Conclusion, excluding references).

## Trailer in `assembly_log.md`
`## Figure list`, `## Table list`, `## Placeholders still open`, `## CITATION NEEDED list`,
`## Check results`, `## Open for the manager`, `## WHAT I DID NOT VERIFY`.

## Rules
Edit no other file (the draft files stay as they are). Fill Verified/Decisions/Next, set Status DONE.

## Ledger
(no cluster jobs)

## Verified
- All three outputs written: `manuscript/2J_manuscript_AE_revised.md` (1,143 lines; main-text word
  count Abstract-to-Conclusion, References excluded, = 12,076 words, `wc -w`), `manuscript/2J_SI_AE_revised.md`
  (356 lines, 4,470 words), `manuscript/prep/assembly_log.md` (this task's full log, with Figure
  list, Table list, Placeholders, CITATION NEEDED list, Check results, Open for the manager, WHAT I
  DID NOT VERIFY trailers).
- Section order confirmed by `grep -n "^# "`: Title, Abstract, Highlights, Keywords, 1 Introduction,
  2 Framework, 3 Results, 4 Discussion, 5 Limitations, 6 Conclusion, References -- matches the task
  doc's Output #1 order exactly.
- 30 references kept (all found in the archive, zero `[REFERENCE ENTRY NEEDED]` needed); 6
  `[CITATION NEEDED]` tags remain (3 Introduction, 3 Discussion, not word-for-word duplicates).
- Figures 1-8 and S1-S2 each have exactly one caption and consistent in-text numbering (PASS).
  6 of 10 cited figures have no local image file (FILE NOT FOUND: Figures 2, 7, 8, S1) -- see
  assembly_log.md's Figure list for exact search paths.
- Checks run on the assembled main file: no-forecast FAILS (3 occurrences, all negated "not a
  forecast" usage, not fixed -- out of this task's edit scope); no em/en dash PASS; no "failure"
  PASS; no bare internal labels PASS (S-None/S-Partial/S-Revert-std all sit inside parentheses); no
  retired numbers PASS.

## Decisions
- Figure numbering: used each figure's own "home" section (matching the pre-existing WP11 file-name
  numbering fig01-fig09) rather than a literal first-mention scan, since Section 3.2 forward-references
  Section 3.4's own figures before Section 3.3's figure is introduced. Flagged for manager confirmation.
- Excluded each of the three SI drafts' own opening provenance paragraph (internal task IDs, drafting
  process notes) from the merged SI file, judged as trailer-like metadata rather than reader-facing
  content. Not an explicit task-doc instruction; flagged.
- Did not merge `tables/SI/Table_B1_B2.md` (the actual content behind "SI Table B1") into the SI
  file, since it is not one of "the three SI drafts" named for merging; only renumbered its label to
  Table S1 and flagged the gap.
- Filled 2 of 4 Introduction 1.5 `[NUMBER FROM RESULTS]` placeholders (households-per-panel = 50; SHEU
  agreement = "48 of 48 cells within the plus or minus 15 percent band"), left the architecture-search
  and simulation-run counts open per the task doc's explicit "do not guess" instruction.

## Next
- Manager to resolve assembly_log.md's "Open for the manager" list, in particular: source the 4
  missing figure images (Figures 2, 7, 8, S1) from the T76/T77/T73/T75 cluster outputs; rule on the
  "no forecast" check's FAIL; decide whether/how to fold Table_B1_B2.md's content into Table S1; and
  confirm the figure-numbering judgement call.
- After that: thread `response_map.md` (per plan log (dx)), then WP13 (Applied Energy house-style /
  submission checks).

## WHAT I DID NOT VERIFY
- Did not independently re-derive or re-check any number copied from the ten source drafts; every
  figure was carried over exactly as the accepted draft stated it (task's "copy text as it stands"
  rule), not re-derived from a raw CSV or cluster output.
- Did not verify image files exist anywhere outside the local `rejection revision/impl/` and
  `../figures/` trees (e.g. on the cluster); searched only the local project tree.
- Did not open `tables/SI/Table_B1_B2.md`'s content, only confirmed the file exists on disk.
- Did not check the assembled files against Applied Energy's own house-style/submission rules beyond
  the mechanical checks this task doc itself lists (that is WP13's job, per plan log (dx)).
- Did not verify reference DOIs/URLs resolve; copied byte-for-byte from the archive.
- Ran the required checks only on the main manuscript file, per the task doc's scope; the same greps
  were run informally on the SI file and logged, but the SI file was not itself a required check
  target.
