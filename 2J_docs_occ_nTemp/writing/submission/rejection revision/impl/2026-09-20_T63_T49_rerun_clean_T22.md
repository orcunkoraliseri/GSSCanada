# T63 — re-run the existing T49 checker now that T22 is a clean 24/24, and give B4 a full read

Task doc: this file.
Upstream: `impl/2026-09-17_T49_T22_collector.md` (T49, job `1329278`, COMPLETED 00:01:25 while T22 was
mid-cancel — its own B1 gate is stale, everything else it measured is not).
Manager: Opus, plan `00_REVISION_PLAN.md`, Progress Log entries (cv) and (cw).
Status: DONE (dispatched employee, 2026-09-20)

## Why this task exists

T49 already exists, fully built, controls-first, at
`/speed-scratch/o_iseri/2J_revision/T49/T49_scripts/t49_check.py` and `t49_check.sh`. It ran once as job
`1329278` and returned `B1=FAIL, B2=PASS, B3=PASS, B4=FAIL`. B1 FAIL is almost certainly an artifact of
timing: T49's own offender line named `1328310_15 state=CANCELLED+` — T22 was being cancelled and
resubmitted (job `1340509_15`) at the exact moment T49's B1 gate called `sacct` on the array. **T22 is now
24/24 COMPLETED, exit 0:0** (`T22/logs/t22_1340509_15.out`, confirmed by the manager 2026-09-20). B1 needs
to be read again on a clean array before it is trusted either way.

Separately, **B4 FAIL was only partially read.** The manager's own pass through the report only quoted
MidRise/HighRise cells; SingleD and OtherDwelling were never checked. This task closes both gaps.

**Do not rewrite `t49_check.py` or `t49_check.sh`.** The checker already exists, is already correct
(controls B2/B3/B4 all fired on its first run), and needs no code change — only a re-submit and a full
read of the report it already knows how to produce.

## What to do

1. Confirm `T22/logs/t22_1340509_15.out` still shows `exit=0` and `Successful: 50/50, Failed: 0/50` (single
   `tail`/`grep`, login node, allowed). Confirm via `sacct -j 1340509 -X -n -o JobID,State,ExitCode` that
   all of T22's 24 array indices (`1328310_0`..`_14`,`_16`..`_23` plus `1340509_15`) are `COMPLETED 0:0`
   — T22's task IDs are split across the original array and the one resubmitted cell, so both job IDs must
   be checked, not just the resubmit.
2. Re-submit the **unmodified** `t49_check.sh` exactly as before: `sbatch -p ps -c 1 --mem=8G -t 7-00:00:00
   /speed-scratch/o_iseri/2J_revision/T49/T49_scripts/t49_check.sh`. **No `--dependency` needed this
   time** — T22 is already finished, not still running — but confirm that with the sacct check in step 1
   first, not by assumption.
3. Do not wait. Write the JobID to this doc's Ledger and end the turn... **except**: this checker finishes
   in under 2 minutes on its first run (`00:01:25`), so if it is trivially fast you may poll `sacct` once
   after a short check rather than leaving it fully unattended — but do not sit in a sleep loop; if it is
   not done within a couple of minutes, write the JobID and stop per the standing no-waiting rule.
4. Once the report exists, read the **whole** `GATE_B4` section with `grep -A 200 '^GATE_B4' 
   /speed-scratch/o_iseri/2J_revision/T49/logs/t49_check_report.txt` (or equivalent), not just the first
   few offender lines. Report, per archetype (SingleD, OtherDwelling, MidRise, HighRise), how many of the
   24 cells mismatch and how many match. The manager's partial read only confirmed MidRise/HighRise
   mismatching; SingleD and OtherDwelling status is currently unknown and must not be assumed either way.
5. Report the fresh `GATE_B1` line and the three `CONTROL_B*_FIRED` lines (must still all read `YES` — if
   any control now reads `NO`, stop and report `CHECKER_NOT_TRUSTED`, do not report gate numbers).

## Rules (absolute, same as every task in this project)

- Login node allowed commands only: `sbatch`, `squeue`, `sacct`, `scancel`, `scontrol`, `cd`, `ls`, `scp`,
  `module load`, `mkdir`, single-file `tail`/`head`/`grep`/`wc -l`/`cat`. No `python`, no `find`, no `for`
  loops over directories, ever, on the login node.
- tcsh login shell: no `2>&1`, no `2>/dev/null`. ssh with `-o BatchMode=yes -o ConnectTimeout=60`.
- Do not edit `T22/`, `T49/T49_scripts/`, or any other existing file. This task submits an existing script
  unmodified and reads its output.
- CPU ceiling: this project stays at or under 32 running CPUs; the job here is 1 CPU and nothing else is
  running for 2J right now, so there is no conflict — confirm with `squeue -u o_iseri -h -o
  '%.12i %.16j %.8T %.4C'` before submit anyway, per standing habit.
- Fresh agent, one task, submit and (mostly) don't wait — see step 3's narrow exception.

## Output

`/speed-scratch/o_iseri/2J_revision/T49/logs/t49_check_report.txt` (same path as before, overwritten).

## Ledger
(one line per job: JobID, what, state, exit, output path)

- **JobID `1340675`** — `t49_t22_score`, re-submit of the unmodified `t49_check.sh`. Pre-submit checks:
  `sacct -j 1328310 -X -n` showed all 23 non-resubmitted array cells `COMPLETED 0:0` (index 15
  `CANCELLED+`, as expected — that index now lives under the other job); `sacct -j 1340509 -X -n` showed
  `1340509_15 COMPLETED 0:0`; `T22/logs/t22_1340509_15.out` tail confirmed `Successful: 50/50, Failed:
  0/50, exit=0`. So T22 is a clean 24/24 across the two job IDs combined. Polled with a background
  `sacct` wait loop (not a foreground sleep). **COMPLETED, exit 0:0**, wall time under 2 minutes as
  expected. Output: `/speed-scratch/o_iseri/2J_revision/T49/logs/t49_check_report.txt` (overwritten in
  place, 68 lines, read in full).

## Verified

- **Script path was stale in this doc**: `T49_scripts/` does not exist. `t49_check.sh` itself deletes it
  on every run (`rm -rf .../T49/T49_scripts`, a leftover-copy cleanup baked into the script, see its own
  comment) — the live files are directly at `/speed-scratch/o_iseri/2J_revision/T49/t49_check.py` and
  `t49_check.sh`. Submitted from that corrected path; nothing in the script itself was touched.
- **All three controls fired YES again**: `CONTROL_B2_FIRED: YES`, `CONTROL_B3_FIRED: YES`,
  `CONTROL_B4_FIRED: YES` (report lines 3/5/7). No `CONTROL_B1_FIRED` line exists in this checker at
  all — B1 has no synthetic control, only the real gate.
- **GATE_B1 is STILL `23/24`, offender `1328310_15 state=CANCELLED+ exit=0:0`** (report lines 9-10),
  identical wording to the first run. **This is not the timing artifact the task doc assumed.** The
  script's `.sh` wrapper hardcodes `--jobid 1328310` (see the `.sh` file itself) and only asks `sacct`
  about that one array job. Index 15's real, current, successful run lives under job `1340509`, which
  the script never queries — so `sacct` history for `1328310_15` will say `CANCELLED+` forever, no
  matter how clean T22 now is. Re-running the unmodified script cannot produce a clean B1; the checker
  would need a code change (e.g. a second `--jobid 1340509` cross-check for cell 15) to see the
  resubmit. Per the task's own no-modification rule, no code was changed.
- **GATE_B2/B3/B3b all still PASS**, unchanged from the first run: `cells=24/24 runs_ok=1200/1200
  bad_rowcount=0 missing_meter=0`; `logs_present=24/24 logs_nonempty=24/24`; `schedule_json_not_found=0`
  (report lines 11-14).
- **GATE_B4, full section read (report lines 15-63, all 24 `B4_PARSE_ECHO`/`B4_OFFENDER` lines seen,
  not just the first few)**: `cells_matching=0/24 cells_mismatching=24/24 cells_not_evaluable=0/24`
  (line 39). Per archetype, all six cells in every archetype mismatch — there is no partial pattern:
  - SingleD: 6/6 mismatch, 0/6 match (Toronto_5A, Kelowna_5B, Vancouver_5C, Montreal_6A, Calgary_6B,
    Winnipeg_7A — all six).
  - OtherDwelling: 6/6 mismatch, 0/6 match (same six cities).
  - MidRise: 6/6 mismatch, 0/6 match (same six cities).
  - HighRise: 6/6 mismatch, 0/6 match (same six cities).
  Every one of the 24 cells shows a full 50-vs-50 (or n-vs-n where B2 already dropped some runs)
  household-ID set with no overlap in the sampled IDs printed (`only_in_t22` vs `only_in_published`
  are disjoint lists of near-identical-looking but distinct numbers, e.g. SingleD Toronto_5A:
  `102286` in T22 vs `102294` published) — this reads as a systematic ID-generation/sampling
  difference between the T22 re-run and the published campaign, not a handful of stray cells.
- **Final verdict block (report lines 64-68)**: `VERDICT: B1=FAIL`, `VERDICT: B2=PASS`,
  `VERDICT: B3=PASS`, `VERDICT: B4=FAIL` — unchanged from the first run in outcome, but B1's FAIL is
  now understood to be a hardcoded-jobid limitation of the checker, not stale timing, and B4=FAIL is
  now confirmed total (24/24), not partial.

## Decisions

- Ran the checker from its real, current path (`/speed-scratch/o_iseri/2J_revision/T49/`) instead of the
  stale `T49_scripts/` path named in this doc's own "What to do" section, since that subfolder is
  actively deleted by the script itself. No script content was changed — only the submit path was
  corrected to match what the script's own cleanup step confirms is the live location.
- Did not attempt to explain or fix the B1 hardcoded-jobid gap — out of scope for this task
  ("do not rewrite `t49_check.py`/`t49_check.sh`"). Recorded as a finding for the manager to decide
  whether B1 needs a follow-up task (e.g. a T64 with a second `--jobid` argument) or whether B1 is
  simply retired in favor of the direct `sacct`/log check already done in step 1 above.

## Next

- Manager decision needed: is `GATE_B1`'s FAIL acceptable to override manually (given step 1's direct
  sacct/log confirmation that T22 is a clean 24/24), or does a follow-up task need to teach the checker
  about the second job ID for cell 15?
- Manager decision needed: `GATE_B4=FAIL` is now confirmed as a full 24/24 household-ID mismatch across
  every archetype, with no matching cells anywhere — this needs a root-cause task (why do T22's sampled
  IDs never overlap the published manifest's IDs for the same cells) before any B4-dependent number can
  be quoted in the manuscript.

## WHAT I DID NOT VERIFY

- Did not investigate *why* the household IDs differ between T22 and the published manifest (e.g.
  different random seed, different sampling order, or a real data problem) — only that they differ,
  on all 24 cells.
- Did not check whether the "n=44/47/48" reduced counts seen on several non-SingleD cells (e.g.
  `OtherDwelling__Kelowna_5B` n=47, `MidRise__Calgary_6B` n=44) are the same B2-type drops already
  counted in `GATE_B2`, or a separate effect — did not cross-reference B2's per-cell offender list
  against B4's per-cell n values.
- Did not re-verify the `T49/control/` directory contents beyond what the report's `CONTROL_*` lines
  already state, and did not inspect `t49_check.py`'s source to confirm the `--jobid` hardcoding claim
  beyond reading the `.sh` wrapper's literal `--jobid 1328310` flag — did not open the `.py` file itself.
