# sweep_G1.yaml — single-arm sweep driver for G1 full training
# Feeds configs/G1.yaml into the job-array machinery (submit_step4_array.sh).
# Submit: bash Speed_Cluster/submit_step4_array.sh configs/sweep_G1.yaml G1_$(date +%Y%m%d_%H%M)
tags:
  - G1
