# WP0 task: freeze and inventory the 5J data (two employees, A and B)

Written 2026-09-22 by the manager. Design context: `5J_docs_occ/5thJ_01_Methods_Design.md`, sections
3, 4 and 5 (WP0). You are an employee: execute, measure, record. Make no design choice. If a choice is
needed, write it under "Decisions" as an open question and carry on with the rest.

## Shared rules
* Data goes in `C:\Users\o_iseri\Desktop\GSSCanada\_5J_data\` (create it; outside the repo). Never put
  data files inside `GSSCanada-main`.
* Record the sha256, byte size, download URL and download time of every file you keep.
* Local Windows machine. Use `py -3` (never `python`). Never print a large file into your context:
  use `wc -l`, `head`, pandas with `usecols`/`nrows`, or a script that prints only counts.
* Montreal's portal returns 403 to plain curl; send a browser User-Agent and a Referer header.
* Every number you write must be one you read from a file or page in this task. Quote licence text
  verbatim with the URL and date. If a page or file cannot be reached, write NOT REACHED with the
  status code; do not guess.
* Write state to your own impl file AS YOU GO (not at the end), using the section layout below.
  Finish with a "WHAT I DID NOT VERIFY" section and end your turn. Do not wait on anything.

## Employee A: permits, assessment roll, linkage
Impl file: `5J_docs_occ/impl/2026-09-22_wp0A_permits_roll.md`
1. Montreal permits CSV (`https://donnees.montreal.ca/dataset/permis-construction`, main CSV):
   download, checksum, row count, columns, date range of `date_emission`.
2. Toronto: Active Permits, Cleared since 2017, Cleared 2000 to 2016 (links in
   `5J_docs_occ/5thJ_00_Kickoff_Note.md`, "Permit file check"). Download, checksum, row counts.
   De-duplicate across the three files by `PERMIT_NUM` + `REVISION_NUM`: report the unique count and
   how many rows appear in more than one file. Count residential rows (STRUCTURE_TYPE rule described
   in the kickoff note) after dedupe. Count DESCRIPTION strings that start with "HVAC -" and the
   median length of DESCRIPTION after that prefix is stripped.
3. Toronto licence: fetch the CKAN `package_show` for both datasets and the portal licence page
   (`https://open.toronto.ca/open-data-license/`); quote what each says about licence.
4. Montreal assessment roll: find the "unités d'évaluation foncière" dataset on donnees.montreal.ca.
   Download the main file (CSV or GeoJSON). Record: licence, row count, all column names, and fill
   rate of the construction-year, dwelling-count, building-category and any coordinate or geometry
   field. Tabulate construction year into: before 1960, 1960 to 1989, 1990 and later, missing.
5. Linkage test on Montreal residential permits (the kickoff note's "resident"/"habitation" rule):
   if the roll has coordinates or geometry, share of permits with coordinates whose nearest roll unit
   is within 15 m (and within 30 m); if not, share joined by normalised address (civic number +
   street name). Report the share joined, and among joined permits the vintage-band split.

## Employee B: area truth, StatCan, existing building models
Impl file: `5J_docs_occ/impl/2026-09-22_wp0B_areatruth_models.md`
1. EnerGuide open data (NRCan, open.canada.ca; search "EnerGuide Rating System" housing data). Find
   the per-home file(s). Download, checksum. Record: licence (quoted), row count, date range, all
   column names, and the exact columns that describe heat pump, air conditioning, primary heating
   system or fuel, and insulation (ceiling, walls, basement). For the postal area column: number of
   distinct FSAs starting with H (Montreal region) and with M (Toronto); rows per FSA (min, median,
   max); how many H and M FSAs have at least 30 homes. If the file has both pre- and post-retrofit
   evaluations of a home, say how that is marked and count each.
2. StatCan tables 38-10-0019-01 and 38-10-0286-01: download the full-table CSVs from
   `https://www150.statcan.gc.ca/n1/tbl/csv/<8-digit id>-eng.zip`. Save a small extract with only the
   rows for Montreal, Toronto, Quebec and Ontario, to
   `_5J_data\statcan_extract_38100019_38100286.csv`. Record which years and which categories exist.
3. Existing EnergyPlus models:
   `GSSCanada-main/2J_docs_occ_nTemp/BEM_setup/Buildings_MTL_v242/` (four IDFs). For each: floor
   area and zone count; heating and cooling system objects (object types and names); window, wall and
   roof constructions with their layers; any infiltration object and its value; which objects a change
   of window, insulation, heating system (baseboard, furnace, heat pump) or air conditioning would
   touch. Read-only: never edit these files. Also read
   `2J_docs_occ_nTemp/BEM_setup/BEM_setup_idf_reader.md` if it helps.

## Impl file layout (both)
```
# <task> - implementation state
Task doc: 5J_docs_occ/impl/2026-09-22_wp0_task.md
Status:   IN PROGRESS | DONE | BLOCKED
## Files kept      <- path, bytes, sha256, URL, time
## Verified        <- numbers actually read, with where
## Decisions       <- open questions for the manager
## Next
## WHAT I DID NOT VERIFY
```

## Employee B2: EnerGuide counts, redone (added by the manager 2026-09-22)
Why: Employee B's EnerGuide numbers were computed while files were still downloading; they are void.
StatCan and the IDF inventory from B stand. Append your results to B's impl file
`5J_docs_occ/impl/2026-09-22_wp0B_areatruth_models.md` under a new heading "## B2 EnerGuide (redo)",
and set its Status to DONE at the end. Scripts go in `_5J_data\scripts_wp0\`.
Files: `_5J_data\energuide_2004-2006.csv` and `energuide_2007.csv` ... `energuide_2026.csv`
(dictionary `nrcan_open_data_dictionary.xlsx`). They are large (up to 1 GB): read with pandas
`usecols` + `chunksize`, never whole.
1. Per file: bytes, sha256, data rows (count with the csv parser, not `wc -l`, as text fields may hold
   newlines; report both), min and max `ENTRYDATE`. First confirm every file is complete: its size
   equals the last "HTTP 200 bytes" line for that year in `_5J_data\download_log.txt`.
2. `EVALTYPE`: counts of each value overall. D/E pairs: how many `EVALUATIONSID` values have both a D
   and an E row. Distinct `HOUSEID` overall.
3. Distinct values with counts (all files, top 30 each) of `AIRCONDTYPE`, `HPSOURCE`, `CCASHP`,
   `FURNACEFUEL`, `FURNACETYPE`, and `CLIENTPCODE` string length. Do not interpret them; list them.
4. FSA (`CLIENTPCODE`, first 3 characters, upper-cased): for FSAs starting with H and with M, the number
   of distinct FSAs, and distinct `HOUSEID` per FSA (min, median, max), and how many have ≥ 30 distinct
   houses. Also the same for H FSAs split by the second character (H1 ... H9).
5. Per FSA (H and M only, ≥ 30 distinct houses): keep each house's LATEST D or E evaluation with
   `ENTRYDATE` ≤ 2022-12-31 (ties: prefer E). Report, per FSA: n houses; share with `HPSOURCE` not
   empty and not "N/A"; share with `CCASHP` = 1; share with `AIRCONDTYPE` not empty and not "Not
   installed" (use the exact strings you found in item 3; if the strings differ, list which ones you
   counted); counts per `FURNACEFUEL` value. Also the same shares for all H and all M houses pooled.
   Write to `_5J_data\energuide_fsa_shares_2022.csv`; record its sha256.
6. Finish with WHAT I DID NOT VERIFY and end your turn. Never wait on a background process.
