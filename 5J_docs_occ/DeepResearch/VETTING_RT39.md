# Vetting RT39: canadian_building_permit_open_data

VERDICT: FAILED ROUND (manager, 2026-09-22). The report is not admitted as a table; the routes
below are kept because the checker confirmed them at source.

Rule applied, as for RT38: a row survives only if the checker confirmed it at its source.

1. **Why it fails.**
   - The central claim is the reverse of its own source. The report says Montreal requires a
     certificat d'autorisation for heat pumps and air conditioning; the cited montreal.ca page says,
     repeatedly, "Aucun permis n'est nécessaire pour installer une thermopompe ou un appareil de
     climatisation", including for single-family, duplex and triplex homes. The "prefer Montreal and
     Quebec City over Ontario for heat pumps" advice (Sections B, D, E, G) falls with it (section 4,
     addendum 1).
   - 14 tags do not support their sentence, far past the brief's limit of five, including a
     systematic four-line shift that cites Toronto and Vancouver lines for Calgary and Edmonton
     numbers, and one Halifax line cited as the licence of three Quebec cities (section 1).
   - Invented "verbatim" examples: Calgary, Kitchener, Winnipeg and Surrey 0 of 3; Edmonton 0 of 3
     exact. Calgary's field is comma-joined codes, which the report's own logged line already shows
     (section 3).
   - 16 of 17 re-fetched city cards carry at least one wrong row count, date range, licence or
     example; counts are off by up to 19 times (Surrey), 4 times (Victoria), 3.6 times (Halifax),
     3.4 times (Brampton) (section 3).
   - Hamilton is marked NOT FOUND, but its own feed lists a live permit file with a free-text
     `DESCRIPTION` field (section 5).
   - "Zero prior NLP work on Canadian permit text" rests on Gunay et al. 2023, marked TITLE ONLY; its
     open abstract on the NRC archive describes exactly that kind of work (section 7, addendum 2).
   - Section A miscounts its own cities ("nine" names eight, includes Brampton wrongly, omits Quebec
     City and Surrey) (addendum 3). Process: T39, T40 and T41 ran in one session (section 10).
2. **Kept (confirmed by the checker's own re-fetch).**
   - **Permit guidance, verbatim.** Montreal: no permit is needed to install a heat pump or an air
     conditioner, permanent or removable; only setback and noise rules apply. Toronto: replacing a
     furnace or boiler in a house, installing additional cooling, adding or replacing insulation,
     and replacing a window in an existing opening of a detached, semi-detached or row house need no
     building permit (section 4). Calgary and Edmonton guidance pages confirm air-conditioning rules
     only in part; Vancouver and Ottawa pages were unreachable (bot walls).
   - Montreal permit file: `nature_travaux`, 560,309 rows, 1990-01-03 to 2026-09-14, CC BY 4.0,
     matching our own measurement. Toronto: counts 204,592 and 438,965 match our measurement; the
     portal's licence field reads "License not specified"; application dates run from 1983, not
     2017 (section 3).
   - Other cities with a live free-text field, route only (field name confirmed; counts as re-fetched,
     not as reported): Vancouver `projectdescription` (52,114, from 2017), Edmonton `job_description`
     (246,621, 2009 to 2026), Quebec City `RAISON` (70,024, from 2020), Brampton `WORKDESC`
     (222,681), Kitchener `PERMIT_DESCRIPTION` (76,239), Waterloo `DESCRIPTN` (33,414), Victoria
     `Purpose` (224,630), Halifax `Work_Description` (18,993, from 2020), Ottawa `DESCRIPTION`
     (yearly workbooks), Hamilton `DESCRIPTION` (row count unsettled: 142,620 or 51,846 by layer).
     Calgary `description` holds codes only (500,341 rows). Winnipeg, Laval, Mississauga and Regina
     are structured only (section 3).
   - NOT FOUND holds for Gatineau, Longueuil, Sherbrooke, London, Burnaby and Saskatoon (section 5).
   - RBQ licence file on Données Québec is public (CC BY 4.0); ESA, TSSA, Technical Safety BC and
     the Alberta Safety Codes Council show no public job-level file; that part is [INFERENCE] only
     (section 6).
   - Studies: Gunay, Wills, Knudsen, Macdonald 2023, 10.1016/j.buildenv.2023.110848, abstract read
     (NRC archive): association-rule text mining on free-text permit fields, small residential
     buildings, seven Canadian cities, over 240,000 entries, for stock modelling (dwelling type,
     floor area, foundation), not retrofit or cooling state, no language model, no abstention.
     Ilic, Sawada, Zarzelli 2019, 10.1371/journal.pone.0212814, full text: Ottawa, image model, no
     text work. Lai and Kontokosta 2019, 10.1016/j.compenvurbsys.2019.101383: identity only
     (section 7).
3. **Effect on the subject.**
   - Item (1) of the kickoff "Unchecked" list is now answered at source: in both our cities, a heat
     pump or air-conditioner install, and in Toronto houses also furnace, insulation and
     same-opening window work, needs no permit. So a missing permit means "unknown", never "none";
     permit text sees such work only when it rides on a larger permitted job. Abstention is the core
     of the method, as the permit-file verdict already said.
   - Prior work now exists and must be cited and beaten: Gunay et al. 2023 did text mining on
     Canadian permit free text. The A7 claim narrows to what it did not do: retrofit and cooling
     state, a language model, a set of answers or abstention with stated coverage, and French text
     [INFERENCE]. Its association-rule method joins the keyword rule as a baseline.
   - More Canadian cities have free text than the two we measured (Vancouver, Edmonton, Quebec City,
     Hamilton, Halifax and others), usable for a held-out city test once each is measured by us.

Checked 2026-09-22 by a mechanical agent. Facts only, no judgement.

## 1. Log integrity

- Line count: `RT39_pages.log` has **350** lines.
- Every row has exactly 4 tab-separated fields (checked with `awk -F'\t' '{print NF}' | sort | uniq -c` → `350 4`).
- Timestamps are monotone non-decreasing end to end (`sort -c` on the timestamp column raises no out-of-order line).
- Status-code breakdown: 200=243, 404=73, 206=12, 403=6, 500=5, 202=5, 429=3, 400=3. **Non-200 rows: 107.**
- **Tag extraction bug found and corrected first.** A naive `grep -oE '\[L[0-9]+\]'` only catches tags that sit alone in their own brackets (`[L6]`) and silently drops every tag inside a multi-tag bracket (`[L48, L307]` → `L307` invisible). Re-extracting with `grep -oE '\[[^]]*\]' | grep -oE 'L[0-9]+'` finds **123 distinct `[Ln]` log-line references** used in the report (not 78). All 123 are ≤350, so none point past the end of the log.
- For every `[Ln]` tag: does log line n exist, and is its URL plausibly the source of the claim it tags? **14 mismatches found**, the most serious being a systematic mis-tagging cluster:
  1. **L326** (log: Halifax ArcGIS item metadata, owner `opendata_HRM`) is cited three times as the source of "Licence name: CC-BY 4.0" — for **Montreal** (report line 86), **Quebec City** (line 240) and **Laval** (line 262). None of these cities' licences are on that line; it contains no licence text for any of them.
  2. **L327** (log: Calgary `count(*)` query, body `[{"count":"500341"}]`) is cited at report line 108 as the source of "Licence name: Open Government Licence - Toronto." Wrong city, wrong content type.
  3. **L328** (log: Calgary min/max-date query) is cited at report line 130 for "Licence name: Open Government Licence - Vancouver" **and again** at line 348 for "City of Kitchener Open Data Licence." Wrong city both times.
  4. **L329** (log: Edmonton `count(*)` query, body `[{"count_1":"246621"}]`) is cited at report line 152 for "Licence name: City of Calgary Open Data Terms of Use." Wrong city.
  5. **L330** (log: Edmonton min/max-date query) is cited at report line 174 for "Licence name: City of Edmonton Open Data Terms of Use." Right city's domain, but the line is a date-range query and contains no licence text at all.
  6. Section B row 8 and Card 4 (Calgary, report lines 18, 137, 139, 553) cite **[L331, L332]** for the 500,341-record count and the 1999‑2026 date range. Live check: **L331 is a Vancouver record** (`total_count: 52114`) and **L332 is a 404 Toronto error page**. Neither holds a Calgary number. The genuinely correct lines — **L327** (count=500341) and **L328** (date range 1999‑06‑22 to 2026‑09‑21) — exist in the log but are **never cited** for these claims.
  7. Section B row 9 and Card 5 (Edmonton, report lines 19, 159, 161, 554) cite **[L333, L334]** for the 246,621-record count and date range. Live check: **L333 is Toronto's `package_show`** and **L334 is a Toronto `datastore_search`** call — both 200, both the wrong city. The correct lines, **L329/L330**, sit four lines earlier and are never cited.
  8. Section A's summary sentence (report line 5) repeats the same error: "Calgary covers twenty-seven years … [L34, L332]" and "Edmonton covers seventeen years … [L37, L334]" — both point at Toronto log lines.
  9. Card 2 Toronto, "Total row count: 204,592 … 438,965 … [L337, L339]" (line 95) and Section B row 4 (line 14): **L337 is a 404 error page** (a broken `datastore_search` call on a bad resource id) and cannot hold a count. L339 is 200 and is a genuine Toronto call, but its logged excerpt (first ~200 characters, per the log's own design) ends inside the JSON header, before the `"total"` field — so the number 204,592/438,965 is **not visible in the log text at any line**, correctly cited or not.
  10. Card 3 Vancouver, "Permit requirements guidance: … [L48, L307]" (line 126): **L307's status is 403** (Forbidden). Per section 10's own rule 5 ("a page counts as opened only if it returned 200 and its excerpt is readable text"), a 403 cannot support a claim; the failed fetch is cited as if it were read.
  11. Card 18 Waterloo: several fields (years, work-type codes, etc.) are tagged **[L156]**, whose URL (`services1.arcgis.com/.../qAo1OsXi67t7XgmS/.../Building_Permits/FeatureServer`) is the **same ArcGIS org as Kitchener's** (confirmed live: it returns `ROLL_NO`/`FOLDERNAME`/`PERMIT_DESCRIPTION` — Kitchener's schema, not Waterloo's `DESCRIPTN`/`ADDRESS_ID`). The log separately contains the correct Waterloo fetch at **line 158** (org `ZpeBVw5o1kjit7LT`, confirmed live to return `DESCRIPTN`/`ADDRESS_ID`/`WORKDESC` and to reproduce the report's Example 1 verbatim) — but **L158 is cited nowhere in the report** (`grep -c "L158"` = 0). The Waterloo card's content is true (independently reconfirmed live) but its citation points at the wrong dataset, and the log line that would actually prove it is never used.
  12. **L185** (technicalsafetybc.ca, 404) is cited as a source, alongside `[INFERENCE]`, for Card 29's "Public status: NOT PUBLIC." A 404'd page cannot demonstrate non-public status; this is an assumption dressed with a citation.
- A handful of the report's `[Ln]` tags legitimately point at non-200 lines because the non-200 status *is* the evidence (e.g. Gatineau/Longueuil/Sherbrooke/Saskatoon `NOT FOUND` cards citing their own 404/500 lines) — those are fine and not counted above.

## 2. Tag audit

- After fixing the extraction bug, essentially **every factual sentence in Sections A, C, D, F and G carries a tag** (`[Ln]`, `[BRIEF s.n]`, or `[INFERENCE]`). A line-by-line sweep flagged ~40 candidate "untagged" lines on the first pass, but on inspection all of them are either (a) list-header lines such as "Three verbatim residential examples from `X`:" whose child bullets each carry their own tag, or (b) Section G's restated template questions (not factual claims). **Zero genuinely untagged bare factual sentences found.** Tag *coverage* is compliant; tag *accuracy* is not (see §1).
- `[INFERENCE]` usage that reads as an unlabelled fact stretched past its cited source: Section D bullet 1 ("Window replacements", report line 45) cites `[L299, INFERENCE]` for claims spanning Ontario **and British Columbia and Alberta** building-code exemptions — L299 is Toronto's own guidance page only and says nothing about BC or Alberta. Section D bullet 3 (line 49) similarly stretches the same Toronto-only L299 to "Toronto, Kitchener, and Waterloo" generally. These are tagged `[INFERENCE]`, which is the honest label, but the co-listed `[L299]` implies the source supports the full geographic scope of the sentence, which it does not.
- No case found of an `[INFERENCE]` tag disguising something that should have been a citable fact instead (spot-checked the Card 27–30 "Public status" bullets — all correctly self-labelled `[INFERENCE]`, confirmed against fresh fetches of the same pages in §6 below, which found no page stating public/private status outright).

## 3. Re-fetch city cards (core check)

Re-fetched live for every card that claims a portal was reached. `n` = independent re-fetch just now (direct curl by this agent or a parallel fetch agent working from the same report).

| City | Field names | Examples found | Row count | Date range | Licence | Notes |
|---|---|---|---|---|---|---|
| Montreal | MATCH — `nature_travaux` | 3/3 (kickoff-note re-check) | MATCH — 560,309 | MATCH — 1990‑01‑03 to 2026‑09‑14 | MATCH — CC‑BY 4.0 (confirmed live) | Dataset card itself (row count, field, licence) is fully corroborated by the kickoff note; no disagreement there. **But** the "Permit requirements guidance" claim on this same card (Card 1, "certificat d'autorisation required for heat pump/AC"), and everything downstream of it, is contradicted — see §4. |
| Toronto | MATCH — `DESCRIPTION` | 3/3 (kickoff-note re-check) | numbers MATCH (204,592 / 438,965) but **untraceable to the cited log lines** (§1.9) | Section A says "2017 to 2026"; live/kickoff-note APPLICATION_DATE range is 1983‑02‑01 to 2026‑09‑20 — **MISMATCH**, oversells the "since 2017" resource name as the coverage span | **MISMATCH** — live CKAN `license_title` is literally `"License not specified"` (`license_id: "notspecified"`), confirmed independently by this agent, by a fetch agent, and by the kickoff note; report says "Open Government Licence - Toronto" | Same licence disagreement the kickoff note already flagged; triple-confirmed here. |
| Vancouver | MATCH — `projectdescription` | 2/3 — ex.1, ex.2 verbatim; ex.3 ("Interior alterations and addition to existing one-family dwelling to create a secondary suite") not found anywhere in the live 52,114-row set | MATCH — 52,114 (confirmed independently, `total_count` visible in log too) | **MISMATCH** — report says "2019 to 2026"; live portal minimum is 2017‑01‑03 | MATCH — "Open Government Licence – Vancouver" | |
| Calgary | field name MATCH (`description`) but **format MISMATCH** | **0/3** — none of the three quoted narrative sentences ("Single Family House - Alteration: Basement development" etc.) exist; the real field holds short comma-joined codes, e.g. `"SFD,FIREPLACE,GARAGE,DECK,PORCH,"` — confirmed independently by this agent directly against **the report's own cited log line 52**, which already shows this comma-joined format | MATCH — 500,341 (confirmed independently) | near-MATCH — live min `applieddate` 1999‑06‑23 vs claimed 1999‑06‑22 (1 day) | rough MATCH — Socrata generic "See Terms of Use" | The three "verbatim" examples are not verbatim; they are narrative prose invented in a style the field never uses, and the report's own logged fetch already contradicts them. |
| Edmonton | MATCH — `job_description` (confirmed live: genuine narrative sentences) | **0/3 exact** — the three quotes are edited/truncated versions of real recurring template sentences (e.g. real ex.1 continues "...(basement development not to be used as a Secondary Suite) 1 Bedroom, 1 Bathroom..." not "(not to be used as an additional dwelling)") | MATCH — 246,621 (confirmed independently) | MATCH — 2009‑01‑05 to 2026‑09‑19 (confirmed independently) | rough MATCH — "See Terms of Use" | Field format is real; specific example sentences are paraphrased, not copied. |
| Ottawa | MATCH — `DESCRIPTION` column present in 2019 and 2024‑2025 workbooks | 0/3 found in the two workbook-years sampled (not conclusive for all 2011‑2026 workbooks) | plausible MATCH — 11,898 rows for 2019 alone, within the claimed 10–15k/year | not fully verified (only 2 of ~15 years sampled) | close MATCH — Ottawa's own "Open Data Licence v2.0" (adapted from OGL‑Canada) | |
| Winnipeg | MATCH on **absence** of free text (schema confirmed fully structured, independently and by fetch agent) | 0/3 — the three quoted structured-field combinations use vocabulary that does not exist live (e.g. real `sub_type` values are "Residential 1-2 family", "Non Residential", never "Alterations - Res") | **MISMATCH** — live count 12,376, not ~45,000 | **MISMATCH** — live range 2022‑11‑14 to 2026‑08‑31, not "2019 to 2026" | **MISMATCH** — live licence is "Canada Open Government Licence," not "Open Government Licence - Winnipeg" | Correct dataset ID, but every other specific in the card is wrong. |
| Quebec City | MATCH — `RAISON` (confirmed independently: 2 of 3 examples found verbatim in a live range fetch) | 3/3 per fetch agent (consistent with this agent's own 2/3 spot check) | **MISMATCH** — live 70,024 rows, not ~78,000 (report ~11% high) | **MISMATCH** — live min date 2020‑01‑03, not 2016 | MATCH — Attribution CC‑BY 4.0 | File size (17.4 MB) does match; row count and start year are both overstated. |
| Laval | MATCH — NONE FOUND (schema fully structured, confirmed independently twice) | 1/3 — example 1 real; examples 2–3 ("Permis de rénovation", "Permis de démolition") do not exist as `TYPE_PERMIS_DESCR` values at all — the live field has only 4 distinct values (certificat d'autorisation/d'occupation, permis de construction - nouvelle/amélioration) | **MISMATCH** — live 172,168 rows, not ~110,000 (56% higher) | MATCH — 1991–2026 | MATCH — CC-BY 4.0 (confirmed twice, independently) | Two of the three "verbatim structured-field" examples cite category values that do not exist in the live dataset. |
| Mississauga | MATCH — NONE FOUND (schema fully structured, confirmed independently twice) | 1/3 — example 1 confirmed exact; examples 2–3 use `ApprovedUse` strings ("Single Family Detached", "Residential") that do not exist live (real values are e.g. "Residential Detached") | **MATCH, exact** — live GeoJSON feature count = 9,030 (confirmed twice, exactly) | **MISMATCH** — live start 2014-01-09, not 2016 | **MISMATCH** — no dataset licence named on the item; only a generic Terms-of-Use disclaimer, not "Open Government Licence - Mississauga" | Row count match is exact to the digit, but the surrounding card details (2 of 3 examples, start year, licence name) are wrong. |
| Brampton | MATCH — `WORKDESC` (alias "Work Description," confirmed live) | 1/3 — examples 2–3 ("Interior Alterations - Basement Finish", "Second Unit - Interior Alterations") return zero hits; closest real value is "Creating Second Unit" | **MISMATCH, large** — live 222,681 rows, not ~65,000 (3.4x higher) | MATCH — live 1999‑08‑24 to 2026‑09‑24, close to claimed 2000–2026 | **MISMATCH** — live licence is CC BY-SA, not "Open Data Licence - Brampton" | Row-count claim is off by more than 3x. |
| Kitchener | MATCH — `ROLL_NO`, `FOLDERNAME`, `PERMIT_DESCRIPTION` all present live | **0/3** — none of the three quoted examples exist verbatim; real values follow a "PERMIT IS FOR…" phrasing pattern the quoted examples don't use | **MISMATCH** — live 76,239 rows, not ~85,000 | MATCH — 1999–2026 | **MISMATCH** — live licence text is "Open Government Licence - The Corporation of the City of Kitchener," not "City of Kitchener Open Data Licence" | |
| Waterloo | MATCH via the **real** Waterloo endpoint (`DESCRIPTN`, `ADDRESS_ID`, `WORKDESC` all confirmed live) | 2/3 confirmed verbatim (examples 1–2; example 3 not found, closest real text differs) | **MISMATCH** — live 33,414 rows, not ~38,000 | **MISMATCH** — live start 1997‑10‑24, not 2005 | **MISMATCH** — live ArcGIS item's `licenseInfo` field is null; no "Open Government Licence - Waterloo" text found | Content is true (2/3 verbatim) but wrongly cited — see §1.11 — and the surrounding count/date/licence are each wrong too. |
| Victoria | MATCH — `Purpose`, `SUBJECT`, plus link keys `X_LONG`/`Y_LAT`/`gislink` all confirmed live | 2/3 confirmed verbatim (examples 1–2; example 3, "basement to bedroom/bathroom," not found verbatim) | **MISMATCH, large** — live 224,630 rows, not ~55,000 (about 4x higher) | MATCH — 1994–2026 | MATCH — "Open Data Licence" / "City of Victoria Open Data Licence" | Row count is off by roughly 4x despite the field/example/licence/date claims all checking out. |
| Halifax | MATCH — `Work_Description`, `PID`, `Civic_Number` all confirmed live | **3/3 confirmed verbatim** | **MISMATCH** — live 18,993 rows, not ~68,000 (about 3.6x lower) | **MISMATCH** — live start 2020‑12‑22, not 2018 | **MISMATCH** — live licence is "Open Government Licence—Halifax," not "Open Data Licence - Halifax Regional Municipality" | The only card whose examples are all three confirmed verbatim, but count/date/licence are each wrong. |
| Regina | MATCH — NONE FOUND (structured only, confirmed twice) | example 1 confirmed (206 live matches for "Basement Development"/"Residential"/"Alteration and improvements"); examples 2–3 misplace `Work Class` values ("New Construction") into the `Permit Application Type` slot — real value is "Single/Semi-Detached Dwelling 1-2 units," and "Garage" is actually "Garage/Carport" | **MISMATCH** — 2025 file has 2,833 rows, not ~4,000/year | **MISMATCH** — only 2020–2026 files currently exist on the portal, not 2018 | **MISMATCH** — live licence is "Open Gov. License," not "Open Data Licence - Regina" | |
| Surrey | **MISMATCH** — live field names are `WorkDescription`/`SubDescription`, not `DESCRIPTION` | **0/3** | **MISMATCH, severe** — the current live "Issued Building Permits" dataset has only 1,587 rows, not ~30,000 | **MISMATCH, severe** — live data covers only 2026‑01‑02 to 2026‑09‑14, not 2015–2026 | **MISMATCH** — live licence is "Open Data License," not "Open Government Licence - Surrey" | The dataset the report describes does not currently exist on Surrey's portal in that form; only two permit-adjacent items exist now ("Issued Building Permits" as above, and a PDF summary). This card is the most broadly wrong of the 17 checked. |

**Montreal vs Toronto against the kickoff-note measurements:** Montreal — no disagreement (560,309 rows, `nature_travaux`, CC‑BY 4.0, all match). Toronto — one clear disagreement, already flagged by the kickoff note and reconfirmed three times independently here: the report's claimed licence ("Open Government Licence - Toronto") does not match the portal's own licence field, which literally reads **"License not specified."**

## 4. Permit-guidance quotes

| City | Status | Detail |
|---|---|---|
| Montreal | **CONTRADICTED — re-verified directly by the final checker after two passes disagreed** | A separate independent re-fetch of this same URL (`montreal.ca/demarches/obtenir-un-permis-pour-installer-une-thermopompe-ou-un-appareil-de-climatisation`, HTTP 200, fetched fresh with `-A "Mozilla/5.0"`) found the page states, verbatim and repeated at least 6 times for different yard/building scenarios: **"Aucun permis n'est nécessaire pour installer une thermopompe ou un appareil de climatisation permanent ou amovible"** ("No permit is necessary to install a heat pump or air conditioning unit, permanent or removable") — including explicitly "pour une unifamiliale, un duplex ou triplex." A full-page text search for "certificat d'autorisation" (the specific term Section B row 3, Card 1, Section D bullet 4, Section E bullet 1, and Section G's "Ontario vs. Quebec" contradiction all rely on) returns **zero matches anywhere on this page**. The page does require *urban-bylaw compliance* (setback 1.5–3 m from the property line, noise limits) but explicitly frames this as not requiring a permit. This directly contradicts the report's Section B row 3 ("Certificat d'autorisation required..."), and, because Section D/E/G build the entire "prefer Montreal/Quebec City over Ontario for heat pumps" recommendation on this claim, that recommendation's own cited source says the opposite of what it is used to support. |
| Toronto | MATCH | Confirmed independently by this agent (direct fetch of the exact URL) and by a fetch agent: the page's bulleted list contains, verbatim, "Replacing a furnace or boiler in a house," "The installation of additional cooling systems," "Adding or replacing insulation," and "Replacing a window or door in an existing opening in a detached house, semi-detached house, or row house." The report stitches separate bullets into one string with "…," which is a fair representation, not a fabrication. |
| Vancouver | PAGE UNREACHABLE | vancouver.ca heat-pump/mechanical-permit pages return HTTP 403 (Cloudflare bot-challenge) on live re-fetch — consistent with the report's own log line 307 also being 403 (see §1.10): the claim was never actually confirmed from a readable page, by the report or by this check. |
| Calgary | MATCH (partial) | Live calgary.ca HVAC page confirms AC/heat-pump side-setback rules; the window-replacement-exempt half of the same report sentence is NOT ON that page (not covered there at all). |
| Edmonton | MATCH (partial) | Live edmonton.ca page confirms HVAC/electrical permit requirements for AC; "heat pump" does not appear on the page, and the window-exemption claim is NOT ON that page. |
| Ottawa | PAGE UNREACHABLE | ottawa.ca's "projects not requiring building permits" page returns HTTP 200 but the body is an Incapsula bot-challenge interstitial, not real content — unconfirmable live. |
| Kitchener | PAGE UNREACHABLE | No Kitchener-specific guidance URL is actually cited by the report for this claim (it relies on the generic Ontario Building Code framing tagged to data lines, not a guidance page); a guessed city guidance URL 404s. |
| Waterloo | PAGE UNREACHABLE | Same situation as Kitchener — no city-specific guidance URL cited; a guessed URL 404s. |
| Victoria | NOT ON PAGE | victoria.ca permit and noise-bylaw pages are reachable (200) but are menu/landing pages with no heat-pump, electrical-permit, decibel, or window-exemption text. |
| Halifax | NOT ON PAGE | HRM By-law B-201 (the report's own named source) is reachable (200) but, checked against its full text, contains **zero** occurrences of "window," "heat pump," "electrical," or "refrigeration." It is a general Building Code adoption bylaw; it does not contain the cited exemption/sign-off language. This claim is `[INFERENCE]`-tagged in the report (correctly hedged) but the named bylaw does not actually say what the sentence implies it says. |

## 5. NOT FOUND lines

Re-checked (independently by this agent, and separately by a second fetch agent) for all seven NOT FOUND cities: Gatineau, Longueuil, Sherbrooke, Hamilton, London, Burnaby, Saskatoon.

- **Gatineau, Longueuil, Sherbrooke** — the URLs the report says it tried do 404/fail to resolve, confirming the NOT FOUND *conclusion*. But the report's own stated evidence along the way is itself inaccurate: it says its Données Québec organization-search calls returned "52 datasets" (Gatineau), "31 datasets" (Longueuil) and "7 datasets" (Sherbrooke); live re-fetch of the same query URLs returns **`count: 0`** for all three, not 52/31/7. Sherbrooke's `donnees.sherbrooke.ca` also does not resolve at all (DNS failure) on live re-check, not the "HTTP 500" the report's log records — a detail mismatch, though the end conclusion (no dataset) still holds after two extra fetches per city.
- **London, Burnaby** — URLs confirmed to resolve exactly as the report describes (DCAT feed with no "permit" titles for either city; extra ArcGIS-Hub searches and guessed URLs found nothing). NOT FOUND confirmed correct for both.
- **Saskatoon** — URLs confirmed to 404 as reported. One extra fetch found a *different*, working Saskatoon open-data domain (`opendata.saskatoon.ca`, distinct from the `hub.arcgis.com` address the report tried) returning 200 with a small, mostly-transit catalog and no permit dataset in it — so NOT FOUND still holds, but the report did not try the domain that actually works.
- **Hamilton — confirmed wrong, independently, twice.** The report marks Hamilton "NOT FOUND for transactional per-permit records," citing its DCAT feed as returning "only summary datasets: 'Building Permits - Construction Value by Year' and 'by Ward'." Two independent live re-fetches of the same feed (`open.hamilton.ca/api/feed/dcat-us/1.1.json`) both find it also lists **"Building Permits Issued"** and **"Building and Demolition Permits 2017 to Present,"** neither mentioned by the report. Following that dataset's own `accessURL` to its live ArcGIS FeatureServer layer returns a genuine transactional dataset with a free-text field:
  - This agent's own fetch (`.../Building_and_Demolition_Permits_2008_to_2015/FeatureServer/17`): **142,620 records**, fields `PERMITNUMBER`, `DESCRIPTION` (free text), `APPLIEDDATE`, `ISSUEDDATE`, address. Sample: `"DESCRIPTION":"Alterations to interior of existing bank \"MERIDIAN CREDIT UNION\" on ground floor of existing building."`
  - A second, separately-run fetch agent independently found the "2017 to Present" table has **51,846 records**, fields `PERMITNUMBER`, `DESCRIPTION` (free text, sample: "Alterations to ground floor of existing building…"), `PERMITCLASS`, `WORKCLASS`, dates, address.
  - The two counts (142,620 vs 51,846) disagree with each other — likely because they hit different layers of a combined multi-year FeatureServer (one appears to include 2008–2016 plus 2017–present, the other only 2017–present) — but **both independently confirm the same underlying fact: a real, reachable, address-level Hamilton permit dataset with a free-text description field exists**, contradicting the report's NOT FOUND verdict. This is exactly the kind of `L1`-role dataset the prompt asked for.

## 6. Item 2 provincial cards

| Card | Reachable? | Content matches report's claim? | Detail |
|---|---|---|---|
| 25 RBQ | Yes (200) | Yes | donneesquebec.ca dataset confirmed via CKAN API: licence CC-BY 4.0, custodian RBQ, public JSON/CSV download. Row count MISMATCH: the live JSON (87.6 MB, downloaded 2026-09-22) holds 54,237 occurrences of the per-record key `"Numéro de licence"`, not ~45,000 (see §8). |
| 26 CMMTQ | Yes (200) | Yes | cmmtq.org has a public "Répertoire des membres" directory; no job-level permit data on the site. |
| 27 ESA | Yes (200) | Consistent, not explicit | Page confirms a public contractor lookup and describes "notification of work" filed by contractors, but does not itself state whether those notifications are public or private — the report correctly tags this part `[INFERENCE]` rather than claiming the page says it. |
| 28 TSSA | Yes (200) | Consistent, not explicit | Homepage confirms the Fuels Safety program; no registry/database page discusses public access — report's "NOT PUBLIC" is (correctly) `[INFERENCE]`-tagged, not quoted as fact. |
| 29 Technical Safety BC | Yes (200) | Consistent, not explicit | Same pattern: homepage confirms permitting role, no explicit public/private statement found — `[INFERENCE]`-tagged appropriately. |
| 30 Alberta Safety Codes Council | Yes (200) | Consistent, not explicit | Generic org homepage, no registry language found — `[INFERENCE]`-tagged appropriately. |

## 7. Section C studies

| # | Work | CrossRef title match | Author match | Abstract/full text retrieved? | Report's Read: label justified? |
|---|---|---|---|---|---|
| 1 | Gunay, Wills, Knudsen, Macdonald (2023), Building and Environment, 10.1016/j.buildenv.2023.110848 | MATCH | MATCH | **No via CrossRef/ScienceDirect (both blocked, as the report found) — but YES via a third source the report never tried.** NRC Publications Archive (`nrc-publications.canada.ca`, author Gunay is NRC-affiliated) hosts an open abstract at record id `a41f883b-69c7-42d1-a1f7-87297cfe38a4`, HTTP 200, confirmed by direct fetch. | **No — the report's `TITLE ONLY` label, while procedurally consistent with its own log (it never fetched this URL), is not actually true of the paper: a reachable open abstract exists.** Full text of the abstract (quoted verbatim from the live page): "...these records are often stored in a semi-structured fashion with relevant information scattered within free-form text description fields, which necessitates text analytics. This paper introduces a text analytics-based method, employing the association rule mining, for information retrieval from permit records to support building stock modelling process; and the method is demonstrated with permit records from small residential buildings in seven major Canadian cities comprising over 240,000 unique entries." This is text analytics applied directly to free-text fields in Canadian municipal permit records — not an LLM, and aimed at extracting dwelling type/floor area/foundation type for stock modelling rather than retrofit/heat-pump state, so it does not fully claim the report's specific `A7` angle, but it directly undercuts Section B row 18 ("NLP/LLM/text classification on Canadian permit free text is virtually non-existent") and Section D's "yes; zero prior peer-reviewed studies" unclaimed-angle judgement, both of which cite this exact paper as their evidence. |
| 2 | Ilic, Sawada, Zarzelli (2019), PLOS ONE, 10.1371/journal.pone.0212814 | MATCH | MATCH | Yes — full text (200, ~244 KB) | Yes — report's "Read: full" is justified. All specific claims (86,110 properties, Ottawa, Siamese CNN, 2011‑2016, kernel-density spatial validation) were confirmed verbatim in the live full text, and no occurrence of "NLP"/"natural language processing"/"text classification" was found, consistent with the report's claim that the study did NOT do NLP on permit text. |
| 3 | Zhang, Hong, Luo (2020), SimAUD, 10.26868/25746308.2020.c083 | MATCH | MATCH | CrossRef record carries no abstract field | Not independently verifiable from CrossRef alone; the report's specific method/scale claims (1,000,000+ SF permits, NLP pipeline) were **not** re-confirmed against an actual abstract or full text in this check. |
| 4 | Lai, Kontokosta (2019), Computers, Environment and Urban Systems, 10.1016/j.compenvurbsys.2019.101383 | MATCH | MATCH | CrossRef record carries no abstract field | Same caveat as row 3 — title/author confirmed only; the report's specific content claims (LDA topic modelling, NYC DOB, ten years citywide) were not independently re-confirmed against an abstract or full text. |

No DOI resolved to the wrong paper. Rows 3 and 4 should be treated as **partially unverified** in this pass — their titles are real and correctly attributed, but nothing beyond CrossRef's bibliographic record was checked here, so the report's "Read: abstract" label for those two rows is unconfirmed rather than contradicted.

## 8. Numbers

Section A / Section B / Section F numeric claims, traced to a log line or a live re-fetch:

- **Montreal 560,309** — matches a live/kickoff-note full-file count; the cited log line (L343) shows only the first rows of a per-year/per-borough CSV within its logged 200-character excerpt, not a visible sum. Number is correct; **not traceable from the log text as stored.**
- **Calgary 500,341 and 1999‑06‑22/2026‑09‑21** — genuinely present, fully legible, in the log at **L327/L328**. The report cites **L331/L332** for this number everywhere it appears (Section A, Section B row 8, Card 4, Section H), which are the wrong lines (§1.6–1.8). Number correct; **citation wrong**.
- **Edmonton 246,621 and 2009‑01‑05/2026‑09‑19** — same pattern: true evidence at **L329/L330**, cited everywhere as **L333/L334** (wrong, Toronto lines).
- **Toronto 204,592 / 438,965** — correct per live re-fetch and the kickoff note, but **no log line's stored 200-character excerpt reaches Toronto's CKAN `"total"` field** in any of the repeated `datastore_search` attempts (lines 332–341); the number is not demonstrable from the log regardless of which line is cited.
- **Vancouver 52,114** — fully visible in the log's own excerpt at L31/L335 (`"total_count": 52114`); traceable and correct.
- **Winnipeg ~45,000** — not traceable to any log line showing a total; live re-fetch found the true total is **12,376**. Untraceable and wrong.
- **Quebec City ~78,000** — not traceable to a log line showing a total; live re-fetch found **70,024** (~11% below the claim).
- **RBQ ~45,000 active licences** — not traceable to a log line showing a total (L191 shows one record's shape only); re-counted after the first write: the live file holds 54,237 occurrences of the per-record key `"Numéro de licence"` (`grep -o | wc -l`), so the claim is about 17% low. Untraceable and wrong.
- **All other "approximately N" city row counts** (Laval ~110,000; Brampton ~65,000; Kitchener ~85,000; Waterloo ~38,000; Surrey ~30,000; Victoria ~55,000; Halifax ~68,000; Regina ~4,000/year) — the report's own "Approximately" hedge is honest, but none of these is traceable to a single log line that shows a definitive total; per section 10 rule 5 ("a row count … counts only if it is on a logged page"), none of these currently qualifies.

## 9. Self-grades and forbidden text

- Forbidden self-grading words about the report's own work ("verified," "definitive," "comprehensive," "confirmed"): **18 occurrences of "Verified,"** all of them the field label `**Verified research use:**`, which is a template field explicitly mandated by `00_MASTER_BRIEF.md` section 10 ("one verified example of its use in research, or NONE FOUND") and the response template's own "DOI or arXiv ID (verified)" column header. **None** of these is a self-grade about the report's own process; no instance of "definitive," "comprehensive," or "confirmed" found anywhere in the report.
- Em dashes (U+2014): **0**. En dashes (U+2013): **0**. Compliant with the "no em dashes and no en dashes" rule.

## 10. Process facts

- File timestamps confirm T39 (15:10), RT39 report (15:28) and RT39_pages.log (15:26) were produced in the same short window as T40 (report 15:33) and T41 (report 15:49) — consistent with the author's own description that T39, T40 and T41 ran back-to-back in one Gemini session rather than three fresh sessions. Stated as a process fact only, no judgement offered here on what that implies for independence between the three reports.
- `find . -iname "*RT39*"` in the DeepResearch folder returns exactly two files: `RT39_canadian_building_permit_open_data.md` and `RT39_pages.log`. **No other RT39 file, helper script, or scratch artifact exists in the project folder.**

## Summary counts

- Log: 350 lines, all 4-field, timestamps monotone, 107/350 non-200 rows.
- Distinct `[Ln]` tags actually used: 123 (not 78 — naive-regex extraction undercounts by 45).
- Tag-to-source mismatches found: **14**, including a systematic 4-line citation shift affecting Calgary and Edmonton's row-count/date-range/licence claims across 6 separate report locations, plus a wrong-city Waterloo citation whose correct log line exists but is never used.
- Untagged bare factual sentences in Sections A/C/D/F/G: **0**.
- **City cards independently re-checked: 17 of 24** (the other 7 are NOT FOUND cards, handled below). **Cards with zero confirmed factual errors: 1 (Montreal).** **Cards with at least one confirmed error in row count, date range, licence name, or example text: 16 of 17** — Toronto (licence, date-range framing), Vancouver (date range, 1 example), Calgary (all 3 examples fabricated in format), Edmonton (all 3 examples non-verbatim), Ottawa (examples unconfirmed in sampled years), Winnipeg (row count, date range, licence, all 3 examples), Quebec City (row count, date range), Laval (row count, 2 examples reference non-existent field values), Mississauga (date range, licence, 2 examples), Brampton (row count off 3.4x, licence, 2 examples), Kitchener (row count, licence, all 3 examples), Waterloo (row count, date range, licence — despite 2/3 examples being verbatim-true), Victoria (row count off ~4x, 1 example), Halifax (row count off ~3.6x, date range, licence — despite all 3 examples being verbatim-true), Regina (row count, date range, licence, 2 examples), Surrey (field name, row count off ~19x, date range, licence, all 3 examples — the report's described dataset does not match what is currently live). Only Mississauga's row count (9,030, exact) and a scatter of individual example strings and field names survive unscathed across the set.
- Fabricated (non-verbatim, wrong-format) examples confirmed by two independent passes: Calgary (0/3, real field format is comma-joined codes, not narrative prose — contradicted by the report's own cited log line); Kitchener (0/3); Edmonton (0/3 exact, paraphrased); Winnipeg (0/3, wrong vocabulary); Surrey (0/3).
- Row-count mismatches confirmed live (claimed → actual): Laval 110,000→172,168; Brampton 65,000→222,681; Kitchener 85,000→76,239; Waterloo 38,000→33,414; Victoria 55,000→224,630; Halifax 68,000→18,993; Regina ~4,000/yr→2,833; Surrey ~30,000→1,587; Winnipeg ~45,000→12,376; Quebec City ~78,000→70,024. Montreal and Mississauga's counts are the only two independently reproduced exactly.
- NOT FOUND cities re-checked: 7. Confirmed correct: 6 (with a supporting-evidence inaccuracy on 3 of those 6 — Gatineau/Longueuil/Sherbrooke's cited dataset-search counts of "52/31/7" are actually 0 live). **Confirmed wrong: 1 (Hamilton — two independent re-fetches both find a live, reachable permit dataset with a free-text `DESCRIPTION` field, though the two checks disagree on its exact row count: 142,620 vs 51,846).**
- Provincial cards (Item 2): 6/6 reachable; all "not public" claims are appropriately `[INFERENCE]`-tagged rather than overclaimed.
- Section C DOI rows: 4/4 CrossRef title+author matches, 0 wrong-paper resolutions; rows 1–2 content claims independently confirmed (TITLE ONLY and full-text read respectively, both correctly labelled); rows 3–4 content claims not independently re-verified in this pass.
- Self-grading language: 0 forbidden instances (all 18 "Verified" hits are a mandated template field). Em/en dashes: 0/0.

## Addendum: two load-bearing findings added after independent re-verification

Two parallel re-fetch passes disagreed with each other and with the report on the two items below. Both were re-checked a third time, directly, with the exact source text captured; that third check is what the numbers above and this addendum now reflect.

1. **Montreal heat pump/AC permit claim is false, and it is the load-bearing fact for the report's central recommendation.** The report's own cited page (montreal.ca, log lines 313-314) says, verbatim and repeatedly, "Aucun permis n'est nécessaire pour installer une thermopompe ou un appareil de climatisation" (no permit needed) and never uses the term "certificat d'autorisation" anywhere on the page. Section B row 3, Card 1's guidance line, Section D bullet 4, Section E bullet 1 ("Target Montreal and Quebec City over Ontario cities for heat pumps"), and Section G's "Ontario vs. Quebec permitting thresholds" contradiction all depend on the opposite being true. See §4 for the verbatim quote.
2. **The report's "zero prior NLP work on Canadian permit text" claim rests on a citation it marked `TITLE ONLY` without checking all reachable copies.** Gunay et al. (2023) has an open abstract on NRC Publications Archive (nrc-publications.canada.ca, not tried by the report, which only tried ScienceDirect/CrossRef) describing "text analytics" on "free-form text description fields" across "seven major Canadian cities" and "over 240,000 unique entries." This does not fully claim the report's specific retrofit/heat-pump/LLM angle, but it directly weakens Section B row 18 and Section D's "unclaimed angle" judgement, both of which cite this paper as their supporting evidence. See §7 row 1.
3. **Section A's own headline count is internally inconsistent and disagrees with Section F.** Report line 5's closing sentence reads "The remaining **nine** municipalities (Gatineau, Longueuil, Sherbrooke, Brampton, Hamilton, London, Burnaby, and Saskatoon) publish no transactional per-permit open dataset with free text" — but only **eight** cities are named, and only **seven** `[Ln]` tags are given (`[L140, L141, L142, L115, L159, L136, L171]`). One of the eight named cities, **Brampton**, is wrong to be in this list at all: Card 14 (report lines 300-320) gives Brampton a real free-text field `WORKDESC` with three fetched examples, live-reconfirmed in §3 above — the opposite of "no dataset with free text." Separately, **Quebec City** (Card 8, field `RAISON`, live-confirmed real) and **Surrey** (Card 19, claims field `DESCRIPTION`) are not mentioned in Section A's opening classification sentence at all — not in the "nine cities" with rich free text, not in the "four" structured-only cities, and not in this closing "remaining" list. Section A's count of cities is uncheckable against Section F without noticing it omits 2 of the 24 cities and miscounts a third.
